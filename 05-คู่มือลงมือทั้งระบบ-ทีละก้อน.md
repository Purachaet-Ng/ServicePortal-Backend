# 05 — คู่มือลงมือทั้งระบบจากบนลงล่างทีละก้อน

คู่มือนี้เป็นคู่มือพิเศษที่ใช้ต่อจาก `00-SETUP-ก่อนเริ่มโปรเจกต์.md` เพียงไฟล์เดียว เมื่อทำ `00` ครบแล้วให้เปิด `05` และทำจากบนลงล่างจนจบได้ทันที ไม่ต้องสลับไปเปิดคู่มือไฟล์อื่นระหว่างทาง

```text
00-SETUP-ก่อนเริ่มโปรเจกต์.md → 05-คู่มือลงมือทั้งระบบ-ทีละก้อน.md → เว็บที่รันได้
```

โค้ด Setup ที่เป็นเจ้าของโดยคู่มือ 00 ให้วางจากคู่มือ 00 เพียงครั้งเดียว ส่วนโค้ดระบบที่เป็นเจ้าของโดยคู่มือ 05 มี Code block ครบอยู่ในไฟล์นี้ ห้ามคัดลอกไฟล์ Setup ซ้ำ ห้ามข้ามหัวข้อ และห้ามเดาขั้นตอนเพิ่มเอง

## จุดรับช่วงจากคู่มือ 00 — ตรวจครั้งเดียวก่อนเริ่ม

คู่มือ 05 พึ่งพาเฉพาะ Setup จากคู่มือ 00 ก่อนเริ่ม CHECKPOINT 1 ให้ตรวจจาก Terminal `BackEnd`:

```powershell
npm ls --depth=0
node --check scripts/createDatabase.js
```

ต้องครบทุกข้อ:

- ไม่มี `UNMET DEPENDENCY` หรือ `npm ERR!`
- `BackEnd/.env` ใช้ `PORT=5000` และตั้ง `DATABASE_URL`/`DATABASE_NAME` เป็น `trash_toilet_japan` เหมือนกัน
- `scripts/createDatabase.js` มี `allowPublicKeyRetrieval: true` ภายใน `mariadb.createConnection({ ... })`
- ในคู่มือ 00 ส่วน B เคยรัน `npm run db:create` และเห็น `Database trash_toilet_japan is ready` แล้ว

ไม่ต้องรัน `npm run db:create` ซ้ำในคู่มือ 05 เพราะ Database ที่สร้างจากคู่มือ 00 ยังอยู่ใน MariaDB ให้รันซ้ำเฉพาะกรณีที่ยังไม่เคยเห็นข้อความสำเร็จในคู่มือ 00 หรือ Database ถูกลบเท่านั้น

ถ้าการตรวจไม่ผ่าน หรือคู่มือ 00 ยังสร้าง Database ไม่สำเร็จ ให้กลับไปแก้คู่มือ 00 ส่วน B ข้อ 4.1–4.2 ก่อน ห้ามเริ่ม CHECKPOINT 1 เพราะ CHECKPOINT 2–6 ต้องใช้ Database นี้ต่อกัน ส่วน CHECKPOINT 7–10 เรียก Backend จากชุดเดียวกัน

ก่อนเริ่ม ตรวจ `BackEnd/.env` ให้เป็น `PORT=5000` และเมื่อถึง Frontend ให้ `VITE_API_URL=http://localhost:5000/api`

## กติกา CHECKPOINT — ห้ามข้ามเมื่อมี Error

ในคู่มือนี้ ทุกข้อความ **“เมื่อคัดครบไฟล์ ให้รัน...”** คือ CHECKPOINT บังคับ ให้ทำตามวงจรนี้ทุกครั้ง:

```text
วางโค้ดของไฟล์ให้ครบ → บันทึก → รันคำสั่ง CHECKPOINT → ผ่านจึงไปไฟล์ถัดไป
                                                ↳ ถ้ามี Error ให้หยุดแก้ตรงนี้
```

- Backend ใช้ `node --check <ชื่อไฟล์>` หลังทุกไฟล์ เพื่อตรวจ syntax ทันที
- Prisma ใช้ `validate → db push → generate → studio` หลัง Schema ครบ
- Frontend ใช้ `npm run lint` หลังทุกไฟล์ที่คู่มือระบุ
- `npm run dev` ใช้เฉพาะ CHECKPOINT ที่ส่วนประกอบพร้อมแล้ว ได้แก่ Health Check ชั่วคราว, Backend ฉบับเต็ม และ Frontend หลัง `main.jsx` เพื่อไม่ให้เจอ error จากไฟล์ที่ยังทำไม่ครบ
- ถ้าคำสั่งไม่คืน Terminal เช่น `npm run dev` หรือ `prisma studio` ให้ตรวจผลแล้วกด `Ctrl+C` ก่อนทำขั้นถัดไป

## ลำดับเชื่อมระบบ

```text
Database → Prisma Client → Backend Schema/Controller/Middleware/Route → server mount
→ API test → Frontend api.js → Context/Provider → Component/Page → App Route
→ Browser test → lint → build
```

---

# ส่วน E — Database และ Backend


ทำตามหัวข้อจากบนลงล่าง โค้ดทุกก้อนมาจากต้นฉบับที่ใช้ QC แล้ว ห้ามคัดข้ามหมายเลข และห้ามวางใน `เฉลย/` หากก้อนหนึ่งเปิดปีกกาไว้ ให้คัดก้อนของ function นั้นให้ครบก่อนบันทึก

## B1 ชั่วคราว — เปิด Health Check ก่อนเชื่อม Route

หลังทำ `00-SETUP-ก่อนเริ่มโปรเจกต์.md` ครบทุกข้อแล้ว ให้เปิด `BackEnd/src/server.js` และใส่ก้อนชั่วคราวนี้เพื่อพิสูจน์ว่า Express เปิดได้ โดยยังไม่ import Route:

```js
import "dotenv/config";
import express from "express";

const app = express();
// app คือ Express application ที่รับ request จาก Frontend/Postman
const PORT = Number(process.env.PORT || 5000);

app.use(express.json());
// อ่าน JSON Body ก่อนส่ง request ต่อไปยัง Routes

app.get("/", (req, res) => {
// Route ง่าย ๆ สำหรับตรวจว่า Backend เปิดอยู่หรือไม่
  res.json({ status: "ok", message: "Trash & Toilet Japan API" });
});

app.listen(PORT, () => {
// เริ่ม Server เป็นลำดับสุดท้าย หลัง Middleware และ Routes พร้อมแล้ว
  console.log(`Server running at http://localhost:${PORT}`);
});
```

รัน `npm run dev` และเปิด `http://localhost:5000/` ต้องได้ status ok จากนั้นหยุดด้วย `Ctrl+C` เมื่อทำ Controller/Middleware/Route ครบ คู่มือจะให้ประกอบ `server.js` ฉบับเต็มแทนก้อนชั่วคราวนี้

### CHECKPOINT 1 — RUN DEV: Express เริ่มทำงาน

ต้องเห็น `Server running at http://localhost:5000` และ Browser ต้องได้ JSON ที่มี `status: "ok"` ถ้าไม่ผ่าน ห้ามเริ่มส่วนตรวจรับ Setup จากคู่มือ 00

## หัวข้อ 1–4 ทำเสร็จจากคู่มือ 00 แล้ว — ตรวจเท่านั้น ห้ามทำซ้ำ

คู่มือ 00 แบ่ง Setup ที่ส่งต่อมายังคู่มือ 05 ดังนี้:

| ส่วนในคู่มือ 00 | ไฟล์/ผลลัพธ์ที่ต้องพร้อม | คู่มือ 05 ใช้เมื่อใด |
|---|---|---|
| ส่วน A — Backend Dependencies | `BackEnd/package.json`, `node_modules` | CHECKPOINT 1 และ Backend ทุกหัวข้อ |
| ส่วน B — Environment และ Database | `.env`, `scripts/createDatabase.js`, Database ว่าง | CHECKPOINT 2–6 |
| ส่วน C — Prisma | `prisma.config.ts`, `prisma/schema.prisma`, `src/lib/prisma.js`, generated client | Controller ทุกระบบ |
| ส่วน D — Frontend Setup | Frontend package/config/env/assets | CHECKPOINT 7–10 |

ห้ามลบหรือวางโค้ดทับ `package.json`, `prisma.config.ts`, `prisma/schema.prisma` และ `src/lib/prisma.js` ในคู่มือ 05 เพราะโค้ดเต็มอยู่ในคู่มือ 00 แล้ว ให้ตรวจรับงานด้วยคำสั่งต่อไปนี้จาก `BackEnd`:

```powershell
npm ls --depth=0
node --check scripts/createDatabase.js
npx prisma validate
npx prisma db push
npx prisma generate
```

ผลที่ต้องได้:

- ไม่มี `UNMET DEPENDENCY` หรือ `npm ERR!`
- Database จากคู่มือ 00 ส่วน B ยังอยู่ และ MariaDB เปิดทำงาน
- `validate`, `db push` และ `generate` ผ่าน
- มี `BackEnd/generated/prisma/client.js`
- `src/lib/prisma.js` มี `allowPublicKeyRetrieval: true`

### CHECKPOINT 2 — Schema, Database และ Prisma Client

เมื่อคำสั่งตรวจรับงานทั้งหมดผ่าน ให้รันจาก `BackEnd`:

```powershell
npm run dev
```

ต้องเห็น `Server running at http://localhost:5000` และเปิด `http://localhost:5000/` แล้วยังได้ JSON ที่มี `status: "ok"` จากนั้นกด `Ctrl+C` ถือว่า CHECKPOINT 2 ผ่าน ไม่ต้องเปิด Prisma Studio ซ้ำถ้าตรวจตาราง `User`, `Place`, `Report` ในคู่มือ 00 แล้ว หากข้อใด Error ให้กลับไปแก้ **คู่มือ 00 ส่วนที่ตรงกับตารางด้านบน** ก่อนเริ่มหัวข้อ 5

## 5. `BackEnd/src/validations/schemas.js`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนต่อไปนี้เรียงกัน

### 5.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `BackEnd/src/validations/schemas.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/validations/schemas.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 5.1 ทำหน้าที่ `import { z } from "zod";` โดยสร้างกฎ Zod ให้ Backend ตรวจข้อมูลที่ผู้ใช้หรือ URL ส่งเข้ามาก่อนส่งต่อให้ Controller ช่วยกันข้อมูลผิดรูปแบบไม่ให้ไปถึง Database

**โค้ดก้อนนี้ทำงานอย่างไร:** Validation middleware จะนำ `req.body`, `req.params` หรือ `req.query` มาเทียบกับ Schema นี้ ถ้าถูกต้องจะส่งค่าที่แปลงและตรวจแล้วไปทำงานต่อ; ถ้าไม่ถูกต้อง API จะตอบสถานะ 400 พร้อมรายละเอียด โดย Controller จะยังไม่ทำงาน

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Route นำ Schema นี้ไปใช้ ช่องกรอก ฟอร์ม Query หรือค่า `:id` ที่เกี่ยวข้องจะถูกตรวจฝั่ง Backend ก่อนเรียก Controller ข้อมูลผิดจะหยุดที่ Validation และไม่ถูกบันทึกลง Database

```js
import { z } from "zod";
// ลำดับอ่าน 3D: ไฟล์รวมกติกาข้อมูลที่ Routes ส่งให้ validateMiddleware | Schema บอกว่า field ใดจำเป็น เป็นชนิดใด และอนุญาตค่าอะไรบ้าง
```

### 5.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/validations/schemas.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/validations/schemas.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 5.2 ทำหน้าที่ `Register รับข้อมูลจาก POST /api/auth/register` โดยสร้างกฎ Zod ให้ Backend ตรวจข้อมูลที่ผู้ใช้หรือ URL ส่งเข้ามาก่อนส่งต่อให้ Controller ช่วยกันข้อมูลผิดรูปแบบไม่ให้ไปถึง Database

**โค้ดก้อนนี้ทำงานอย่างไร:** Validation middleware จะนำ `req.body`, `req.params` หรือ `req.query` มาเทียบกับ Schema นี้ ถ้าถูกต้องจะส่งค่าที่แปลงและตรวจแล้วไปทำงานต่อ; ถ้าไม่ถูกต้อง API จะตอบสถานะ 400 พร้อมรายละเอียด โดย Controller จะยังไม่ทำงาน `.email()` ตรวจรูปแบบอีเมล และ `.toLowerCase()` ทำให้รูปแบบอีเมลสม่ำเสมอ `.min()` ป้องกันข้อความว่างหรือค่าที่สั้นกว่ากำหนด `.max()` จำกัดความยาวเพื่อป้องกันข้อมูลเกินขอบเขต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Route นำ Schema นี้ไปใช้ ช่องกรอก ฟอร์ม Query หรือค่า `:id` ที่เกี่ยวข้องจะถูกตรวจฝั่ง Backend ก่อนเรียก Controller ข้อมูลผิดจะหยุดที่ Validation และไม่ถูกบันทึกลง Database

```js
// Register รับข้อมูลจาก POST /api/auth/register
export const registerSchema = z.object({
  // trim() ตัดช่องว่างหัวท้าย และห้ามชื่อว่างหรือยาวเกิน 100 ตัวอักษร
  name: z.string().trim().min(1, "กรุณากรอกชื่อ").max(100, "ชื่อยาวเกินไป"),
  // ตรวจรูปแบบ email และเปลี่ยนเป็นตัวพิมพ์เล็กก่อนส่งให้ Controller
  email: z.string().trim().email("รูปแบบอีเมลไม่ถูกต้อง").toLowerCase(),
  // รหัสผ่านต้องเป็นข้อความและยาวอย่างน้อย 6 ตัวอักษร
  password: z.string().min(6, "รหัสผ่านต้องมีอย่างน้อย 6 ตัวอักษร"),
  // รหัสผ่านต้องเป็นข้อความและยาวอย่างน้อย 6 ตัวอักษร
});
```

### 5.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/validations/schemas.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/validations/schemas.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 5.3 ทำหน้าที่ `Login รับข้อมูลจาก POST /api/auth/login` โดยสร้างกฎ Zod ให้ Backend ตรวจข้อมูลที่ผู้ใช้หรือ URL ส่งเข้ามาก่อนส่งต่อให้ Controller ช่วยกันข้อมูลผิดรูปแบบไม่ให้ไปถึง Database

**โค้ดก้อนนี้ทำงานอย่างไร:** Validation middleware จะนำ `req.body`, `req.params` หรือ `req.query` มาเทียบกับ Schema นี้ ถ้าถูกต้องจะส่งค่าที่แปลงและตรวจแล้วไปทำงานต่อ; ถ้าไม่ถูกต้อง API จะตอบสถานะ 400 พร้อมรายละเอียด โดย Controller จะยังไม่ทำงาน `.email()` ตรวจรูปแบบอีเมล และ `.toLowerCase()` ทำให้รูปแบบอีเมลสม่ำเสมอ `.min()` ป้องกันข้อความว่างหรือค่าที่สั้นกว่ากำหนด

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Route นำ Schema นี้ไปใช้ ช่องกรอก ฟอร์ม Query หรือค่า `:id` ที่เกี่ยวข้องจะถูกตรวจฝั่ง Backend ก่อนเรียก Controller ข้อมูลผิดจะหยุดที่ Validation และไม่ถูกบันทึกลง Database

```js
// Login รับข้อมูลจาก POST /api/auth/login
export const loginSchema = z.object({
  // จัดรูป email ให้เหมือนตอน Register เพื่อค้นหาใน Database ได้ตรงกัน
  email: z.string().trim().email("รูปแบบอีเมลไม่ถูกต้อง").toLowerCase(),
  // Login ตรวจเพียงว่ามี password ส่วน bcrypt จะตรวจว่าถูกต้องใน Controller
  password: z.string().min(1, "กรุณากรอกรหัสผ่าน"),
  // Login ตรวจเพียงว่ามี password ส่วน bcrypt จะตรวจว่าถูกต้องใน Controller
});
```

### 5.4 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/validations/schemas.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/validations/schemas.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 5.4 ทำหน้าที่ `ตรวจ :id ใน URL ของ Place เช่น /api/places/1` โดยสร้างกฎ Zod ให้ Backend ตรวจข้อมูลที่ผู้ใช้หรือ URL ส่งเข้ามาก่อนส่งต่อให้ Controller ช่วยกันข้อมูลผิดรูปแบบไม่ให้ไปถึง Database

**โค้ดก้อนนี้ทำงานอย่างไร:** Validation middleware จะนำ `req.body`, `req.params` หรือ `req.query` มาเทียบกับ Schema นี้ ถ้าถูกต้องจะส่งค่าที่แปลงและตรวจแล้วไปทำงานต่อ; ถ้าไม่ถูกต้อง API จะตอบสถานะ 400 พร้อมรายละเอียด โดย Controller จะยังไม่ทำงาน ในก้อนนี้ค่าจาก URL เริ่มต้นเป็นข้อความ จึงใช้ `z.coerce.number()` แปลงเป็นตัวเลข แล้ว `.int()` และ `.positive()` บังคับให้เป็นจำนวนเต็มมากกว่า 0

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Route ผูก Schema นี้กับ `params` ค่า `:id` จาก URL เช่น `/api/places/1` หรือ `/api/reports/1` จะถูกตรวจฝั่ง Backend ก่อนเรียก Controller โดยค่านี้ไม่ใช่ช่อง input ในฟอร์ม แต่เป็นเลขที่อยู่ในเส้นทาง URL; ถ้าไม่ใช่จำนวนเต็มบวก API จะตอบ 400 และไม่แตะ Database

```js
// ตรวจ :id ใน URL ของ Place เช่น /api/places/1
export const placeIdSchema = z.object({
  // Params เริ่มเป็น string จึงใช้ coerce แปลงเป็นเลขจำนวนเต็มบวก
  id: z.coerce.number().int().positive("Place id ต้องมากกว่า 0"),
  // Params เริ่มเป็น string จึงใช้ coerce แปลงเป็นเลขจำนวนเต็มบวก
});
```

### 5.5 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/validations/schemas.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/validations/schemas.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 5.5 ทำหน้าที่ `ตรวจ Body ตอน Member/Admin เพิ่มสถานที่` โดยสร้างกฎ Zod ให้ Backend ตรวจข้อมูลที่ผู้ใช้หรือ URL ส่งเข้ามาก่อนส่งต่อให้ Controller ช่วยกันข้อมูลผิดรูปแบบไม่ให้ไปถึง Database

**โค้ดก้อนนี้ทำงานอย่างไร:** Validation middleware จะนำ `req.body`, `req.params` หรือ `req.query` มาเทียบกับ Schema นี้ ถ้าถูกต้องจะส่งค่าที่แปลงและตรวจแล้วไปทำงานต่อ; ถ้าไม่ถูกต้อง API จะตอบสถานะ 400 พร้อมรายละเอียด โดย Controller จะยังไม่ทำงาน ในก้อนนี้ค่าจาก URL เริ่มต้นเป็นข้อความ จึงใช้ `z.coerce.number()` แปลงเป็นตัวเลข แล้ว `.int()` และ `.positive()` บังคับให้เป็นจำนวนเต็มมากกว่า 0 `.min()` ป้องกันข้อความว่างหรือค่าที่สั้นกว่ากำหนด `.max()` จำกัดความยาวเพื่อป้องกันข้อมูลเกินขอบเขต `z.enum()` อนุญาตเฉพาะค่าที่ระบบรองรับ จึงกันข้อความสถานะหรือประเภทที่สะกดผิด

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Route ผูก Schema นี้กับ `params` ค่า `:id` จาก URL เช่น `/api/places/1` หรือ `/api/reports/1` จะถูกตรวจฝั่ง Backend ก่อนเรียก Controller โดยค่านี้ไม่ใช่ช่อง input ในฟอร์ม แต่เป็นเลขที่อยู่ในเส้นทาง URL; ถ้าไม่ใช่จำนวนเต็มบวก API จะตอบ 400 และไม่แตะ Database

```js
// ตรวจ Body ตอน Member/Admin เพิ่มสถานที่
export const createPlaceSchema = z.object({
  // ชื่อและที่อยู่ต้องไม่ว่าง พร้อมจำกัดความยาวไม่ให้ข้อมูลใหญ่เกินไป
  name: z.string().trim().min(1, "กรุณากรอกชื่อสถานที่").max(150),
  // enum อนุญาตเฉพาะประเภทที่ schema.prisma กำหนดไว้
  type: z.enum(["toilet", "trash"], {
  // enum อนุญาตเฉพาะประเภทที่ schema.prisma กำหนดไว้
    message: "type ต้องเป็น toilet หรือ trash",
  }),
  address: z.string().trim().min(1, "กรุณากรอกที่อยู่").max(255),
  // latitude อยู่ระหว่าง -90 ถึง 90 และ longitude อยู่ระหว่าง -180 ถึง 180
  latitude: z.coerce.number().min(-90).max(90),
  longitude: z.coerce.number().min(-180).max(180),
});
```

### 5.6 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/validations/schemas.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/validations/schemas.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 5.6 ทำหน้าที่ `ตรวจ Query ของ GET /api/places เช่น ?type=toilet&status=active` โดยสร้างกฎ Zod ให้ Backend ตรวจข้อมูลที่ผู้ใช้หรือ URL ส่งเข้ามาก่อนส่งต่อให้ Controller ช่วยกันข้อมูลผิดรูปแบบไม่ให้ไปถึง Database

**โค้ดก้อนนี้ทำงานอย่างไร:** Validation middleware จะนำ `req.body`, `req.params` หรือ `req.query` มาเทียบกับ Schema นี้ ถ้าถูกต้องจะส่งค่าที่แปลงและตรวจแล้วไปทำงานต่อ; ถ้าไม่ถูกต้อง API จะตอบสถานะ 400 พร้อมรายละเอียด โดย Controller จะยังไม่ทำงาน `.max()` จำกัดความยาวเพื่อป้องกันข้อมูลเกินขอบเขต `z.enum()` อนุญาตเฉพาะค่าที่ระบบรองรับ จึงกันข้อความสถานะหรือประเภทที่สะกดผิด

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Route นำ Schema นี้ไปใช้ ช่องกรอก ฟอร์ม Query หรือค่า `:id` ที่เกี่ยวข้องจะถูกตรวจฝั่ง Backend ก่อนเรียก Controller ข้อมูลผิดจะหยุดที่ Validation และไม่ถูกบันทึกลง Database

```js
// ตรวจ Query ของ GET /api/places เช่น ?type=toilet&status=active
export const placesQuerySchema = z.object({
  // optional() หมายถึงไม่ส่ง field นี้มาก็ได้
  type: z.enum(["toilet", "trash"]).optional(),
  // optional() หมายถึงไม่ส่ง field นี้มาก็ได้
  search: z.string().trim().max(150).optional(),
  // all ใช้สำหรับ Admin ขอดู Place ทุกสถานะ โดย Controller จะตรวจ role อีกครั้ง
  status: z
  // all ใช้สำหรับ Admin ขอดู Place ทุกสถานะ โดย Controller จะตรวจ role อีกครั้ง
    .enum(["pending", "active", "rejected", "inactive", "all"])
    .optional(),
});
```

### 5.7 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/validations/schemas.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/validations/schemas.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 5.7 ทำหน้าที่ `ตรวจ GPS Query ของ GET /api/places/nearby` โดยสร้างกฎ Zod ให้ Backend ตรวจข้อมูลที่ผู้ใช้หรือ URL ส่งเข้ามาก่อนส่งต่อให้ Controller ช่วยกันข้อมูลผิดรูปแบบไม่ให้ไปถึง Database

**โค้ดก้อนนี้ทำงานอย่างไร:** Validation middleware จะนำ `req.body`, `req.params` หรือ `req.query` มาเทียบกับ Schema นี้ ถ้าถูกต้องจะส่งค่าที่แปลงและตรวจแล้วไปทำงานต่อ; ถ้าไม่ถูกต้อง API จะตอบสถานะ 400 พร้อมรายละเอียด โดย Controller จะยังไม่ทำงาน ในก้อนนี้ค่าจาก URL เริ่มต้นเป็นข้อความ จึงใช้ `z.coerce.number()` แปลงเป็นตัวเลข แล้ว `.int()` และ `.positive()` บังคับให้เป็นจำนวนเต็มมากกว่า 0 `.min()` ป้องกันข้อความว่างหรือค่าที่สั้นกว่ากำหนด `.max()` จำกัดความยาวเพื่อป้องกันข้อมูลเกินขอบเขต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Route ผูก Schema นี้กับ `params` ค่า `:id` จาก URL เช่น `/api/places/1` หรือ `/api/reports/1` จะถูกตรวจฝั่ง Backend ก่อนเรียก Controller โดยค่านี้ไม่ใช่ช่อง input ในฟอร์ม แต่เป็นเลขที่อยู่ในเส้นทาง URL; ถ้าไม่ใช่จำนวนเต็มบวก API จะตอบ 400 และไม่แตะ Database

```js
// ตรวจ GPS Query ของ GET /api/places/nearby
export const nearbyQuerySchema = z.object({
  // Query เป็น string จึงใช้ coerce แปลงเป็น number ก่อนตรวจช่วงพิกัด
  latitude: z.coerce.number().min(-90).max(90),
  longitude: z.coerce.number().min(-180).max(180),
  // radius เป็นกิโลเมตร ต้องมากกว่า 0 และจำกัดสูงสุดไว้ที่ 100 กิโลเมตร
  radius: z.coerce.number().positive().max(100).optional(),
  // radius เป็นกิโลเมตร ต้องมากกว่า 0 และจำกัดสูงสุดไว้ที่ 100 กิโลเมตร
});
```

### 5.8 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/validations/schemas.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/validations/schemas.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 5.8 ทำหน้าที่ `ตรวจ Body ตอน Admin เปลี่ยนสถานะ Place` โดยสร้างกฎ Zod ให้ Backend ตรวจข้อมูลที่ผู้ใช้หรือ URL ส่งเข้ามาก่อนส่งต่อให้ Controller ช่วยกันข้อมูลผิดรูปแบบไม่ให้ไปถึง Database

**โค้ดก้อนนี้ทำงานอย่างไร:** Validation middleware จะนำ `req.body`, `req.params` หรือ `req.query` มาเทียบกับ Schema นี้ ถ้าถูกต้องจะส่งค่าที่แปลงและตรวจแล้วไปทำงานต่อ; ถ้าไม่ถูกต้อง API จะตอบสถานะ 400 พร้อมรายละเอียด โดย Controller จะยังไม่ทำงาน `z.enum()` อนุญาตเฉพาะค่าที่ระบบรองรับ จึงกันข้อความสถานะหรือประเภทที่สะกดผิด

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Route นำ Schema นี้ไปใช้ ช่องกรอก ฟอร์ม Query หรือค่า `:id` ที่เกี่ยวข้องจะถูกตรวจฝั่ง Backend ก่อนเรียก Controller ข้อมูลผิดจะหยุดที่ Validation และไม่ถูกบันทึกลง Database

```js
// ตรวจ Body ตอน Admin เปลี่ยนสถานะ Place
export const placeStatusSchema = z.object({
  // รับเฉพาะค่าที่มีอยู่ใน PlaceStatus ของ schema.prisma
  status: z.enum(["pending", "active", "rejected", "inactive"]),
  // รับเฉพาะค่าที่มีอยู่ใน PlaceStatus ของ schema.prisma
});
```

### 5.9 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/validations/schemas.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/validations/schemas.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 5.9 ทำหน้าที่ `ตรวจ Body ตอน Member/Admin ส่ง Report` โดยสร้างกฎ Zod ให้ Backend ตรวจข้อมูลที่ผู้ใช้หรือ URL ส่งเข้ามาก่อนส่งต่อให้ Controller ช่วยกันข้อมูลผิดรูปแบบไม่ให้ไปถึง Database

**โค้ดก้อนนี้ทำงานอย่างไร:** Validation middleware จะนำ `req.body`, `req.params` หรือ `req.query` มาเทียบกับ Schema นี้ ถ้าถูกต้องจะส่งค่าที่แปลงและตรวจแล้วไปทำงานต่อ; ถ้าไม่ถูกต้อง API จะตอบสถานะ 400 พร้อมรายละเอียด โดย Controller จะยังไม่ทำงาน ในก้อนนี้ค่าจาก URL เริ่มต้นเป็นข้อความ จึงใช้ `z.coerce.number()` แปลงเป็นตัวเลข แล้ว `.int()` และ `.positive()` บังคับให้เป็นจำนวนเต็มมากกว่า 0 `.min()` ป้องกันข้อความว่างหรือค่าที่สั้นกว่ากำหนด `.max()` จำกัดความยาวเพื่อป้องกันข้อมูลเกินขอบเขต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Route ผูก Schema นี้กับ `params` ค่า `:id` จาก URL เช่น `/api/places/1` หรือ `/api/reports/1` จะถูกตรวจฝั่ง Backend ก่อนเรียก Controller โดยค่านี้ไม่ใช่ช่อง input ในฟอร์ม แต่เป็นเลขที่อยู่ในเส้นทาง URL; ถ้าไม่ใช่จำนวนเต็มบวก API จะตอบ 400 และไม่แตะ Database

```js
// ตรวจ Body ตอน Member/Admin ส่ง Report
export const createReportSchema = z.object({
  // placeId ต้องเป็น ID ของ Place และ Controller จะตรวจต่อว่า Place มีจริงหรือไม่
  placeId: z.coerce.number().int().positive(),
  // reason จำเป็น ส่วน description ไม่จำเป็น
  reason: z.string().trim().min(1, "กรุณากรอกเหตุผล").max(100),
  // reason จำเป็น ส่วน description ไม่จำเป็น
  description: z.string().trim().max(1000).optional(),
});
```

### 5.10 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/validations/schemas.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/validations/schemas.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 5.10 ทำหน้าที่ `ตรวจ Query สำหรับ Admin กรองรายการ Report ตาม status` โดยสร้างกฎ Zod ให้ Backend ตรวจข้อมูลที่ผู้ใช้หรือ URL ส่งเข้ามาก่อนส่งต่อให้ Controller ช่วยกันข้อมูลผิดรูปแบบไม่ให้ไปถึง Database

**โค้ดก้อนนี้ทำงานอย่างไร:** Validation middleware จะนำ `req.body`, `req.params` หรือ `req.query` มาเทียบกับ Schema นี้ ถ้าถูกต้องจะส่งค่าที่แปลงและตรวจแล้วไปทำงานต่อ; ถ้าไม่ถูกต้อง API จะตอบสถานะ 400 พร้อมรายละเอียด โดย Controller จะยังไม่ทำงาน `z.enum()` อนุญาตเฉพาะค่าที่ระบบรองรับ จึงกันข้อความสถานะหรือประเภทที่สะกดผิด

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Route นำ Schema นี้ไปใช้ ช่องกรอก ฟอร์ม Query หรือค่า `:id` ที่เกี่ยวข้องจะถูกตรวจฝั่ง Backend ก่อนเรียก Controller ข้อมูลผิดจะหยุดที่ Validation และไม่ถูกบันทึกลง Database

```js
// ตรวจ Query สำหรับ Admin กรองรายการ Report ตาม status
export const reportsQuerySchema = z.object({
// ตรวจ Query สำหรับ Admin กรองรายการ Report ตาม status
  status: z.enum(["pending", "resolved", "rejected"]).optional(),
});
```

### 5.11 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/validations/schemas.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/validations/schemas.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 5.11 ทำหน้าที่ `ตรวจ :id ใน URL ของ Report เช่น /api/reports/1` โดยสร้างกฎ Zod ให้ Backend ตรวจข้อมูลที่ผู้ใช้หรือ URL ส่งเข้ามาก่อนส่งต่อให้ Controller ช่วยกันข้อมูลผิดรูปแบบไม่ให้ไปถึง Database

**โค้ดก้อนนี้ทำงานอย่างไร:** Validation middleware จะนำ `req.body`, `req.params` หรือ `req.query` มาเทียบกับ Schema นี้ ถ้าถูกต้องจะส่งค่าที่แปลงและตรวจแล้วไปทำงานต่อ; ถ้าไม่ถูกต้อง API จะตอบสถานะ 400 พร้อมรายละเอียด โดย Controller จะยังไม่ทำงาน ในก้อนนี้ค่าจาก URL เริ่มต้นเป็นข้อความ จึงใช้ `z.coerce.number()` แปลงเป็นตัวเลข แล้ว `.int()` และ `.positive()` บังคับให้เป็นจำนวนเต็มมากกว่า 0

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Route ผูก Schema นี้กับ `params` ค่า `:id` จาก URL เช่น `/api/places/1` หรือ `/api/reports/1` จะถูกตรวจฝั่ง Backend ก่อนเรียก Controller โดยค่านี้ไม่ใช่ช่อง input ในฟอร์ม แต่เป็นเลขที่อยู่ในเส้นทาง URL; ถ้าไม่ใช่จำนวนเต็มบวก API จะตอบ 400 และไม่แตะ Database

```js
// ตรวจ :id ใน URL ของ Report เช่น /api/reports/1
export const reportIdSchema = z.object({
// ตรวจ :id ใน URL ของ Report เช่น /api/reports/1
  id: z.coerce.number().int().positive("Report id ต้องมากกว่า 0"),
});
```

### 5.12 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/validations/schemas.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/validations/schemas.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 5.12 ทำหน้าที่ `ตรวจ Body ตอน Admin จบการจัดการ Report` โดยสร้างกฎ Zod ให้ Backend ตรวจข้อมูลที่ผู้ใช้หรือ URL ส่งเข้ามาก่อนส่งต่อให้ Controller ช่วยกันข้อมูลผิดรูปแบบไม่ให้ไปถึง Database

**โค้ดก้อนนี้ทำงานอย่างไร:** Validation middleware จะนำ `req.body`, `req.params` หรือ `req.query` มาเทียบกับ Schema นี้ ถ้าถูกต้องจะส่งค่าที่แปลงและตรวจแล้วไปทำงานต่อ; ถ้าไม่ถูกต้อง API จะตอบสถานะ 400 พร้อมรายละเอียด โดย Controller จะยังไม่ทำงาน `z.enum()` อนุญาตเฉพาะค่าที่ระบบรองรับ จึงกันข้อความสถานะหรือประเภทที่สะกดผิด

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Route นำ Schema นี้ไปใช้ ช่องกรอก ฟอร์ม Query หรือค่า `:id` ที่เกี่ยวข้องจะถูกตรวจฝั่ง Backend ก่อนเรียก Controller ข้อมูลผิดจะหยุดที่ Validation และไม่ถูกบันทึกลง Database

```js
// ตรวจ Body ตอน Admin จบการจัดการ Report
export const reportStatusSchema = z.object({
  // ไม่รับ pending เพราะ Report ใหม่ถูกตั้ง pending อัตโนมัติอยู่แล้ว
  status: z.enum(["resolved", "rejected"], {
  // ไม่รับ pending เพราะ Report ใหม่ถูกตั้ง pending อัตโนมัติอยู่แล้ว
    message: "status ต้องเป็น resolved หรือ rejected",
  }),
});
```

เมื่อคัดครบไฟล์ ให้รันจาก `BackEnd`:

```powershell
node --check src/validations/schemas.js
```

## 6. `BackEnd/src/middlewares/validateMiddleware.js`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนต่อไปนี้เรียงกัน

### 6.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `BackEnd/src/middlewares/validateMiddleware.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/middlewares/validateMiddleware.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 6.1 ทำหน้าที่ `import fs from "fs";` เพื่อสร้างตัวกลางที่เชื่อมกฎ Zod เข้ากับ Express Route ทำให้ทุก Endpoint ใช้วิธีตรวจข้อมูลและรูปแบบ Error เดียวกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Middleware อ่านส่วนที่ Route ระบุ เช่น `body`, `params` หรือ `query` แล้วเรียก Schema ตรวจ หากผ่านจะเก็บค่าที่ผ่านการแปลงกลับลง Request และเรียก `next()`; หากไม่ผ่านจะตอบ 400 โดยไม่เข้า Controller

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Route ที่ครอบด้วย Middleware นี้จะปฏิเสธข้อมูลไม่ถูกต้องได้อัตโนมัติ ทำให้ Controller รับข้อมูลที่ผ่านกฎแล้วและลดการตรวจซ้ำ

```js
import fs from "fs";
// ก้อนนี้นำ Module ที่จำเป็นเข้ามา เพื่อให้โค้ดส่วนถัดไปเรียกใช้ความสามารถเหล่านั้น
```

### 6.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/middlewares/validateMiddleware.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/middlewares/validateMiddleware.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 6.2 ทำหน้าที่ `ถ้าอัปโหลดรูปมาแล้วแต่ข้อมูลอื่นไม่ผ่าน ให้ลบไฟล์ที่เพิ่งอัปโหลด` เพื่อสร้างตัวกลางที่เชื่อมกฎ Zod เข้ากับ Express Route ทำให้ทุก Endpoint ใช้วิธีตรวจข้อมูลและรูปแบบ Error เดียวกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Middleware อ่านส่วนที่ Route ระบุ เช่น `body`, `params` หรือ `query` แล้วเรียก Schema ตรวจ หากผ่านจะเก็บค่าที่ผ่านการแปลงกลับลง Request และเรียก `next()`; หากไม่ผ่านจะตอบ 400 โดยไม่เข้า Controller

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Route ที่ครอบด้วย Middleware นี้จะปฏิเสธข้อมูลไม่ถูกต้องได้อัตโนมัติ ทำให้ Controller รับข้อมูลที่ผ่านกฎแล้วและลดการตรวจซ้ำ

```js
export function validate(schema, target = "body") {
// ประกาศฟังก์ชันนี้และกำหนดขอบเขตงานตามชื่อของฟังก์ชัน
  return (req, res, next) => {
  // ส่งผลลัพธ์ออกจากฟังก์ชันและหยุดทำบรรทัดถัดไปในฟังก์ชันนี้
    const result = schema.safeParse(req[target]);
    // ประกาศค่าที่ใช้ภายในขอบเขตนี้และไม่อนุญาตให้เปลี่ยนตัวแปรไปอ้างค่าใหม่

    if (!result.success) {
      // ถ้าอัปโหลดรูปมาแล้วแต่ข้อมูลอื่นไม่ผ่าน ให้ลบไฟล์ที่เพิ่งอัปโหลด
      if (req.file?.path && fs.existsSync(req.file.path)) {
      // ถ้าอัปโหลดรูปมาแล้วแต่ข้อมูลอื่นไม่ผ่าน ให้ลบไฟล์ที่เพิ่งอัปโหลด
        fs.unlinkSync(req.file.path);
      }

      // แปลง Zod issues ให้เป็นข้อความสั้น ๆ ที่ Frontend นำไปแสดงได้
      const errors = result.error.issues.map((issue) => ({
      // แปลง Zod issues ให้เป็นข้อความสั้น ๆ ที่ Frontend นำไปแสดงได้
        field: issue.path.join("."),
        message: issue.message,
      }));

      return res.status(400).json({
        message: "ข้อมูลไม่ถูกต้อง",
        errors,
      });
    }

    // body แก้ค่าได้ จึงใช้ค่าที่ Zod trim/coerce เรียบร้อยแล้วต่อใน Controller
    if (target === "body") {
    // body แก้ค่าได้ จึงใช้ค่าที่ Zod trim/coerce เรียบร้อยแล้วต่อใน Controller
      req.body = result.data;
    }

    // เก็บค่าที่ผ่าน Validation ไว้ด้วย เผื่อ Controller ต้องการใช้ภายหลัง
    req.validated = req.validated || {};
    // เก็บค่าที่ผ่าน Validation ไว้ด้วย เผื่อ Controller ต้องการใช้ภายหลัง
    req.validated[target] = result.data;
    next();
    // ถ้าเป็น Admin ให้ไปทำ Controller ตัวถัดไป
  };
}
```

เมื่อคัดครบไฟล์ ให้รันจาก `BackEnd`:

```powershell
node --check src/middlewares/validateMiddleware.js
```

## 7. `BackEnd/src/controllers/authController.js`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนต่อไปนี้เรียงกัน

### 7.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `BackEnd/src/controllers/authController.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/controllers/authController.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 7.1 ทำหน้าที่ `import bcrypt from "bcrypt";` ซึ่งเป็นส่วน Business Logic ที่รับข้อมูลจาก Route/Middleware แล้วอ่านหรือแก้ไขข้อมูลผ่าน Prisma

**โค้ดก้อนนี้ทำงานอย่างไร:** Controller จะอ่านข้อมูลที่ผ่าน Validation และตัวตนจาก Request เรียก `prisma` เพื่อ query/create/update/delete ตามเงื่อนไข แล้วส่ง JSON และ HTTP status กลับ Frontend; กรณีผิดพลาดจะส่งต่อให้ Error handler

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อประกอบ Controller ครบ Endpoint ที่ Route เชื่อมไว้จะทำงานกับ Database จริงตามสิทธิ์และเงื่อนไขของระบบ

```js
import bcrypt from "bcrypt";
// ก้อนนี้นำ Module ที่จำเป็นเข้ามา เพื่อให้โค้ดส่วนถัดไปเรียกใช้ความสามารถเหล่านั้น
```

### 7.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/controllers/authController.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/controllers/authController.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 7.2 ทำหน้าที่ `import { prisma } from "../lib/prisma.js";` ซึ่งเป็นส่วน Business Logic ที่รับข้อมูลจาก Route/Middleware แล้วอ่านหรือแก้ไขข้อมูลผ่าน Prisma

**โค้ดก้อนนี้ทำงานอย่างไร:** Controller จะอ่านข้อมูลที่ผ่าน Validation และตัวตนจาก Request เรียก `prisma` เพื่อ query/create/update/delete ตามเงื่อนไข แล้วส่ง JSON และ HTTP status กลับ Frontend; กรณีผิดพลาดจะส่งต่อให้ Error handler

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อประกอบ Controller ครบ Endpoint ที่ Route เชื่อมไว้จะทำงานกับ Database จริงตามสิทธิ์และเงื่อนไขของระบบ

```js
import { prisma } from "../lib/prisma.js";
// ก้อนนี้ติดต่อ Database ผ่าน Prisma เพื่ออ่านหรือเปลี่ยนแปลงข้อมูลตามงานของระบบ
```

### 7.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/controllers/authController.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/controllers/authController.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 7.3 ทำหน้าที่ `import jwt from "jsonwebtoken";` ซึ่งเป็นส่วน Business Logic ที่รับข้อมูลจาก Route/Middleware แล้วอ่านหรือแก้ไขข้อมูลผ่าน Prisma

**โค้ดก้อนนี้ทำงานอย่างไร:** Controller จะอ่านข้อมูลที่ผ่าน Validation และตัวตนจาก Request เรียก `prisma` เพื่อ query/create/update/delete ตามเงื่อนไข แล้วส่ง JSON และ HTTP status กลับ Frontend; กรณีผิดพลาดจะส่งต่อให้ Error handler

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อประกอบ Controller ครบ Endpoint ที่ Route เชื่อมไว้จะทำงานกับ Database จริงตามสิทธิ์และเงื่อนไขของระบบ

```js
import jwt from "jsonwebtoken";
// ก้อนนี้นำ Module ที่จำเป็นเข้ามา เพื่อให้โค้ดส่วนถัดไปเรียกใช้ความสามารถเหล่านั้น
```

### 7.4 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/controllers/authController.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/controllers/authController.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 7.4 ทำหน้าที่ `function createToken(user) {` ซึ่งเป็นส่วน Business Logic ที่รับข้อมูลจาก Route/Middleware แล้วอ่านหรือแก้ไขข้อมูลผ่าน Prisma

**โค้ดก้อนนี้ทำงานอย่างไร:** Controller จะอ่านข้อมูลที่ผ่าน Validation และตัวตนจาก Request เรียก `prisma` เพื่อ query/create/update/delete ตามเงื่อนไข แล้วส่ง JSON และ HTTP status กลับ Frontend; กรณีผิดพลาดจะส่งต่อให้ Error handler

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อประกอบ Controller ครบ Endpoint ที่ Route เชื่อมไว้จะทำงานกับ Database จริงตามสิทธิ์และเงื่อนไขของระบบ

```js
function createToken(user) {
// ประกาศฟังก์ชันนี้และกำหนดขอบเขตงานตามชื่อของฟังก์ชัน
  return jwt.sign(
  // jwt.sign() สร้าง Token ที่ Backend สามารถตรวจสอบภายหลังได้
    { userId: user.id, role: user.role },
    // Payload: เก็บ id สำหรับระบุผู้ใช้ และ role สำหรับตรวจสิทธิ์ Member/Admin | ไม่ใส่ password หรือข้อมูลลับลงใน Token
    process.env.JWT_SECRET,
    // Secret ใช้เซ็นและตรวจลายเซ็น Token ต้องตรงกับค่า JWT_SECRET ใน .env
    { expiresIn: "7d" }
    // Token หมดอายุใน 7 วัน หลังจากนั้นผู้ใช้ต้อง Login ใหม่
  );
}
```

### 7.5 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/controllers/authController.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/controllers/authController.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 7.5 ทำหน้าที่ `POST /api/auth/register เริ่มทำงานหลัง authRoutes รับ request` ซึ่งเป็นส่วน Business Logic ที่รับข้อมูลจาก Route/Middleware แล้วอ่านหรือแก้ไขข้อมูลผ่าน Prisma

**โค้ดก้อนนี้ทำงานอย่างไร:** Controller จะอ่านข้อมูลที่ผ่าน Validation และตัวตนจาก Request เรียก `prisma` เพื่อ query/create/update/delete ตามเงื่อนไข แล้วส่ง JSON และ HTTP status กลับ Frontend; กรณีผิดพลาดจะส่งต่อให้ Error handler

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อประกอบ Controller ครบ Endpoint ที่ Route เชื่อมไว้จะทำงานกับ Database จริงตามสิทธิ์และเงื่อนไขของระบบ

```js
// POST /api/auth/register เริ่มทำงานหลัง authRoutes รับ request
export async function register(req, res) {
// POST /api/auth/register เริ่มทำงานหลัง authRoutes รับ request
  try {
    // รับข้อมูล JSON ที่ Express แปลงไว้ใน req.body
    const { name, email, password } = req.body;
    // รับข้อมูล JSON ที่ Express แปลงไว้ใน req.body

    // หยุดทันทีถ้าข้อมูลสมัครสมาชิกไม่ครบ
    if (!name || !email || !password) {
    // หยุดทันทีถ้าข้อมูลสมัครสมาชิกไม่ครบ
      return res.status(400).json({ message: "กรุณากรอกข้อมูลให้ครบ" });
      // ส่ง HTTP status และ JSON กลับ Client พร้อมหยุด Controller
    }

    // ค้นหา email เพราะ schema กำหนดให้ email ห้ามซ้ำ
    const existingUser = await prisma.user.findUnique({
    // ค้นหา email เพราะ schema กำหนดให้ email ห้ามซ้ำ
      where: { email },
    });

    if (existingUser) {
    // ตรวจเงื่อนไขก่อนอนุญาตให้โค้ดภายในทำงาน
      return res.status(409).json({ message: "อีเมลนี้ถูกใช้งานแล้ว" });
      // ส่ง HTTP status และ JSON กลับ Client พร้อมหยุด Controller
    }

    // เปลี่ยนรหัสผ่านเป็น hash ก่อนบันทึก ห้ามเก็บรหัสผ่านจริง
    const hashedPassword = await bcrypt.hash(password, 10);
    // เปลี่ยนรหัสผ่านเป็น hash ก่อนบันทึก ห้ามเก็บรหัสผ่านจริง

    // Prisma สร้างแถวใหม่ในตาราง User และ role เริ่มต้นเป็น member
    const user = await prisma.user.create({
    // Prisma สร้างแถวใหม่ในตาราง User และ role เริ่มต้นเป็น member
      data: {
      // กำหนดข้อมูลที่ Prisma จะสร้างหรืออัปเดตใน Database
        name,
        // ชื่อสถานที่จาก Body
        email,
        password: hashedPassword,
      },
    });

    // ส่งข้อมูลที่ปลอดภัยกลับ จึงไม่ส่ง password กลับไป
    return res.status(201).json({
    // ส่งข้อมูลที่ปลอดภัยกลับ จึงไม่ส่ง password กลับไป
      message: "สมัครสมาชิกสำเร็จ",
      user: {
        id: user.id,
        name: user.name,
        email: user.email,
        role: user.role,
      },
    });
  } catch (error) {
  // รับ Error ที่เกิดจาก Prisma หรือ Logic ภายใน try
    console.error(error);
    // พิมพ์ Error จริงใน Terminal เพื่อช่วยวิเคราะห์โดยไม่ส่งรายละเอียดลับให้ Client
    return res.status(500).json({ message: "เกิดข้อผิดพลาดจาก Server" });
  }
}
```

### 7.6 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/controllers/authController.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/controllers/authController.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 7.6 ทำหน้าที่ `POST /api/auth/login ตรวจ email/password แล้วสร้าง JWT` ซึ่งเป็นส่วน Business Logic ที่รับข้อมูลจาก Route/Middleware แล้วอ่านหรือแก้ไขข้อมูลผ่าน Prisma

**โค้ดก้อนนี้ทำงานอย่างไร:** Controller จะอ่านข้อมูลที่ผ่าน Validation และตัวตนจาก Request เรียก `prisma` เพื่อ query/create/update/delete ตามเงื่อนไข แล้วส่ง JSON และ HTTP status กลับ Frontend; กรณีผิดพลาดจะส่งต่อให้ Error handler

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อประกอบ Controller ครบ Endpoint ที่ Route เชื่อมไว้จะทำงานกับ Database จริงตามสิทธิ์และเงื่อนไขของระบบ

```js
// POST /api/auth/login ตรวจ email/password แล้วสร้าง JWT
export async function login(req, res) {
// POST /api/auth/login ตรวจ email/password แล้วสร้าง JWT
  try {
    const { email, password } = req.body;
    // ประกาศค่าที่ใช้ภายในขอบเขตนี้และไม่อนุญาตให้เปลี่ยนตัวแปรไปอ้างค่าใหม่

    if (!email || !password) {
    // ตรวจเงื่อนไขก่อนอนุญาตให้โค้ดภายในทำงาน
      return res.status(400).json({ message: "กรุณากรอกอีเมลและรหัสผ่าน" });
      // ส่ง HTTP status และ JSON กลับ Client พร้อมหยุด Controller
    }

    // ค้นหาผู้ใช้จาก email ที่เป็น unique
    const user = await prisma.user.findUnique({
      where: { email },
    });

    if (!user) {
    // ตรวจเงื่อนไขก่อนอนุญาตให้โค้ดภายในทำงาน
      return res.status(401).json({ message: "อีเมลหรือรหัสผ่านไม่ถูกต้อง" });
      // ส่ง HTTP status และ JSON กลับ Client พร้อมหยุด Controller
    }

    // เปรียบเทียบรหัสที่ส่งมากับ hash ใน Database
    const passwordIsCorrect = await bcrypt.compare(password, user.password);
    // เปรียบเทียบรหัสที่ส่งมากับ hash ใน Database

    if (!passwordIsCorrect) {
    // ตรวจเงื่อนไขก่อนอนุญาตให้โค้ดภายในทำงาน
      return res.status(401).json({ message: "อีเมลหรือรหัสผ่านไม่ถูกต้อง" });
      // ส่ง HTTP status และ JSON กลับ Client พร้อมหยุด Controller
    }

    // Token เก็บเฉพาะข้อมูลที่ Middleware ต้องใช้ ไม่เก็บ password
    const token = createToken(user);
    // Token เก็บเฉพาะข้อมูลที่ Middleware ต้องใช้ ไม่เก็บ password

    // Frontend จะเก็บ Token นี้ไว้ส่งกับ API ที่ต้อง Login
    return res.json({
    // Frontend จะเก็บ Token นี้ไว้ส่งกับ API ที่ต้อง Login
      message: "เข้าสู่ระบบสำเร็จ",
      token,
    });
  } catch (error) {
  // รับ Error ที่เกิดจาก Prisma หรือ Logic ภายใน try
    console.error(error);
    // พิมพ์ Error จริงใน Terminal เพื่อช่วยวิเคราะห์โดยไม่ส่งรายละเอียดลับให้ Client
    return res.status(500).json({ message: "เกิดข้อผิดพลาดจาก Server" });
  }
}
```

เมื่อคัดครบไฟล์ ให้รันจาก `BackEnd`:

```powershell
node --check src/controllers/authController.js
```

## 8. `BackEnd/src/middlewares/authMiddleware.js`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนต่อไปนี้เรียงกัน

### 8.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `BackEnd/src/middlewares/authMiddleware.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/middlewares/authMiddleware.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 8.1 ทำหน้าที่ `import jwt from "jsonwebtoken";` เพื่อยืนยันตัวตนจาก JWT ก่อนอนุญาตให้ผู้ใช้เข้าถึง Endpoint ที่ต้อง Login

**โค้ดก้อนนี้ทำงานอย่างไร:** Middleware อ่าน Bearer token จาก Header, ตรวจลายเซ็นด้วย `JWT_SECRET`, ค้นหาผู้ใช้ที่เกี่ยวข้อง และแนบข้อมูลผู้ใช้ไว้ใน Request; token หาย หมดอายุ หรือปลอมจะถูกปฏิเสธก่อน Controller

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Route ที่ใช้ Middleware นี้จะแยกผู้ใช้ที่ Login แล้วออกจากผู้เยี่ยมชม และ Controller สามารถใช้ข้อมูลผู้ใช้จาก Request ได้อย่างปลอดภัยขึ้น

```js
import jwt from "jsonwebtoken";
// ก้อนนี้นำ Module ที่จำเป็นเข้ามา เพื่อให้โค้ดส่วนถัดไปเรียกใช้ความสามารถเหล่านั้น
```

### 8.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/middlewares/authMiddleware.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/middlewares/authMiddleware.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 8.2 ทำหน้าที่ `Middleware นี้ทำงานหลัง Route ได้รับ request และก่อนเข้า Controller` เพื่อยืนยันตัวตนจาก JWT ก่อนอนุญาตให้ผู้ใช้เข้าถึง Endpoint ที่ต้อง Login

**โค้ดก้อนนี้ทำงานอย่างไร:** Middleware อ่าน Bearer token จาก Header, ตรวจลายเซ็นด้วย `JWT_SECRET`, ค้นหาผู้ใช้ที่เกี่ยวข้อง และแนบข้อมูลผู้ใช้ไว้ใน Request; token หาย หมดอายุ หรือปลอมจะถูกปฏิเสธก่อน Controller

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Route ที่ใช้ Middleware นี้จะแยกผู้ใช้ที่ Login แล้วออกจากผู้เยี่ยมชม และ Controller สามารถใช้ข้อมูลผู้ใช้จาก Request ได้อย่างปลอดภัยขึ้น

```js
// Middleware นี้ทำงานหลัง Route ได้รับ request และก่อนเข้า Controller
export function authMiddleware(req, res, next) {
  // Frontend/Postman ต้องส่ง Header รูปแบบ: Authorization: Bearer TOKEN
  const authorization = req.headers.authorization;

  if (!authorization || !authorization.startsWith("Bearer ")) {
  // ตรวจเงื่อนไขก่อนอนุญาตให้โค้ดภายในทำงาน
    return res.status(401).json({ message: "กรุณาเข้าสู่ระบบ" });
    // ส่ง HTTP status และ JSON กลับ Client พร้อมหยุด Controller
  }

  // แยกเอาเฉพาะ Token ที่อยู่หลังคำว่า Bearer
  const token = authorization.split(" ")[1];

  try {
    // ตรวจ Token แล้วเก็บ userId และ role ไว้ให้ Controller ใช้ต่อ
    req.user = jwt.verify(token, process.env.JWT_SECRET);
    // ตรวจ Token แล้วเก็บ userId และ role ไว้ให้ Controller ใช้ต่อ
    next();
    // ถ้าเป็น Admin ให้ไปทำ Controller ตัวถัดไป
  } catch {
    return res.status(401).json({ message: "Token ไม่ถูกต้องหรือหมดอายุ" });
    // ส่ง HTTP status และ JSON กลับ Client พร้อมหยุด Controller
  }
}
```

### 8.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/middlewares/authMiddleware.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/middlewares/authMiddleware.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 8.3 ทำหน้าที่ `Middleware แบบ optional ใช้กับ Route ที่เปิดให้คนทั่วไป แต่ Admin มีสิทธิ์เพิ่ม` เพื่อยืนยันตัวตนจาก JWT ก่อนอนุญาตให้ผู้ใช้เข้าถึง Endpoint ที่ต้อง Login

**โค้ดก้อนนี้ทำงานอย่างไร:** Middleware อ่าน Bearer token จาก Header, ตรวจลายเซ็นด้วย `JWT_SECRET`, ค้นหาผู้ใช้ที่เกี่ยวข้อง และแนบข้อมูลผู้ใช้ไว้ใน Request; token หาย หมดอายุ หรือปลอมจะถูกปฏิเสธก่อน Controller

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Route ที่ใช้ Middleware นี้จะแยกผู้ใช้ที่ Login แล้วออกจากผู้เยี่ยมชม และ Controller สามารถใช้ข้อมูลผู้ใช้จาก Request ได้อย่างปลอดภัยขึ้น

```js
// Middleware แบบ optional ใช้กับ Route ที่เปิดให้คนทั่วไป แต่ Admin มีสิทธิ์เพิ่ม
export function optionalAuthMiddleware(req, res, next) {
// Middleware แบบ optional ใช้กับ Route ที่เปิดให้คนทั่วไป แต่ Admin มีสิทธิ์เพิ่ม
  const authorization = req.headers.authorization;

  // ถ้าไม่ได้ส่ง Token มาก็ให้คนทั่วไปใช้งาน Route ต่อได้
  if (!authorization) {
  // ถ้าไม่ได้ส่ง Token มาก็ให้คนทั่วไปใช้งาน Route ต่อได้
    return next();
    // ส่งผลลัพธ์ออกจากฟังก์ชันและหยุดทำบรรทัดถัดไปในฟังก์ชันนี้
  }

  if (!authorization.startsWith("Bearer ")) {
  // ตรวจเงื่อนไขก่อนอนุญาตให้โค้ดภายในทำงาน
    return res.status(401).json({ message: "รูปแบบ Token ไม่ถูกต้อง" });
    // ส่ง HTTP status และ JSON กลับ Client พร้อมหยุด Controller
  }

  try {
    const token = authorization.split(" ")[1];
    req.user = jwt.verify(token, process.env.JWT_SECRET);
    // ตรวจ Token แล้วเก็บ userId และ role ไว้ให้ Controller ใช้ต่อ
    return next();
    // ส่งผลลัพธ์ออกจากฟังก์ชันและหยุดทำบรรทัดถัดไปในฟังก์ชันนี้
  } catch {
    return res.status(401).json({ message: "Token ไม่ถูกต้องหรือหมดอายุ" });
    // ส่ง HTTP status และ JSON กลับ Client พร้อมหยุด Controller
  }
}
```

เมื่อคัดครบไฟล์ ให้รันจาก `BackEnd`:

```powershell
node --check src/middlewares/authMiddleware.js
```

## 9. `BackEnd/src/middlewares/adminMiddleware.js`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนต่อไปนี้เรียงกัน

### 9.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `BackEnd/src/middlewares/adminMiddleware.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/middlewares/adminMiddleware.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 9.1 ทำหน้าที่ `ถ้าเป็น Admin ให้ไปทำ Controller ตัวถัดไป` เพื่อเพิ่มการตรวจสิทธิ์ Admin ต่อจากการตรวจ Login

**โค้ดก้อนนี้ทำงานอย่างไร:** Middleware ตรวจ role ของผู้ใช้ที่ auth middleware แนบไว้ หากไม่ใช่ Admin จะตอบ 403 และไม่เรียก Controller

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Endpoint จัดการข้อมูลส่วนกลางจะเปิดให้เฉพาะ Admin ขณะที่ Member ที่ Login แล้วแต่ไม่มีสิทธิ์จะยังเข้าไม่ได้

```js
export function adminMiddleware(req, res, next) {
// ลำดับอ่าน 3B: ทำงานต่อจาก authMiddleware เฉพาะ Route ของ Admin | ถ้า role ผ่าน next() จะพา request ไปยัง Controller ที่เขียนต่อท้ายใน Route | ต้องเรียก authMiddleware ก่อน เพื่อให้ req.user มีข้อมูลผู้ใช้
  if (req.user.role !== "admin") {
  // ตรวจเงื่อนไขก่อนอนุญาตให้โค้ดภายในทำงาน
    return res.status(403).json({ message: "เฉพาะ Admin เท่านั้น" });
    // ส่ง HTTP status และ JSON กลับ Client พร้อมหยุด Controller
  }

  // ถ้าเป็น Admin ให้ไปทำ Controller ตัวถัดไป
  next();
  // ถ้าเป็น Admin ให้ไปทำ Controller ตัวถัดไป
}
```

เมื่อคัดครบไฟล์ ให้รันจาก `BackEnd`:

```powershell
node --check src/middlewares/adminMiddleware.js
```

## 10. `BackEnd/src/middlewares/uploadMiddleware.js`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนต่อไปนี้เรียงกัน

### 10.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `BackEnd/src/middlewares/uploadMiddleware.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/middlewares/uploadMiddleware.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 10.1 ทำหน้าที่ `import fs from "fs";` เพื่อกำหนดวิธีรับไฟล์จากฟอร์ม multipart ก่อนส่งข้อมูลไฟล์ให้ Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Multer ใช้กฎในก้อนนี้เลือกตำแหน่งและชื่อไฟล์ ตรวจชนิดไฟล์ และจำกัดขนาด หากไฟล์ไม่ผ่านจะหยุด Request ก่อนการบันทึกข้อมูลหลัก

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Route อัปโหลดจะได้รับข้อมูลไฟล์ที่ผ่านข้อกำหนดใน `req.file` หรือ `req.files` และ Controller จึงนำ path ไปเก็บใน Database ได้

```js
import fs from "fs";
// ก้อนนี้นำ Module ที่จำเป็นเข้ามา เพื่อให้โค้ดส่วนถัดไปเรียกใช้ความสามารถเหล่านั้น
```

### 10.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/middlewares/uploadMiddleware.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/middlewares/uploadMiddleware.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 10.2 ทำหน้าที่ `import path from "path";` เพื่อกำหนดวิธีรับไฟล์จากฟอร์ม multipart ก่อนส่งข้อมูลไฟล์ให้ Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Multer ใช้กฎในก้อนนี้เลือกตำแหน่งและชื่อไฟล์ ตรวจชนิดไฟล์ และจำกัดขนาด หากไฟล์ไม่ผ่านจะหยุด Request ก่อนการบันทึกข้อมูลหลัก

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Route อัปโหลดจะได้รับข้อมูลไฟล์ที่ผ่านข้อกำหนดใน `req.file` หรือ `req.files` และ Controller จึงนำ path ไปเก็บใน Database ได้

```js
import path from "path";
// ก้อนนี้นำ Module ที่จำเป็นเข้ามา เพื่อให้โค้ดส่วนถัดไปเรียกใช้ความสามารถเหล่านั้น
```

### 10.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/middlewares/uploadMiddleware.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/middlewares/uploadMiddleware.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 10.3 ทำหน้าที่ `import multer from "multer";` เพื่อกำหนดวิธีรับไฟล์จากฟอร์ม multipart ก่อนส่งข้อมูลไฟล์ให้ Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Multer ใช้กฎในก้อนนี้เลือกตำแหน่งและชื่อไฟล์ ตรวจชนิดไฟล์ และจำกัดขนาด หากไฟล์ไม่ผ่านจะหยุด Request ก่อนการบันทึกข้อมูลหลัก

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Route อัปโหลดจะได้รับข้อมูลไฟล์ที่ผ่านข้อกำหนดใน `req.file` หรือ `req.files` และ Controller จึงนำ path ไปเก็บใน Database ได้

```js
import multer from "multer";
// multer รับไฟล์ multipart/form-data จาก Frontend
```

### 10.4 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/middlewares/uploadMiddleware.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/middlewares/uploadMiddleware.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 10.4 ทำหน้าที่ `const uploadDirectory = path.resolve("uploads");` เพื่อกำหนดวิธีรับไฟล์จากฟอร์ม multipart ก่อนส่งข้อมูลไฟล์ให้ Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Multer ใช้กฎในก้อนนี้เลือกตำแหน่งและชื่อไฟล์ ตรวจชนิดไฟล์ และจำกัดขนาด หากไฟล์ไม่ผ่านจะหยุด Request ก่อนการบันทึกข้อมูลหลัก

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Route อัปโหลดจะได้รับข้อมูลไฟล์ที่ผ่านข้อกำหนดใน `req.file` หรือ `req.files` และ Controller จึงนำ path ไปเก็บใน Database ได้

```js
const uploadDirectory = path.resolve("uploads");
// เปลี่ยน "uploads" เป็น Absolute path | เมื่อรัน npm run dev จาก BackEnd จะชี้ไปที่ BackEnd/uploads
```

### 10.5 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/middlewares/uploadMiddleware.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/middlewares/uploadMiddleware.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 10.5 ทำหน้าที่ `สร้างโฟลเดอร์ uploads อัตโนมัติถ้ายังไม่มี` เพื่อกำหนดวิธีรับไฟล์จากฟอร์ม multipart ก่อนส่งข้อมูลไฟล์ให้ Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Multer ใช้กฎในก้อนนี้เลือกตำแหน่งและชื่อไฟล์ ตรวจชนิดไฟล์ และจำกัดขนาด หากไฟล์ไม่ผ่านจะหยุด Request ก่อนการบันทึกข้อมูลหลัก

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Route อัปโหลดจะได้รับข้อมูลไฟล์ที่ผ่านข้อกำหนดใน `req.file` หรือ `req.files` และ Controller จึงนำ path ไปเก็บใน Database ได้

```js
// สร้างโฟลเดอร์ uploads อัตโนมัติถ้ายังไม่มี
fs.mkdirSync(uploadDirectory, { recursive: true });
// สร้างโฟลเดอร์ uploads ก่อน Multer บันทึกไฟล์ | recursive: true สร้างโฟลเดอร์แม่ที่ขาดและไม่ Error ถ้ามีอยู่แล้ว
```

### 10.6 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/middlewares/uploadMiddleware.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/middlewares/uploadMiddleware.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 10.6 ทำหน้าที่ `ใช้เวลา + เลขสุ่ม ป้องกันชื่อไฟล์ชนกัน` เพื่อกำหนดวิธีรับไฟล์จากฟอร์ม multipart ก่อนส่งข้อมูลไฟล์ให้ Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Multer ใช้กฎในก้อนนี้เลือกตำแหน่งและชื่อไฟล์ ตรวจชนิดไฟล์ และจำกัดขนาด หากไฟล์ไม่ผ่านจะหยุด Request ก่อนการบันทึกข้อมูลหลัก

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Route อัปโหลดจะได้รับข้อมูลไฟล์ที่ผ่านข้อกำหนดใน `req.file` หรือ `req.files` และ Controller จึงนำ path ไปเก็บใน Database ได้

```js
function createStorage(prefix) {
// รับ prefix เพื่อแยกประเภทชื่อไฟล์ เช่น place, avatar หรือ report
  return multer.diskStorage({
  // diskStorage กำหนดให้ Multer บันทึกไฟล์จริงลง Disk
    destination: (req, file, callback) => callback(null, uploadDirectory),
    filename: (req, file, callback) => {
      // ใช้เวลา + เลขสุ่ม ป้องกันชื่อไฟล์ชนกัน
      const extension =
      // อ่านนามสกุลจากชื่อเดิมและเปลี่ยนเป็นตัวเล็ก; ถ้าไม่มีให้ใช้ .jpg
        path.extname(file.originalname).toLowerCase() || ".jpg";
      callback(
      // รูปแบบชื่อ: prefix-เวลา-เลขสุ่ม.นามสกุล ช่วยลดโอกาสชื่อชนกัน
        null,
        `${prefix}-${Date.now()}-${Math.round(Math.random() * 1e9)}${extension}`
      );
    },
  });
}
```

### 10.7 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/middlewares/uploadMiddleware.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/middlewares/uploadMiddleware.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 10.7 ทำหน้าที่ `function createUpload(prefix) {` เพื่อกำหนดวิธีรับไฟล์จากฟอร์ม multipart ก่อนส่งข้อมูลไฟล์ให้ Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Multer ใช้กฎในก้อนนี้เลือกตำแหน่งและชื่อไฟล์ ตรวจชนิดไฟล์ และจำกัดขนาด หากไฟล์ไม่ผ่านจะหยุด Request ก่อนการบันทึกข้อมูลหลัก

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Route อัปโหลดจะได้รับข้อมูลไฟล์ที่ผ่านข้อกำหนดใน `req.file` หรือ `req.files` และ Controller จึงนำ path ไปเก็บใน Database ได้

```js
function createUpload(prefix) {
// สร้าง Multer middleware โดยรับ prefix สำหรับตั้งชื่อไฟล์แต่ละประเภท
  return multer({
  // ส่งผลลัพธ์ออกจากฟังก์ชันและหยุดทำบรรทัดถัดไปในฟังก์ชันนี้
    storage: createStorage(prefix),
    // ใช้กฎโฟลเดอร์และชื่อไฟล์จาก createStorage()
    limits: { fileSize: 5 * 1024 * 1024 },
    // จำกัดขนาดไฟล์ไม่เกิน 5 MB
    fileFilter: (req, file, callback) => {
    // ตรวจ MIME type ก่อนอนุญาตให้บันทึกไฟล์
      if (!file.mimetype.startsWith("image/")) {
      // ตรวจเงื่อนไขก่อนอนุญาตให้โค้ดภายในทำงาน
        return callback(new Error("อัปโหลดได้เฉพาะไฟล์รูปภาพ"));
        // ส่ง Error และหยุดอัปโหลดเมื่อไม่ใช่รูปภาพ
      }

      callback(null, true);
      // null = ไม่มี Error, true = ยอมรับไฟล์นี้
    },
  });
}
```

### 10.8 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/middlewares/uploadMiddleware.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/middlewares/uploadMiddleware.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 10.8 ทำหน้าที่ `image คือชื่อ field ที่ Frontend ต้องใช้ใน FormData` เพื่อกำหนดวิธีรับไฟล์จากฟอร์ม multipart ก่อนส่งข้อมูลไฟล์ให้ Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Multer ใช้กฎในก้อนนี้เลือกตำแหน่งและชื่อไฟล์ ตรวจชนิดไฟล์ และจำกัดขนาด หากไฟล์ไม่ผ่านจะหยุด Request ก่อนการบันทึกข้อมูลหลัก

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Route อัปโหลดจะได้รับข้อมูลไฟล์ที่ผ่านข้อกำหนดใน `req.file` หรือ `req.files` และ Controller จึงนำ path ไปเก็บใน Database ได้

```js
// image คือชื่อ field ที่ Frontend ต้องใช้ใน FormData
export const uploadPlaceImage = createUpload("place").single("image");
// image คือชื่อ field ที่ Frontend ต้องใช้ใน FormData
```

### 10.9 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/middlewares/uploadMiddleware.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/middlewares/uploadMiddleware.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 10.9 ทำหน้าที่ `avatar คือชื่อ field สำหรับรูปโปรไฟล์` เพื่อกำหนดวิธีรับไฟล์จากฟอร์ม multipart ก่อนส่งข้อมูลไฟล์ให้ Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Multer ใช้กฎในก้อนนี้เลือกตำแหน่งและชื่อไฟล์ ตรวจชนิดไฟล์ และจำกัดขนาด หากไฟล์ไม่ผ่านจะหยุด Request ก่อนการบันทึกข้อมูลหลัก

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Route อัปโหลดจะได้รับข้อมูลไฟล์ที่ผ่านข้อกำหนดใน `req.file` หรือ `req.files` และ Controller จึงนำ path ไปเก็บใน Database ได้

```js
// avatar คือชื่อ field สำหรับรูปโปรไฟล์
export const uploadAvatar = createUpload("avatar").single("avatar");
// avatar คือชื่อ field สำหรับรูปโปรไฟล์
```

### 10.10 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/middlewares/uploadMiddleware.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/middlewares/uploadMiddleware.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 10.10 ทำหน้าที่ `evidence คือรูปหลักฐานที่แนบมากับ Report และจะถูกลบหลัง Admin จัดการ` เพื่อกำหนดวิธีรับไฟล์จากฟอร์ม multipart ก่อนส่งข้อมูลไฟล์ให้ Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Multer ใช้กฎในก้อนนี้เลือกตำแหน่งและชื่อไฟล์ ตรวจชนิดไฟล์ และจำกัดขนาด หากไฟล์ไม่ผ่านจะหยุด Request ก่อนการบันทึกข้อมูลหลัก

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Route อัปโหลดจะได้รับข้อมูลไฟล์ที่ผ่านข้อกำหนดใน `req.file` หรือ `req.files` และ Controller จึงนำ path ไปเก็บใน Database ได้

```js
// evidence คือรูปหลักฐานที่แนบมากับ Report และจะถูกลบหลัง Admin จัดการ
export const uploadReportEvidence = createUpload("report").single("evidence");
// evidence คือรูปหลักฐานที่แนบมากับ Report และจะถูกลบหลัง Admin จัดการ
```

เมื่อคัดครบไฟล์ ให้รันจาก `BackEnd`:

```powershell
node --check src/middlewares/uploadMiddleware.js
```

## 11. `BackEnd/src/controllers/userController.js`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนต่อไปนี้เรียงกัน

### 11.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `BackEnd/src/controllers/userController.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/controllers/userController.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 11.1 ทำหน้าที่ `import { prisma } from "../lib/prisma.js";` ซึ่งเป็นส่วน Business Logic ที่รับข้อมูลจาก Route/Middleware แล้วอ่านหรือแก้ไขข้อมูลผ่าน Prisma

**โค้ดก้อนนี้ทำงานอย่างไร:** Controller จะอ่านข้อมูลที่ผ่าน Validation และตัวตนจาก Request เรียก `prisma` เพื่อ query/create/update/delete ตามเงื่อนไข แล้วส่ง JSON และ HTTP status กลับ Frontend; กรณีผิดพลาดจะส่งต่อให้ Error handler

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อประกอบ Controller ครบ Endpoint ที่ Route เชื่อมไว้จะทำงานกับ Database จริงตามสิทธิ์และเงื่อนไขของระบบ

```js
import { prisma } from "../lib/prisma.js";
// ก้อนนี้ติดต่อ Database ผ่าน Prisma เพื่ออ่านหรือเปลี่ยนแปลงข้อมูลตามงานของระบบ
```

### 11.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/controllers/userController.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/controllers/userController.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 11.2 ทำหน้าที่ `import fs from "fs";` ซึ่งเป็นส่วน Business Logic ที่รับข้อมูลจาก Route/Middleware แล้วอ่านหรือแก้ไขข้อมูลผ่าน Prisma

**โค้ดก้อนนี้ทำงานอย่างไร:** Controller จะอ่านข้อมูลที่ผ่าน Validation และตัวตนจาก Request เรียก `prisma` เพื่อ query/create/update/delete ตามเงื่อนไข แล้วส่ง JSON และ HTTP status กลับ Frontend; กรณีผิดพลาดจะส่งต่อให้ Error handler

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อประกอบ Controller ครบ Endpoint ที่ Route เชื่อมไว้จะทำงานกับ Database จริงตามสิทธิ์และเงื่อนไขของระบบ

```js
import fs from "fs";
// ก้อนนี้นำ Module ที่จำเป็นเข้ามา เพื่อให้โค้ดส่วนถัดไปเรียกใช้ความสามารถเหล่านั้น
```

### 11.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/controllers/userController.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/controllers/userController.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 11.3 ทำหน้าที่ `import path from "path";` ซึ่งเป็นส่วน Business Logic ที่รับข้อมูลจาก Route/Middleware แล้วอ่านหรือแก้ไขข้อมูลผ่าน Prisma

**โค้ดก้อนนี้ทำงานอย่างไร:** Controller จะอ่านข้อมูลที่ผ่าน Validation และตัวตนจาก Request เรียก `prisma` เพื่อ query/create/update/delete ตามเงื่อนไข แล้วส่ง JSON และ HTTP status กลับ Frontend; กรณีผิดพลาดจะส่งต่อให้ Error handler

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อประกอบ Controller ครบ Endpoint ที่ Route เชื่อมไว้จะทำงานกับ Database จริงตามสิทธิ์และเงื่อนไขของระบบ

```js
import path from "path";
// ก้อนนี้นำ Module ที่จำเป็นเข้ามา เพื่อให้โค้ดส่วนถัดไปเรียกใช้ความสามารถเหล่านั้น
```

### 11.4 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/controllers/userController.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/controllers/userController.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 11.4 ทำหน้าที่ `userId มาจาก Token ที่ authMiddleware ตรวจเรียบร้อยแล้ว` ซึ่งเป็นส่วน Business Logic ที่รับข้อมูลจาก Route/Middleware แล้วอ่านหรือแก้ไขข้อมูลผ่าน Prisma

**โค้ดก้อนนี้ทำงานอย่างไร:** Controller จะอ่านข้อมูลที่ผ่าน Validation และตัวตนจาก Request เรียก `prisma` เพื่อ query/create/update/delete ตามเงื่อนไข แล้วส่ง JSON และ HTTP status กลับ Frontend; กรณีผิดพลาดจะส่งต่อให้ Error handler

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อประกอบ Controller ครบ Endpoint ที่ Route เชื่อมไว้จะทำงานกับ Database จริงตามสิทธิ์และเงื่อนไขของระบบ

```js
export async function getMe(req, res) {
// ประกาศฟังก์ชันนี้และกำหนดขอบเขตงานตามชื่อของฟังก์ชัน
  try {
    // userId มาจาก Token ที่ authMiddleware ตรวจเรียบร้อยแล้ว
    const user = await prisma.user.findUnique({
      where: { id: req.user.userId },
      // เลือกเฉพาะข้อมูลที่ปลอดภัย จึงไม่มี password ส่งกลับไป
      select: { id: true, name: true, email: true, role: true, avatarUrl: true },
      // เลือกเฉพาะข้อมูลที่ปลอดภัย จึงไม่มี password ส่งกลับไป
    });

    if (!user) {
    // ตรวจเงื่อนไขก่อนอนุญาตให้โค้ดภายในทำงาน
      return res.status(404).json({ message: "ไม่พบผู้ใช้" });
      // ส่ง HTTP status และ JSON กลับ Client พร้อมหยุด Controller
    }

    return res.json({ user });
    // ส่งข้อมูล JSON กลับ Postman หรือ Frontend
  } catch (error) {
  // รับ Error ที่เกิดจาก Prisma หรือ Logic ภายใน try
    console.error(error);
    // พิมพ์ Error จริงใน Terminal เพื่อช่วยวิเคราะห์โดยไม่ส่งรายละเอียดลับให้ Client
    return res.status(500).json({ message: "เกิดข้อผิดพลาดจาก Server" });
  }
}
```

### 11.5 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/controllers/userController.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/controllers/userController.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 11.5 ทำหน้าที่ `PATCH /api/users/me/avatar เปลี่ยนรูปโปรไฟล์ของผู้ใช้ที่ Login อยู่` ซึ่งเป็นส่วน Business Logic ที่รับข้อมูลจาก Route/Middleware แล้วอ่านหรือแก้ไขข้อมูลผ่าน Prisma

**โค้ดก้อนนี้ทำงานอย่างไร:** Controller จะอ่านข้อมูลที่ผ่าน Validation และตัวตนจาก Request เรียก `prisma` เพื่อ query/create/update/delete ตามเงื่อนไข แล้วส่ง JSON และ HTTP status กลับ Frontend; กรณีผิดพลาดจะส่งต่อให้ Error handler

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อประกอบ Controller ครบ Endpoint ที่ Route เชื่อมไว้จะทำงานกับ Database จริงตามสิทธิ์และเงื่อนไขของระบบ

```js
// PATCH /api/users/me/avatar เปลี่ยนรูปโปรไฟล์ของผู้ใช้ที่ Login อยู่
export async function updateAvatar(req, res) {
// PATCH /api/users/me/avatar เปลี่ยนรูปโปรไฟล์ของผู้ใช้ที่ Login อยู่
  try {
    if (!req.file) {
      return res.status(400).json({ message: "กรุณาเลือกรูปโปรไฟล์" });
      // ส่ง HTTP status และ JSON กลับ Client พร้อมหยุด Controller
    }

    const oldUser = await prisma.user.findUnique({
    // รอ Promise นี้ทำงานเสร็จก่อนใช้ผลลัพธ์หรือทำบรรทัดถัดไป
      where: { id: req.user.userId },
      select: { avatarUrl: true },
    });

    const user = await prisma.user.update({
    // รอ Promise นี้ทำงานเสร็จก่อนใช้ผลลัพธ์หรือทำบรรทัดถัดไป
      where: { id: req.user.userId },
      data: { avatarUrl: `/uploads/${req.file.filename}` },
      select: { id: true, name: true, email: true, role: true, avatarUrl: true },
      // เลือกเฉพาะข้อมูลที่ปลอดภัย จึงไม่มี password ส่งกลับไป
    });

    // เมื่อบันทึกรูปใหม่สำเร็จ จึงลบรูปเก่าเพื่อไม่ให้ไฟล์ค้าง
    if (oldUser?.avatarUrl) {
    // เมื่อบันทึกรูปใหม่สำเร็จ จึงลบรูปเก่าเพื่อไม่ให้ไฟล์ค้าง
      const oldImagePath = path.resolve(oldUser.avatarUrl.replace(/^\//, ""));
      // ประกาศค่าที่ใช้ภายในขอบเขตนี้และไม่อนุญาตให้เปลี่ยนตัวแปรไปอ้างค่าใหม่
      if (fs.existsSync(oldImagePath)) fs.unlinkSync(oldImagePath);
      // ตรวจเงื่อนไขก่อนอนุญาตให้โค้ดภายในทำงาน
    }

    return res.json({ message: "เปลี่ยนรูปโปรไฟล์สำเร็จ", user });
    // ส่งข้อมูล JSON กลับ Postman หรือ Frontend
  } catch (error) {
  // รับ Error ที่เกิดจาก Prisma หรือ Logic ภายใน try
    console.error(error);
    // พิมพ์ Error จริงใน Terminal เพื่อช่วยวิเคราะห์โดยไม่ส่งรายละเอียดลับให้ Client
    return res.status(500).json({ message: "เกิดข้อผิดพลาดจาก Server" });
  }
}
```

เมื่อคัดครบไฟล์ ให้รันจาก `BackEnd`:

```powershell
node --check src/controllers/userController.js
```

## 12. `BackEnd/src/routes/authRoutes.js`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนต่อไปนี้เรียงกัน

### 12.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `BackEnd/src/routes/authRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/authRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 12.1 ทำหน้าที่ `import express from "express";` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
import express from "express";
// ก้อนนี้นำ Module ที่จำเป็นเข้ามา เพื่อให้โค้ดส่วนถัดไปเรียกใช้ความสามารถเหล่านั้น
```

### 12.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/routes/authRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/authRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 12.2 ทำหน้าที่ `import { login, register } from "../controllers/authController.js";` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
import { login, register } from "../controllers/authController.js";
// นำ Dependency หรือ Module ที่บรรทัดถัดไปต้องใช้เข้ามาในไฟล์
```

### 12.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/routes/authRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/authRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 12.3 ทำหน้าที่ `import { validate } from "../middlewares/validateMiddleware.js";` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
import { validate } from "../middlewares/validateMiddleware.js";
// ก้อนนี้นำ Module ที่จำเป็นเข้ามา เพื่อให้โค้ดส่วนถัดไปเรียกใช้ความสามารถเหล่านั้น
```

### 12.4 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/routes/authRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/authRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 12.4 ทำหน้าที่ `import { loginSchema, registerSchema } from "../validations/schemas.js";` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
import { loginSchema, registerSchema } from "../validations/schemas.js";
// นำ Dependency หรือ Module ที่บรรทัดถัดไปต้องใช้เข้ามาในไฟล์
```

### 12.5 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/routes/authRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/authRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 12.5 ทำหน้าที่ `const router = express.Router();` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
const router = express.Router();
// ก้อนนี้เตรียมข้อมูลและควบคุมลำดับการทำงานก่อนส่งผลไปยังส่วนถัดไปของระบบ
```

### 12.6 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/routes/authRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/authRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 12.6 ทำหน้าที่ `Route รับ request แล้วส่งต่อไปยังฟังก์ชันใน Controller` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
router.post("/register", validate(registerSchema), register);
// Route รับ request แล้วส่งต่อไปยังฟังก์ชันใน Controller
```

### 12.7 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/routes/authRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/authRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 12.7 ทำหน้าที่ `router.post("/login", validate(loginSchema), login);` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
router.post("/login", validate(loginSchema), login);
// Route รับ request แล้วส่งต่อไปยังฟังก์ชันใน Controller
// ประกาศ Method และ URL แล้วเรียง Middleware ก่อน Controller
```

### 12.8 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/routes/authRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/authRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 12.8 ทำหน้าที่ `export default router;` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
export default router;
// ส่ง Router ออกไปให้ server.js import และ mount ใช้งาน
```

เมื่อคัดครบไฟล์ ให้รันจาก `BackEnd`:

```powershell
node --check src/routes/authRoutes.js
```

## 13. `BackEnd/src/routes/userRoutes.js`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนต่อไปนี้เรียงกัน

### 13.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `BackEnd/src/routes/userRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/userRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 13.1 ทำหน้าที่ `import express from "express";` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
import express from "express";
// ก้อนนี้นำ Module ที่จำเป็นเข้ามา เพื่อให้โค้ดส่วนถัดไปเรียกใช้ความสามารถเหล่านั้น
```

### 13.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/routes/userRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/userRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 13.2 ทำหน้าที่ `import { getMe, updateAvatar } from "../controllers/userController.js";` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
import { getMe, updateAvatar } from "../controllers/userController.js";
// นำ Dependency หรือ Module ที่บรรทัดถัดไปต้องใช้เข้ามาในไฟล์
```

### 13.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/routes/userRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/userRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 13.3 ทำหน้าที่ `import { authMiddleware } from "../middlewares/authMiddleware.js";` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
import { authMiddleware } from "../middlewares/authMiddleware.js";
// นำ Dependency หรือ Module ที่บรรทัดถัดไปต้องใช้เข้ามาในไฟล์
```

### 13.4 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/routes/userRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/userRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 13.4 ทำหน้าที่ `import { uploadAvatar } from "../middlewares/uploadMiddleware.js";` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
import { uploadAvatar } from "../middlewares/uploadMiddleware.js";
// นำ Dependency หรือ Module ที่บรรทัดถัดไปต้องใช้เข้ามาในไฟล์
```

### 13.5 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/routes/userRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/userRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 13.5 ทำหน้าที่ `const router = express.Router();` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
const router = express.Router();
// ก้อนนี้เตรียมข้อมูลและควบคุมลำดับการทำงานก่อนส่งผลไปยังส่วนถัดไปของระบบ
```

### 13.6 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/routes/userRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/userRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 13.6 ทำหน้าที่ `ลำดับ: รับ GET /me → ตรวจ Token → เรียก getMe` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
router.get("/me", authMiddleware, getMe);
// ลำดับ: รับ GET /me → ตรวจ Token → เรียก getMe
```

### 13.7 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/routes/userRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/userRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 13.7 ทำหน้าที่ `router.patch("/me/avatar", authMiddleware, uploadAvatar, updateAvatar);` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
router.patch("/me/avatar", authMiddleware, uploadAvatar, updateAvatar);
// ลำดับ: รับ GET /me → ตรวจ Token → เรียก getMe
// ประกาศ Method และ URL แล้วเรียง Middleware ก่อน Controller
```

### 13.8 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/routes/userRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/userRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 13.8 ทำหน้าที่ `export default router;` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
export default router;
// ส่ง Router ออกไปให้ server.js import และ mount ใช้งาน
```

เมื่อคัดครบไฟล์ ให้รันจาก `BackEnd`:

```powershell
node --check src/routes/userRoutes.js
```

### CHECKPOINT 3 — RUN DEV: Auth และ User

ตอนนี้ Auth/User ประกอบครบแล้ว ให้แทนที่ Server ชั่วคราวใน `BackEnd/src/server.js` ด้วยก้อนทดสอบนี้:

```js
import "dotenv/config";
import express from "express";
import authRoutes from "./routes/authRoutes.js";
// นำ Dependency หรือ Module ที่บรรทัดถัดไปต้องใช้เข้ามาในไฟล์
import userRoutes from "./routes/userRoutes.js";
// นำ Dependency หรือ Module ที่บรรทัดถัดไปต้องใช้เข้ามาในไฟล์

const app = express();
// app คือ Express application ที่รับ request จาก Frontend/Postman
const PORT = Number(process.env.PORT || 5000);
app.use(express.json());
// อ่าน JSON Body ก่อนส่ง request ต่อไปยัง Routes
app.get("/", (req, res) => res.json({ status: "ok", checkpoint: "auth-user" }));
app.use("/api/auth", authRoutes);
// เชื่อม URL แต่ละกลุ่มเข้ากับไฟล์ Route ของตัวเอง
app.use("/api/users", userRoutes);
// ติดตั้ง Middleware หรือ Route ให้ Express ตามลำดับจากบนลงล่าง
app.listen(PORT, () => console.log(`Server running at http://localhost:${PORT}`));
```

รัน `npm run dev` แล้วเปิด Terminal อีกอันที่ `BackEnd` เพื่อทดสอบ Register และ Login จริง:

```powershell
$checkpointEmail = "checkpoint-$([DateTimeOffset]::UtcNow.ToUnixTimeMilliseconds())@example.com"
$body = @{ name = "Checkpoint User"; email = $checkpointEmail; password = "123456" } | ConvertTo-Json
Invoke-RestMethod -Uri "http://localhost:5000/api/auth/register" -Method Post -ContentType "application/json" -Body $body
Invoke-RestMethod -Uri "http://localhost:5000/api/auth/login" -Method Post -ContentType "application/json" -Body $body
```

ต้องได้ User จาก Register และ Token จาก Login โดยไม่มี HTML/error จากนั้นกด `Ctrl+C` ที่ Terminal Server ก่อนทำหัวข้อ 14

## 14. `BackEnd/src/controllers/placeController.js`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนต่อไปนี้เรียงกัน

### 14.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `BackEnd/src/controllers/placeController.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/controllers/placeController.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 14.1 ทำหน้าที่ `import { prisma } from "../lib/prisma.js";` ซึ่งเป็นส่วน Business Logic ที่รับข้อมูลจาก Route/Middleware แล้วอ่านหรือแก้ไขข้อมูลผ่าน Prisma

**โค้ดก้อนนี้ทำงานอย่างไร:** Controller จะอ่านข้อมูลที่ผ่าน Validation และตัวตนจาก Request เรียก `prisma` เพื่อ query/create/update/delete ตามเงื่อนไข แล้วส่ง JSON และ HTTP status กลับ Frontend; กรณีผิดพลาดจะส่งต่อให้ Error handler

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อประกอบ Controller ครบ Endpoint ที่ Route เชื่อมไว้จะทำงานกับ Database จริงตามสิทธิ์และเงื่อนไขของระบบ

```js
import { prisma } from "../lib/prisma.js";
// ก้อนนี้ติดต่อ Database ผ่าน Prisma เพื่ออ่านหรือเปลี่ยนแปลงข้อมูลตามงานของระบบ
```

### 14.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/controllers/placeController.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/controllers/placeController.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 14.2 ทำหน้าที่ `import fs from "fs";` ซึ่งเป็นส่วน Business Logic ที่รับข้อมูลจาก Route/Middleware แล้วอ่านหรือแก้ไขข้อมูลผ่าน Prisma

**โค้ดก้อนนี้ทำงานอย่างไร:** Controller จะอ่านข้อมูลที่ผ่าน Validation และตัวตนจาก Request เรียก `prisma` เพื่อ query/create/update/delete ตามเงื่อนไข แล้วส่ง JSON และ HTTP status กลับ Frontend; กรณีผิดพลาดจะส่งต่อให้ Error handler

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อประกอบ Controller ครบ Endpoint ที่ Route เชื่อมไว้จะทำงานกับ Database จริงตามสิทธิ์และเงื่อนไขของระบบ

```js
import fs from "fs";
// ก้อนนี้นำ Module ที่จำเป็นเข้ามา เพื่อให้โค้ดส่วนถัดไปเรียกใช้ความสามารถเหล่านั้น
```

### 14.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/controllers/placeController.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/controllers/placeController.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 14.3 ทำหน้าที่ `import path from "path";` ซึ่งเป็นส่วน Business Logic ที่รับข้อมูลจาก Route/Middleware แล้วอ่านหรือแก้ไขข้อมูลผ่าน Prisma

**โค้ดก้อนนี้ทำงานอย่างไร:** Controller จะอ่านข้อมูลที่ผ่าน Validation และตัวตนจาก Request เรียก `prisma` เพื่อ query/create/update/delete ตามเงื่อนไข แล้วส่ง JSON และ HTTP status กลับ Frontend; กรณีผิดพลาดจะส่งต่อให้ Error handler

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อประกอบ Controller ครบ Endpoint ที่ Route เชื่อมไว้จะทำงานกับ Database จริงตามสิทธิ์และเงื่อนไขของระบบ

```js
import path from "path";
// ก้อนนี้นำ Module ที่จำเป็นเข้ามา เพื่อให้โค้ดส่วนถัดไปเรียกใช้ความสามารถเหล่านั้น
```

### 14.4 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/controllers/placeController.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/controllers/placeController.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 14.4 ทำหน้าที่ `const placeTypes = ["toilet", "trash"];` ซึ่งเป็นส่วน Business Logic ที่รับข้อมูลจาก Route/Middleware แล้วอ่านหรือแก้ไขข้อมูลผ่าน Prisma

**โค้ดก้อนนี้ทำงานอย่างไร:** Controller จะอ่านข้อมูลที่ผ่าน Validation และตัวตนจาก Request เรียก `prisma` เพื่อ query/create/update/delete ตามเงื่อนไข แล้วส่ง JSON และ HTTP status กลับ Frontend; กรณีผิดพลาดจะส่งต่อให้ Error handler

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อประกอบ Controller ครบ Endpoint ที่ Route เชื่อมไว้จะทำงานกับ Database จริงตามสิทธิ์และเงื่อนไขของระบบ

```js
const placeTypes = ["toilet", "trash"];
// กำหนดประเภท Place ที่ Backend ยอมรับ เพื่อกันค่าที่ระบบไม่รู้จัก
```

### 14.5 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/controllers/placeController.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/controllers/placeController.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 14.5 ทำหน้าที่ `const placeStatuses = ["pending", "active", "rejected", "inactive"];` ซึ่งเป็นส่วน Business Logic ที่รับข้อมูลจาก Route/Middleware แล้วอ่านหรือแก้ไขข้อมูลผ่าน Prisma

**โค้ดก้อนนี้ทำงานอย่างไร:** Controller จะอ่านข้อมูลที่ผ่าน Validation และตัวตนจาก Request เรียก `prisma` เพื่อ query/create/update/delete ตามเงื่อนไข แล้วส่ง JSON และ HTTP status กลับ Frontend; กรณีผิดพลาดจะส่งต่อให้ Error handler

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อประกอบ Controller ครบ Endpoint ที่ Route เชื่อมไว้จะทำงานกับ Database จริงตามสิทธิ์และเงื่อนไขของระบบ

```js
const placeStatuses = ["pending", "active", "rejected", "inactive"];
// กำหนดสถานะ Place ที่ Backend ยอมรับสำหรับการค้นหาและอัปเดต
```

### 14.6 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/controllers/placeController.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/controllers/placeController.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 14.6 ทำหน้าที่ `สูตรคำนวณระยะทางระหว่างพิกัดสองจุด ผลลัพธ์เป็นกิโลเมตร` ซึ่งเป็นส่วน Business Logic ที่รับข้อมูลจาก Route/Middleware แล้วอ่านหรือแก้ไขข้อมูลผ่าน Prisma

**โค้ดก้อนนี้ทำงานอย่างไร:** Controller จะอ่านข้อมูลที่ผ่าน Validation และตัวตนจาก Request เรียก `prisma` เพื่อ query/create/update/delete ตามเงื่อนไข แล้วส่ง JSON และ HTTP status กลับ Frontend; กรณีผิดพลาดจะส่งต่อให้ Error handler

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อประกอบ Controller ครบ Endpoint ที่ Route เชื่อมไว้จะทำงานกับ Database จริงตามสิทธิ์และเงื่อนไขของระบบ

```js
// สูตรคำนวณระยะทางระหว่างพิกัดสองจุด ผลลัพธ์เป็นกิโลเมตร
function calculateDistance(lat1, lng1, lat2, lng2) {
// สูตรคำนวณระยะทางระหว่างพิกัดสองจุด ผลลัพธ์เป็นกิโลเมตร
  const earthRadius = 6371;
  // กำหนดรัศมีเฉลี่ยของโลกเป็นกิโลเมตร จึงได้ผลลัพธ์ระยะทางเป็น km
  const toRadians = (degree) => (degree * Math.PI) / 180;
  // สร้างฟังก์ชันแปลงองศาเป็นเรเดียน เพราะ Math.sin/Math.cos ใช้เรเดียน
  const latitudeDistance = toRadians(lat2 - lat1);
  // หาระยะต่างของละติจูดแล้วแปลงเป็นเรเดียน
  const longitudeDistance = toRadians(lng2 - lng1);
  // หาระยะต่างของลองจิจูดแล้วแปลงเป็นเรเดียน

  const value =
  // เริ่มคำนวณค่ากลางของสูตร Haversine จากความต่างของพิกัด
    Math.sin(latitudeDistance / 2) ** 2 +
    // ส่วนของความต่าง latitude ในสูตร
    Math.cos(toRadians(lat1)) *
    // ปรับค่าตาม latitude ของจุดเริ่มต้น
      Math.cos(toRadians(lat2)) *
      // ปรับค่าตาม latitude ของจุดปลายทาง
      Math.sin(longitudeDistance / 2) ** 2;
      // ส่วนของความต่าง longitude ในสูตร

  return earthRadius * 2 * Math.atan2(Math.sqrt(value), Math.sqrt(1 - value));
  // แปลงค่ามุมจากสูตรเป็นระยะทางจริงหน่วยกิโลเมตรแล้วส่งกลับ
}
```

### 14.7 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/controllers/placeController.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/controllers/placeController.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 14.7 ทำหน้าที่ `GET /api/places ใช้แสดงสถานที่ active และใช้ค้นหาด้วย type/name` ซึ่งเป็นส่วน Business Logic ที่รับข้อมูลจาก Route/Middleware แล้วอ่านหรือแก้ไขข้อมูลผ่าน Prisma

**โค้ดก้อนนี้ทำงานอย่างไร:** Controller จะอ่านข้อมูลที่ผ่าน Validation และตัวตนจาก Request เรียก `prisma` เพื่อ query/create/update/delete ตามเงื่อนไข แล้วส่ง JSON และ HTTP status กลับ Frontend; กรณีผิดพลาดจะส่งต่อให้ Error handler

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อประกอบ Controller ครบ Endpoint ที่ Route เชื่อมไว้จะทำงานกับ Database จริงตามสิทธิ์และเงื่อนไขของระบบ

```js
// GET /api/places ใช้แสดงสถานที่ active และใช้ค้นหาด้วย type/name
export async function getPlaces(req, res) {
// GET /api/places ใช้แสดงสถานที่ active และใช้ค้นหาด้วย type/name
  try {
    const { type, search, status } = req.query;
    // อ่านตัวกรอง type, search และ status จาก Query string ของ URL

    if (type && !placeTypes.includes(type)) {
    // ตรวจเงื่อนไขนี้ก่อนทำงานต่อ และหยุดด้วย Response ที่กำหนดเมื่อข้อมูลไม่ผ่าน
      return res.status(400).json({ message: "type ต้องเป็น toilet หรือ trash" });
      // ตอบ 400 Bad Request เมื่อค่าที่ Client ส่งมาไม่ถูกต้อง
    }

    if (status && status !== "all" && !placeStatuses.includes(status)) {
    // ตรวจเงื่อนไขนี้ก่อนทำงานต่อ และหยุดด้วย Response ที่กำหนดเมื่อข้อมูลไม่ผ่าน
      return res.status(400).json({ message: "status ไม่ถูกต้อง" });
    }

    // เฉพาะ Admin ที่ส่ง Token เท่านั้นจึงขอดูทุกสถานะพร้อมกันได้
    if (status === "all" && req.user?.role !== "admin") {
    // เฉพาะ Admin ที่ส่ง Token เท่านั้นจึงขอดูทุกสถานะพร้อมกันได้
      return res.status(403).json({ message: "เฉพาะ Admin ที่ดูสถานที่ทั้งหมดได้" });
      // ตอบ 403 Forbidden เมื่อ Login แล้วแต่ไม่มีสิทธิ์ทำรายการนี้
    }

    // ถ้าไม่ส่ง status มา จะคืนเฉพาะ active เพื่อให้หน้า Map ปลอดภัยตาม Logic
    const places = await prisma.place.findMany({
      where: {
      // กำหนดเงื่อนไขที่ Prisma ใช้ค้นหาแถวใน Database
        ...(status === "all" ? {} : { status: status || "active" }),
        // all ไม่กรองสถานะ; ค่าเริ่มต้นกรอง active
        ...(type ? { type } : {}),
        // มี type จึงเพิ่มเงื่อนไข toilet/trash
        ...(search ? { name: { contains: search } } : {}),
        // มี search จึงค้นคำที่อยู่ภายในชื่อ
      },
      orderBy: { createdAt: "desc" },
      // กำหนดลำดับผลลัพธ์จาก Database ตามเวลาสร้าง
    });

    return res.json({ places });
    // ส่งผลลัพธ์ JSON กลับ Postman หรือ Frontend แล้วจบ Controller
  } catch (error) {
  // รับ Error ที่เกิดจาก Prisma หรือ Logic ภายใน try
    console.error(error);
    // พิมพ์ Error จริงใน Terminal เพื่อช่วยวิเคราะห์โดยไม่ส่งรายละเอียดลับให้ Client
    return res.status(500).json({ message: "เกิดข้อผิดพลาดจาก Server" });
  }
}
```

### 14.8 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/controllers/placeController.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/controllers/placeController.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 14.8 ทำหน้าที่ `GET /api/places/nearby?latitude=...&longitude=...&radius=5` ซึ่งเป็นส่วน Business Logic ที่รับข้อมูลจาก Route/Middleware แล้วอ่านหรือแก้ไขข้อมูลผ่าน Prisma

**โค้ดก้อนนี้ทำงานอย่างไร:** Controller จะอ่านข้อมูลที่ผ่าน Validation และตัวตนจาก Request เรียก `prisma` เพื่อ query/create/update/delete ตามเงื่อนไข แล้วส่ง JSON และ HTTP status กลับ Frontend; กรณีผิดพลาดจะส่งต่อให้ Error handler

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อประกอบ Controller ครบ Endpoint ที่ Route เชื่อมไว้จะทำงานกับ Database จริงตามสิทธิ์และเงื่อนไขของระบบ

```js
// GET /api/places/nearby?latitude=...&longitude=...&radius=5
export async function getNearbyPlaces(req, res) {
// GET /api/places/nearby?latitude=...&longitude=...&radius=5
  try {
    const latitude = Number(req.query.latitude);
    // แปลง latitude จาก Query string เป็น Number
    const longitude = Number(req.query.longitude);
    // แปลง longitude จาก Query string เป็น Number
    const radius = Number(req.query.radius || 5);
    // แปลง radius เป็น Number และใช้ 5 กิโลเมตรเมื่อผู้ใช้ไม่ส่งค่า

    if (
      !Number.isFinite(latitude) ||
      // latitude ต้องเป็นตัวเลขที่ใช้งานได้
      !Number.isFinite(longitude) ||
      // longitude ต้องเป็นตัวเลขที่ใช้งานได้
      radius <= 0
      // รัศมีต้องมากกว่า 0 กิโลเมตร
    ) {
      return res.status(400).json({
        message: "กรุณาส่ง latitude, longitude และ radius ให้ถูกต้อง",
        // ข้อความที่ Postman/Frontend นำไปแสดง
      });
    }

    // Marker ทุกตัวมาจาก Database และเลือกเฉพาะสถานที่ active
    const activePlaces = await prisma.place.findMany({
    // Marker ทุกตัวมาจาก Database และเลือกเฉพาะสถานที่ active
      where: { status: "active" },
      // กำหนดเงื่อนไขที่ Prisma ใช้ค้นหาแถวใน Database
    });

    // คำนวณระยะทาง เพิ่ม distance แล้วกรองตามรัศมี
    const places = activePlaces
    // คำนวณระยะทาง เพิ่ม distance แล้วกรองตามรัศมี
      .map((place) => ({
      // วนทุก Place แล้วสร้าง Object ใหม่ที่เพิ่มค่า distance
        ...place,
        distance: calculateDistance(
        // เรียกสูตรคำนวณระยะทางจากตำแหน่งผู้ใช้ไปยัง Place นี้
          latitude,
          // latitude ของผู้ใช้หรือจุดค้นหา
          longitude,
          // longitude ของผู้ใช้หรือจุดค้นหา
          place.latitude,
          // latitude ของ Place จาก Database
          place.longitude
          // longitude ของ Place จาก Database
        ),
      }))
      .filter((place) => place.distance <= radius)
      // เก็บเฉพาะ Place ที่อยู่ไม่เกินรัศมีที่ผู้ใช้กำหนด
      .sort((a, b) => a.distance - b.distance);
      // เรียง Place จากใกล้ไปไกลตามค่า distance

    return res.json({ places });
    // ส่งผลลัพธ์ JSON กลับ Postman หรือ Frontend แล้วจบ Controller
  } catch (error) {
  // รับ Error ที่เกิดจาก Prisma หรือ Logic ภายใน try
    console.error(error);
    // พิมพ์ Error จริงใน Terminal เพื่อช่วยวิเคราะห์โดยไม่ส่งรายละเอียดลับให้ Client
    return res.status(500).json({ message: "เกิดข้อผิดพลาดจาก Server" });
  }
}
```

### 14.9 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/controllers/placeController.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/controllers/placeController.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 14.9 ทำหน้าที่ `GET /api/places/:id ใช้เปิดหน้า Place Detail` ซึ่งเป็นส่วน Business Logic ที่รับข้อมูลจาก Route/Middleware แล้วอ่านหรือแก้ไขข้อมูลผ่าน Prisma

**โค้ดก้อนนี้ทำงานอย่างไร:** Controller จะอ่านข้อมูลที่ผ่าน Validation และตัวตนจาก Request เรียก `prisma` เพื่อ query/create/update/delete ตามเงื่อนไข แล้วส่ง JSON และ HTTP status กลับ Frontend; กรณีผิดพลาดจะส่งต่อให้ Error handler

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อประกอบ Controller ครบ Endpoint ที่ Route เชื่อมไว้จะทำงานกับ Database จริงตามสิทธิ์และเงื่อนไขของระบบ

```js
// GET /api/places/:id ใช้เปิดหน้า Place Detail
export async function getPlaceById(req, res) {
// GET /api/places/:id ใช้เปิดหน้า Place Detail
  try {
    const id = Number(req.params.id);

    if (!Number.isInteger(id)) {
      return res.status(400).json({ message: "Place id ไม่ถูกต้อง" });
      // ตอบ 400 Bad Request เมื่อค่าที่ Client ส่งมาไม่ถูกต้อง
    }

    const place = await prisma.place.findUnique({ where: { id } });
    // ค้นหา Place หนึ่งรายการด้วย id ที่เป็น unique

    if (!place) {
      return res.status(404).json({ message: "ไม่พบสถานที่" });
    }

    return res.json({ place });
    // ส่งผลลัพธ์ JSON กลับ Postman หรือ Frontend แล้วจบ Controller
  } catch (error) {
  // รับ Error ที่เกิดจาก Prisma หรือ Logic ภายใน try
    console.error(error);
    // พิมพ์ Error จริงใน Terminal เพื่อช่วยวิเคราะห์โดยไม่ส่งรายละเอียดลับให้ Client
    return res.status(500).json({ message: "เกิดข้อผิดพลาดจาก Server" });
  }
}
```

### 14.10 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/controllers/placeController.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/controllers/placeController.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 14.10 ทำหน้าที่ `POST /api/places ผู้ใช้ต้อง Login ก่อนจึงมาถึง Controller นี้` ซึ่งเป็นส่วน Business Logic ที่รับข้อมูลจาก Route/Middleware แล้วอ่านหรือแก้ไขข้อมูลผ่าน Prisma

**โค้ดก้อนนี้ทำงานอย่างไร:** Controller จะอ่านข้อมูลที่ผ่าน Validation และตัวตนจาก Request เรียก `prisma` เพื่อ query/create/update/delete ตามเงื่อนไข แล้วส่ง JSON และ HTTP status กลับ Frontend; กรณีผิดพลาดจะส่งต่อให้ Error handler

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อประกอบ Controller ครบ Endpoint ที่ Route เชื่อมไว้จะทำงานกับ Database จริงตามสิทธิ์และเงื่อนไขของระบบ

```js
// POST /api/places ผู้ใช้ต้อง Login ก่อนจึงมาถึง Controller นี้
export async function createPlace(req, res) {
// POST /api/places ผู้ใช้ต้อง Login ก่อนจึงมาถึง Controller นี้
  try {
    const { name, type, address, latitude, longitude } = req.body;
    // อ่านข้อมูลสถานที่จาก Body ที่ Validation และ Multer ส่งต่อมา
    const parsedLatitude = Number(latitude);
    // แปลง latitude จาก multipart/form-data ซึ่งเป็น string ให้เป็น Number
    const parsedLongitude = Number(longitude);
    // แปลง longitude จาก multipart/form-data ซึ่งเป็น string ให้เป็น Number

    if (!name || !address || !placeTypes.includes(type)) {
    // ตรวจเงื่อนไขนี้ก่อนทำงานต่อ และหยุดด้วย Response ที่กำหนดเมื่อข้อมูลไม่ผ่าน
      return res
      // ส่ง Response แล้วหยุด Controller
      // ส่งผลลัพธ์ออกจากฟังก์ชันและหยุดทำบรรทัดถัดไปในฟังก์ชันนี้
        .status(400)
        // 400 หมายถึงข้อมูลจาก Client ไม่ผ่านกฎ
        .json({ message: "กรุณากรอกชื่อ ประเภท และที่อยู่ให้ถูกต้อง" });
        // ส่งสาเหตุเป็น JSON
    }

    if (!Number.isFinite(parsedLatitude) || !Number.isFinite(parsedLongitude)) {
    // ตรวจเงื่อนไขนี้ก่อนทำงานต่อ และหยุดด้วย Response ที่กำหนดเมื่อข้อมูลไม่ผ่าน
      return res.status(400).json({ message: "latitude และ longitude ต้องเป็นตัวเลข" });
      // ตอบ 400 Bad Request เมื่อค่าที่ Client ส่งมาไม่ถูกต้อง
    }

    // ทุกสถานที่ใหม่ต้องมีรูปจาก field image ของ multipart/form-data
    if (!req.file) {
      return res.status(400).json({ message: "กรุณาถ่ายหรือเลือกรูปสถานที่" });
      // ตอบ 400 Bad Request เมื่อค่าที่ Client ส่งมาไม่ถูกต้อง
    }

    // Backend เป็นผู้กำหนด status เอง เพื่อป้องกัน Member ส่ง active มาเอง
    const status = req.user.role === "admin" ? "active" : "pending";
    // Backend เป็นผู้กำหนด status เอง เพื่อป้องกัน Member ส่ง active มาเอง

    const place = await prisma.place.create({
    // สร้างแถว Place ใหม่ใน Database ด้วยข้อมูลที่เตรียมไว้
      data: {
      // กำหนดข้อมูลที่ Prisma จะสร้างหรืออัปเดตใน Database
        name,
        // ชื่อสถานที่จาก Body
        type,
        // ประเภท toilet หรือ trash ที่ Validation ตรวจแล้ว
        address,
        // ที่อยู่สำหรับแสดงในรายละเอียด
        imageUrl: `/uploads/${req.file.filename}`,
        // เก็บ URL สำหรับเปิดรูป แทนการเก็บ Binary file ลง Database
        latitude: parsedLatitude,
        // พิกัด latitude ที่แปลงเป็น Number แล้ว
        longitude: parsedLongitude,
        // พิกัด longitude ที่แปลงเป็น Number แล้ว
        status,
        // active สำหรับ Admin หรือ pending สำหรับ Member
        createdById: req.user.userId,
        // ผูก Place กับ id ของผู้ใช้ที่ผ่าน Token มาแล้ว
      },
    });

    return res.status(201).json({ message: "เพิ่มสถานที่สำเร็จ", place });
    // ตอบ 201 Created พร้อม Place ที่เพิ่งบันทึกสำเร็จ
  } catch (error) {
  // รับ Error ที่เกิดจาก Prisma หรือ Logic ภายใน try
    console.error(error);
    // พิมพ์ Error จริงใน Terminal เพื่อช่วยวิเคราะห์โดยไม่ส่งรายละเอียดลับให้ Client
    return res.status(500).json({ message: "เกิดข้อผิดพลาดจาก Server" });
  }
}
```

### 14.11 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/controllers/placeController.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/controllers/placeController.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 14.11 ทำหน้าที่ `GET /api/places/pending สำหรับหน้า Admin Pending Places` ซึ่งเป็นส่วน Business Logic ที่รับข้อมูลจาก Route/Middleware แล้วอ่านหรือแก้ไขข้อมูลผ่าน Prisma

**โค้ดก้อนนี้ทำงานอย่างไร:** Controller จะอ่านข้อมูลที่ผ่าน Validation และตัวตนจาก Request เรียก `prisma` เพื่อ query/create/update/delete ตามเงื่อนไข แล้วส่ง JSON และ HTTP status กลับ Frontend; กรณีผิดพลาดจะส่งต่อให้ Error handler

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อประกอบ Controller ครบ Endpoint ที่ Route เชื่อมไว้จะทำงานกับ Database จริงตามสิทธิ์และเงื่อนไขของระบบ

```js
// GET /api/places/pending สำหรับหน้า Admin Pending Places
export async function getPendingPlaces(req, res) {
// GET /api/places/pending สำหรับหน้า Admin Pending Places
  try {
    const places = await prisma.place.findMany({
      where: { status: "pending" },
      // กำหนดเงื่อนไขที่ Prisma ใช้ค้นหาแถวใน Database
      include: {
      // ขอข้อมูล Relation ที่เกี่ยวข้องกลับมาพร้อม Place
        createdBy: { select: { id: true, name: true, email: true } },
        // แนบข้อมูลผู้ส่ง Place โดยไม่คืน password
      },
      orderBy: { createdAt: "asc" },
      // กำหนดลำดับผลลัพธ์จาก Database ตามเวลาสร้าง
    });

    return res.json({ places });
    // ส่งผลลัพธ์ JSON กลับ Postman หรือ Frontend แล้วจบ Controller
  } catch (error) {
  // รับ Error ที่เกิดจาก Prisma หรือ Logic ภายใน try
    console.error(error);
    // พิมพ์ Error จริงใน Terminal เพื่อช่วยวิเคราะห์โดยไม่ส่งรายละเอียดลับให้ Client
    return res.status(500).json({ message: "เกิดข้อผิดพลาดจาก Server" });
  }
}
```

### 14.12 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/controllers/placeController.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/controllers/placeController.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 14.12 ทำหน้าที่ `PATCH /api/places/:id/status สำหรับ Admin อนุมัติ ปฏิเสธ หรือปิดสถานที่` ซึ่งเป็นส่วน Business Logic ที่รับข้อมูลจาก Route/Middleware แล้วอ่านหรือแก้ไขข้อมูลผ่าน Prisma

**โค้ดก้อนนี้ทำงานอย่างไร:** Controller จะอ่านข้อมูลที่ผ่าน Validation และตัวตนจาก Request เรียก `prisma` เพื่อ query/create/update/delete ตามเงื่อนไข แล้วส่ง JSON และ HTTP status กลับ Frontend; กรณีผิดพลาดจะส่งต่อให้ Error handler

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อประกอบ Controller ครบ Endpoint ที่ Route เชื่อมไว้จะทำงานกับ Database จริงตามสิทธิ์และเงื่อนไขของระบบ

```js
// PATCH /api/places/:id/status สำหรับ Admin อนุมัติ ปฏิเสธ หรือปิดสถานที่
export async function updatePlaceStatus(req, res) {
// PATCH /api/places/:id/status สำหรับ Admin อนุมัติ ปฏิเสธ หรือปิดสถานที่
  try {
    const id = Number(req.params.id);
    const { status } = req.body;

    if (!Number.isInteger(id) || !placeStatuses.includes(status)) {
    // ตรวจเงื่อนไขนี้ก่อนทำงานต่อ และหยุดด้วย Response ที่กำหนดเมื่อข้อมูลไม่ผ่าน
      return res.status(400).json({ message: "Place id หรือ status ไม่ถูกต้อง" });
      // ตอบ 400 Bad Request เมื่อค่าที่ Client ส่งมาไม่ถูกต้อง
    }

    const existingPlace = await prisma.place.findUnique({ where: { id } });
    // ค้นหา Place หนึ่งรายการด้วย id ที่เป็น unique

    if (!existingPlace) {
    // ตรวจเงื่อนไขนี้ก่อนทำงานต่อ และหยุดด้วย Response ที่กำหนดเมื่อข้อมูลไม่ผ่าน
      return res.status(404).json({ message: "ไม่พบสถานที่" });
    }

    const place = await prisma.place.update({
    // อัปเดตสถานะของ Place ที่พบใน Database
      where: { id },
      // กำหนดเงื่อนไขที่ Prisma ใช้ค้นหาแถวใน Database
      data: { status },
      // กำหนดข้อมูลที่ Prisma จะสร้างหรืออัปเดตใน Database
    });

    return res.json({ message: "เปลี่ยนสถานะสำเร็จ", place });
    // ส่งผลลัพธ์ JSON กลับ Postman หรือ Frontend แล้วจบ Controller
  } catch (error) {
  // รับ Error ที่เกิดจาก Prisma หรือ Logic ภายใน try
    console.error(error);
    // พิมพ์ Error จริงใน Terminal เพื่อช่วยวิเคราะห์โดยไม่ส่งรายละเอียดลับให้ Client
    return res.status(500).json({ message: "เกิดข้อผิดพลาดจาก Server" });
  }
}
```

### 14.13 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/controllers/placeController.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/controllers/placeController.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 14.13 ทำหน้าที่ `DELETE /api/places/:id สำหรับ Admin เท่านั้น` ซึ่งเป็นส่วน Business Logic ที่รับข้อมูลจาก Route/Middleware แล้วอ่านหรือแก้ไขข้อมูลผ่าน Prisma

**โค้ดก้อนนี้ทำงานอย่างไร:** Controller จะอ่านข้อมูลที่ผ่าน Validation และตัวตนจาก Request เรียก `prisma` เพื่อ query/create/update/delete ตามเงื่อนไข แล้วส่ง JSON และ HTTP status กลับ Frontend; กรณีผิดพลาดจะส่งต่อให้ Error handler

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อประกอบ Controller ครบ Endpoint ที่ Route เชื่อมไว้จะทำงานกับ Database จริงตามสิทธิ์และเงื่อนไขของระบบ

```js
// DELETE /api/places/:id สำหรับ Admin เท่านั้น
export async function deletePlace(req, res) {
// DELETE /api/places/:id สำหรับ Admin เท่านั้น
  try {
    const id = Number(req.params.id);

    if (!Number.isInteger(id)) {
      return res.status(400).json({ message: "Place id ไม่ถูกต้อง" });
      // ตอบ 400 Bad Request เมื่อค่าที่ Client ส่งมาไม่ถูกต้อง
    }

    const existingPlace = await prisma.place.findUnique({
    // ค้นหา Place หนึ่งรายการด้วย id ที่เป็น unique
      where: { id },
      // กำหนดเงื่อนไขที่ Prisma ใช้ค้นหาแถวใน Database
      include: { reports: { select: { evidenceImageUrl: true } } },
      // ขอข้อมูล Relation ที่เกี่ยวข้องกลับมาพร้อม Place
    });

    if (!existingPlace) {
    // ตรวจเงื่อนไขนี้ก่อนทำงานต่อ และหยุดด้วย Response ที่กำหนดเมื่อข้อมูลไม่ผ่าน
      return res.status(404).json({ message: "ไม่พบสถานที่" });
    }

    // Schema ตั้ง onDelete: Cascade จึงลบ Reports ของสถานที่นี้ตามไปด้วย
    await prisma.place.delete({ where: { id } });
    // Schema ตั้ง onDelete: Cascade จึงลบ Reports ของสถานที่นี้ตามไปด้วย

    // ลบไฟล์รูปของ Place ออกจาก uploads หลังลบข้อมูลสำเร็จ
    if (existingPlace.imageUrl) {
    // ลบไฟล์รูปของ Place ออกจาก uploads หลังลบข้อมูลสำเร็จ
      const imagePath = path.resolve(existingPlace.imageUrl.replace(/^\//, ""));
      // แปลง URL รูป Place เป็น Absolute path สำหรับลบไฟล์จริง
      if (fs.existsSync(imagePath)) fs.unlinkSync(imagePath);
      // ตรวจเงื่อนไขนี้ก่อนทำงานต่อ และหยุดด้วย Response ที่กำหนดเมื่อข้อมูลไม่ผ่าน
    }
    // Place ถูกลบแล้ว Reports จะหายแบบ Cascade จึงลบรูปหลักฐานของ Reports ด้วย
    for (const report of existingPlace.reports) {
    // Place ถูกลบแล้ว Reports จะหายแบบ Cascade จึงลบรูปหลักฐานของ Reports ด้วย
      if (!report.evidenceImageUrl) continue;
      // ตรวจเงื่อนไขนี้ก่อนทำงานต่อ และหยุดด้วย Response ที่กำหนดเมื่อข้อมูลไม่ผ่าน
      const evidencePath = path.resolve(
      // แปลง URL รูปหลักฐาน Report เป็น Absolute path สำหรับลบไฟล์จริง
        report.evidenceImageUrl.replace(/^\//, "")
        // ตัด / ด้านหน้าเพื่อแปลง URL เป็น path ในเครื่อง
      );
      if (fs.existsSync(evidencePath)) fs.unlinkSync(evidencePath);
    }

    return res.json({ message: "ลบสถานที่สำเร็จ" });
    // ส่งผลลัพธ์ JSON กลับ Postman หรือ Frontend แล้วจบ Controller
  } catch (error) {
  // รับ Error ที่เกิดจาก Prisma หรือ Logic ภายใน try
    console.error(error);
    // พิมพ์ Error จริงใน Terminal เพื่อช่วยวิเคราะห์โดยไม่ส่งรายละเอียดลับให้ Client
    return res.status(500).json({ message: "เกิดข้อผิดพลาดจาก Server" });
  }
}
```

เมื่อคัดครบไฟล์ ให้รันจาก `BackEnd`:

```powershell
node --check src/controllers/placeController.js
```

## 15. `BackEnd/src/routes/placeRoutes.js`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนต่อไปนี้เรียงกัน

### 15.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `BackEnd/src/routes/placeRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/placeRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 15.1 ทำหน้าที่ `import express from "express";` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
import express from "express";
// ก้อนนี้นำ Module ที่จำเป็นเข้ามา เพื่อให้โค้ดส่วนถัดไปเรียกใช้ความสามารถเหล่านั้น
```

### 15.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/routes/placeRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/placeRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 15.2 ทำหน้าที่ `import {` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
import {
  createPlace,
  // เรียก Controller นี้เป็นขั้นตอนสุดท้ายเมื่อ Middleware ก่อนหน้าผ่านทั้งหมด
  deletePlace,
  // เรียก Controller นี้เป็นขั้นตอนสุดท้ายเมื่อ Middleware ก่อนหน้าผ่านทั้งหมด
  getNearbyPlaces,
  // เรียก Controller นี้เป็นขั้นตอนสุดท้ายเมื่อ Middleware ก่อนหน้าผ่านทั้งหมด
  getPendingPlaces,
  // เรียก Controller นี้เป็นขั้นตอนสุดท้ายเมื่อ Middleware ก่อนหน้าผ่านทั้งหมด
  getPlaceById,
  // เรียก Controller นี้เป็นขั้นตอนสุดท้ายเมื่อ Middleware ก่อนหน้าผ่านทั้งหมด
  getPlaces,
  // เรียก Controller นี้เป็นขั้นตอนสุดท้ายเมื่อ Middleware ก่อนหน้าผ่านทั้งหมด
  updatePlaceStatus,
  // เรียก Controller นี้เป็นขั้นตอนสุดท้ายเมื่อ Middleware ก่อนหน้าผ่านทั้งหมด
} from "../controllers/placeController.js";
// นำ Controller ของ Place เข้ามาเป็นขั้นตอนสุดท้ายของแต่ละ Route
```

### 15.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/routes/placeRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/placeRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 15.3 ทำหน้าที่ `import {` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
import {
  authMiddleware,
  optionalAuthMiddleware,
} from "../middlewares/authMiddleware.js";
// นำ Middleware ตรวจ Token ทั้งแบบบังคับและแบบไม่บังคับมาใช้
```

### 15.4 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/routes/placeRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/placeRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 15.4 ทำหน้าที่ `import { adminMiddleware } from "../middlewares/adminMiddleware.js";` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
import { adminMiddleware } from "../middlewares/adminMiddleware.js";
// ก้อนนี้นำ Module ที่จำเป็นเข้ามา เพื่อให้โค้ดส่วนถัดไปเรียกใช้ความสามารถเหล่านั้น
```

### 15.5 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/routes/placeRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/placeRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 15.5 ทำหน้าที่ `import { uploadPlaceImage } from "../middlewares/uploadMiddleware.js";` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
import { uploadPlaceImage } from "../middlewares/uploadMiddleware.js";
// นำ Multer middleware สำหรับรับรูป field image มาใช้ก่อนสร้าง Place
```

### 15.6 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/routes/placeRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/placeRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 15.6 ทำหน้าที่ `import { validate } from "../middlewares/validateMiddleware.js";` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
import { validate } from "../middlewares/validateMiddleware.js";
// ก้อนนี้นำ Module ที่จำเป็นเข้ามา เพื่อให้โค้ดส่วนถัดไปเรียกใช้ความสามารถเหล่านั้น
```

### 15.7 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/routes/placeRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/placeRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 15.7 ทำหน้าที่ `import {` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
import {
  createPlaceSchema,
  // เรียก Controller นี้เป็นขั้นตอนสุดท้ายเมื่อ Middleware ก่อนหน้าผ่านทั้งหมด
  nearbyQuerySchema,
  // ตรวจ latitude, longitude และ radius
  placeIdSchema,
  // ตรวจ :id จาก URL ให้เป็นจำนวนเต็มบวก
  placesQuerySchema,
  // ตรวจ type, search และ status ของหน้ารายการ
  placeStatusSchema,
  // ตรวจ status ใหม่ตอน Admin อัปเดต
} from "../validations/schemas.js";
// นำ Schema ของ Place มาใช้ตรวจ body, params และ query ก่อนเข้า Controller
```

### 15.8 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/routes/placeRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/placeRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 15.8 ทำหน้าที่ `const router = express.Router();` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
const router = express.Router();
// ก้อนนี้เตรียมข้อมูลและควบคุมลำดับการทำงานก่อนส่งผลไปยังส่วนถัดไปของระบบ
```

### 15.9 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/routes/placeRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/placeRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 15.9 ทำหน้าที่ `Route คำเฉพาะต้องอยู่ก่อน /:id ไม่เช่นนั้น Express จะคิดว่า nearby คือ id` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
router.get("/nearby", validate(nearbyQuerySchema, "query"), getNearbyPlaces);
// Route คำเฉพาะต้องอยู่ก่อน /:id ไม่เช่นนั้น Express จะคิดว่า nearby คือ id
```

### 15.10 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/routes/placeRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/placeRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 15.10 ทำหน้าที่ `router.get("/pending", authMiddleware, adminMiddleware, getPendingPlaces);` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
router.get("/pending", authMiddleware, adminMiddleware, getPendingPlaces);
// นำ Middleware ตรวจ role admin มาใช้ป้องกัน Route ฝั่งผู้ดูแล
```

### 15.11 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/routes/placeRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/placeRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 15.11 ทำหน้าที่ `router.get(` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
router.get(
  // เริ่มประกาศ GET Route สำหรับอ่านข้อมูล Place
  "/",
  // ใช้ path "/" เมื่อรวมกับ /api/places ใน server.js จะได้ GET /api/places
  optionalAuthMiddleware,
  // อ่าน Token แบบไม่บังคับ เพื่อให้คนทั่วไปดูรายการได้ และให้ Controller รู้เมื่อผู้ใช้เป็น Admin
  validate(placesQuerySchema, "query"),
  // ตรวจ query เช่น type, search และ status; ถ้าไม่ผ่าน Schema จะตอบ 400 ก่อนเข้า Controller
  getPlaces
  // ดึงรายการ Place จาก Database แล้วส่งผลลัพธ์กลับไปยังผู้เรียก API
);
```

### 15.12 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/routes/placeRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/placeRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 15.12 ทำหน้าที่ `router.get("/:id", validate(placeIdSchema, "params"), getPlaceById);` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
router.get("/:id", validate(placeIdSchema, "params"), getPlaceById);
// เริ่มประกาศ GET Route สำหรับอ่านข้อมูล Place
```

### 15.13 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/routes/placeRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/placeRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 15.13 ทำหน้าที่ `router.post(` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
router.post(
// ประกาศ Route นี้ เพื่อกำหนดลำดับตั้งแต่รับ URL ผ่าน Middleware จนถึง Controller
  "/",
  // กำหนด path "/" ซึ่งจะนำไปต่อกับ URL หลักที่ประกาศไว้ใน server.js
  authMiddleware,
  // ตรวจ Bearer Token; ถ้าไม่มีหรือไม่ถูกต้องจะตอบ 401 และหยุดก่อนขั้นถัดไป
  uploadPlaceImage,
  // ตรวจข้อมูลด้วย Zod Schema ที่ระบุ; ไม่ผ่านจะตอบ 400 ก่อน Controller
  validate(createPlaceSchema),
  // เรียก Controller นี้เป็นขั้นตอนสุดท้ายเมื่อ Middleware ก่อนหน้าผ่านทั้งหมด
  createPlace
  // เรียก createPlace เป็นขั้นตอนสุดท้าย เพื่อประมวลผลและส่ง Response กลับ
);
```

### 15.14 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/routes/placeRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/placeRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 15.14 ทำหน้าที่ `router.delete(` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
router.delete(
// เริ่มประกาศ DELETE Route สำหรับให้ Admin ลบ Place
// เช่น DELETE /api/places/12 โดย 12 อยู่ใน req.params.id
  "/:id",
  // ตรวจ Bearer Token; ไม่ผ่านจะตอบ 401 และไม่ทำขั้นถัดไป
  authMiddleware,
  // ตรวจ Bearer Token; ถ้าไม่มีหรือไม่ถูกต้องจะตอบ 401 และหยุดก่อนขั้นถัดไป
  adminMiddleware,
  // ตรวจข้อมูลด้วย Zod Schema ที่ระบุ; ไม่ผ่านจะตอบ 400 ก่อน Controller
  validate(placeIdSchema, "params"),
  // ตรวจข้อมูลด้วย Zod Schema ที่ระบุ; ถ้าไม่ผ่านจะตอบ 400 ก่อนเข้า Controller
  deletePlace
  // เรียก deletePlace เป็นขั้นตอนสุดท้าย เพื่อประมวลผลและส่ง Response กลับ
);
```

### 15.15 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/routes/placeRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/placeRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 15.15 ทำหน้าที่ `router.patch(` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
router.patch(
// ประกาศ Route นี้ เพื่อกำหนดลำดับตั้งแต่รับ URL ผ่าน Middleware จนถึง Controller
  "/:id/status",
  // ตรวจ Bearer Token; ไม่ผ่านจะตอบ 401 และไม่ทำขั้นถัดไป
  authMiddleware,
  // ตรวจ Bearer Token; ถ้าไม่มีหรือไม่ถูกต้องจะตอบ 401 และหยุดก่อนขั้นถัดไป
  adminMiddleware,
  // ตรวจข้อมูลด้วย Zod Schema ที่ระบุ; ไม่ผ่านจะตอบ 400 ก่อน Controller
  validate(placeIdSchema, "params"),
  // ตรวจข้อมูลด้วย Zod Schema ที่ระบุ; ถ้าไม่ผ่านจะตอบ 400 ก่อนเข้า Controller
  validate(placeStatusSchema),
  // เรียก Controller นี้เป็นขั้นตอนสุดท้ายเมื่อ Middleware ก่อนหน้าผ่านทั้งหมด
  updatePlaceStatus
  // เรียก updatePlaceStatus เป็นขั้นตอนสุดท้าย เพื่อประมวลผลและส่ง Response กลับ
);
```

### 15.16 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/routes/placeRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/placeRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 15.16 ทำหน้าที่ `export default router;` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
export default router;
// ส่ง Router ออกไปให้ server.js import และ mount ใช้งาน
```

เมื่อคัดครบไฟล์ ให้รันจาก `BackEnd`:

```powershell
node --check src/routes/placeRoutes.js
```

### CHECKPOINT 4 — RUN DEV: Place API

เพิ่ม import และ mount สองบรรทัดนี้ใน Server checkpoint โดย import ต้องอยู่ด้านบนและ `app.use` ต้องอยู่ก่อน `app.listen`:

```js
import placeRoutes from "./routes/placeRoutes.js";
// นำ Dependency หรือ Module ที่บรรทัดถัดไปต้องใช้เข้ามาในไฟล์
```

```js
app.use("/api/places", placeRoutes);
// ติดตั้ง Middleware หรือ Route ให้ Express ตามลำดับจากบนลงล่าง
```

รัน `npm run dev` แล้วทดสอบ:

```powershell
Invoke-RestMethod -Uri "http://localhost:5000/api/places"
```

ต้องได้ JSON รายการสถานที่ แม้รายการจะว่างก็ถือว่าผ่าน ถ้าได้ 404 หรือ HTML ให้ตรวจตำแหน่ง import/mount ก่อนทำหัวข้อ 16 แล้วกด `Ctrl+C` เมื่อทดสอบเสร็จ

## 16. `BackEnd/src/controllers/reportController.js`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนต่อไปนี้เรียงกัน

### 16.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `BackEnd/src/controllers/reportController.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/controllers/reportController.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 16.1 ทำหน้าที่ `import { prisma } from "../lib/prisma.js";` ซึ่งเป็นส่วน Business Logic ที่รับข้อมูลจาก Route/Middleware แล้วอ่านหรือแก้ไขข้อมูลผ่าน Prisma

**โค้ดก้อนนี้ทำงานอย่างไร:** Controller จะอ่านข้อมูลที่ผ่าน Validation และตัวตนจาก Request เรียก `prisma` เพื่อ query/create/update/delete ตามเงื่อนไข แล้วส่ง JSON และ HTTP status กลับ Frontend; กรณีผิดพลาดจะส่งต่อให้ Error handler

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อประกอบ Controller ครบ Endpoint ที่ Route เชื่อมไว้จะทำงานกับ Database จริงตามสิทธิ์และเงื่อนไขของระบบ

```js
import { prisma } from "../lib/prisma.js";
// ก้อนนี้ติดต่อ Database ผ่าน Prisma เพื่ออ่านหรือเปลี่ยนแปลงข้อมูลตามงานของระบบ
```

### 16.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/controllers/reportController.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/controllers/reportController.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 16.2 ทำหน้าที่ `import fs from "fs";` ซึ่งเป็นส่วน Business Logic ที่รับข้อมูลจาก Route/Middleware แล้วอ่านหรือแก้ไขข้อมูลผ่าน Prisma

**โค้ดก้อนนี้ทำงานอย่างไร:** Controller จะอ่านข้อมูลที่ผ่าน Validation และตัวตนจาก Request เรียก `prisma` เพื่อ query/create/update/delete ตามเงื่อนไข แล้วส่ง JSON และ HTTP status กลับ Frontend; กรณีผิดพลาดจะส่งต่อให้ Error handler

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อประกอบ Controller ครบ Endpoint ที่ Route เชื่อมไว้จะทำงานกับ Database จริงตามสิทธิ์และเงื่อนไขของระบบ

```js
import fs from "fs";
// ก้อนนี้นำ Module ที่จำเป็นเข้ามา เพื่อให้โค้ดส่วนถัดไปเรียกใช้ความสามารถเหล่านั้น
```

### 16.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/controllers/reportController.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/controllers/reportController.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 16.3 ทำหน้าที่ `import path from "path";` ซึ่งเป็นส่วน Business Logic ที่รับข้อมูลจาก Route/Middleware แล้วอ่านหรือแก้ไขข้อมูลผ่าน Prisma

**โค้ดก้อนนี้ทำงานอย่างไร:** Controller จะอ่านข้อมูลที่ผ่าน Validation และตัวตนจาก Request เรียก `prisma` เพื่อ query/create/update/delete ตามเงื่อนไข แล้วส่ง JSON และ HTTP status กลับ Frontend; กรณีผิดพลาดจะส่งต่อให้ Error handler

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อประกอบ Controller ครบ Endpoint ที่ Route เชื่อมไว้จะทำงานกับ Database จริงตามสิทธิ์และเงื่อนไขของระบบ

```js
import path from "path";
// ก้อนนี้นำ Module ที่จำเป็นเข้ามา เพื่อให้โค้ดส่วนถัดไปเรียกใช้ความสามารถเหล่านั้น
```

### 16.4 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/controllers/reportController.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/controllers/reportController.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 16.4 ทำหน้าที่ `const reportStatuses = ["pending", "resolved", "rejected"];` ซึ่งเป็นส่วน Business Logic ที่รับข้อมูลจาก Route/Middleware แล้วอ่านหรือแก้ไขข้อมูลผ่าน Prisma

**โค้ดก้อนนี้ทำงานอย่างไร:** Controller จะอ่านข้อมูลที่ผ่าน Validation และตัวตนจาก Request เรียก `prisma` เพื่อ query/create/update/delete ตามเงื่อนไข แล้วส่ง JSON และ HTTP status กลับ Frontend; กรณีผิดพลาดจะส่งต่อให้ Error handler

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อประกอบ Controller ครบ Endpoint ที่ Route เชื่อมไว้จะทำงานกับ Database จริงตามสิทธิ์และเงื่อนไขของระบบ

```js
const reportStatuses = ["pending", "resolved", "rejected"];
// ประกาศค่าที่ใช้ภายในขอบเขตนี้และไม่อนุญาตให้เปลี่ยนตัวแปรไปอ้างค่าใหม่
```

### 16.5 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/controllers/reportController.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/controllers/reportController.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 16.5 ทำหน้าที่ `POST /api/reports สำหรับ Member และ Admin ที่ Login แล้ว` ซึ่งเป็นส่วน Business Logic ที่รับข้อมูลจาก Route/Middleware แล้วอ่านหรือแก้ไขข้อมูลผ่าน Prisma

**โค้ดก้อนนี้ทำงานอย่างไร:** Controller จะอ่านข้อมูลที่ผ่าน Validation และตัวตนจาก Request เรียก `prisma` เพื่อ query/create/update/delete ตามเงื่อนไข แล้วส่ง JSON และ HTTP status กลับ Frontend; กรณีผิดพลาดจะส่งต่อให้ Error handler

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อประกอบ Controller ครบ Endpoint ที่ Route เชื่อมไว้จะทำงานกับ Database จริงตามสิทธิ์และเงื่อนไขของระบบ

```js
// POST /api/reports สำหรับ Member และ Admin ที่ Login แล้ว
export async function createReport(req, res) {
// POST /api/reports สำหรับ Member และ Admin ที่ Login แล้ว
  try {
    const { placeId, reason, description } = req.body;
    // ประกาศค่าที่ใช้ภายในขอบเขตนี้และไม่อนุญาตให้เปลี่ยนตัวแปรไปอ้างค่าใหม่
    const parsedPlaceId = Number(placeId);
    // ประกาศค่าที่ใช้ภายในขอบเขตนี้และไม่อนุญาตให้เปลี่ยนตัวแปรไปอ้างค่าใหม่

    if (!Number.isInteger(parsedPlaceId) || !reason) {
    // ตรวจเงื่อนไขก่อนอนุญาตให้โค้ดภายในทำงาน
      return res.status(400).json({ message: "กรุณาส่ง placeId และ reason" });
      // ส่ง HTTP status และ JSON กลับ Client พร้อมหยุด Controller
    }

    if (!req.file) {
      return res.status(400).json({ message: "กรุณาแนบรูปหลักฐาน" });
      // ส่ง HTTP status และ JSON กลับ Client พร้อมหยุด Controller
    }

    // ตรวจว่าสถานที่มีอยู่จริงก่อนสร้าง Report
    const place = await prisma.place.findUnique({
    // ตรวจว่าสถานที่มีอยู่จริงก่อนสร้าง Report
      where: { id: parsedPlaceId },
    });

    if (!place) {
      return res.status(404).json({ message: "ไม่พบสถานที่" });
    }

    // ไม่รับ status จากผู้ใช้ เพราะ Report ใหม่ต้องเริ่มเป็น pending เสมอ
    const report = await prisma.report.create({
    // ไม่รับ status จากผู้ใช้ เพราะ Report ใหม่ต้องเริ่มเป็น pending เสมอ
      data: {
      // กำหนดข้อมูลที่ Prisma จะสร้างหรืออัปเดตใน Database
        placeId: parsedPlaceId,
        reporterId: req.user.userId,
        reason,
        description: description || null,
        evidenceImageUrl: `/uploads/${req.file.filename}`,
        status: "pending",
      },
    });

    return res.status(201).json({ message: "ส่ง Report สำเร็จ", report });
    // ส่ง HTTP status และ JSON กลับ Client พร้อมหยุด Controller
  } catch (error) {
  // รับ Error ที่เกิดจาก Prisma หรือ Logic ภายใน try
    console.error(error);
    // พิมพ์ Error จริงใน Terminal เพื่อช่วยวิเคราะห์โดยไม่ส่งรายละเอียดลับให้ Client
    return res.status(500).json({ message: "เกิดข้อผิดพลาดจาก Server" });
  }
}
```

### 16.6 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/controllers/reportController.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/controllers/reportController.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 16.6 ทำหน้าที่ `GET /api/reports สำหรับหน้า Admin Reports` ซึ่งเป็นส่วน Business Logic ที่รับข้อมูลจาก Route/Middleware แล้วอ่านหรือแก้ไขข้อมูลผ่าน Prisma

**โค้ดก้อนนี้ทำงานอย่างไร:** Controller จะอ่านข้อมูลที่ผ่าน Validation และตัวตนจาก Request เรียก `prisma` เพื่อ query/create/update/delete ตามเงื่อนไข แล้วส่ง JSON และ HTTP status กลับ Frontend; กรณีผิดพลาดจะส่งต่อให้ Error handler

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อประกอบ Controller ครบ Endpoint ที่ Route เชื่อมไว้จะทำงานกับ Database จริงตามสิทธิ์และเงื่อนไขของระบบ

```js
// GET /api/reports สำหรับหน้า Admin Reports
export async function getReports(req, res) {
// GET /api/reports สำหรับหน้า Admin Reports
  try {
    const { status } = req.query;
    // ประกาศค่าที่ใช้ภายในขอบเขตนี้และไม่อนุญาตให้เปลี่ยนตัวแปรไปอ้างค่าใหม่

    if (status && !reportStatuses.includes(status)) {
    // ตรวจเงื่อนไขก่อนอนุญาตให้โค้ดภายในทำงาน
      return res.status(400).json({ message: "status ไม่ถูกต้อง" });
    }

    const reports = await prisma.report.findMany({
    // รอ Promise นี้ทำงานเสร็จก่อนใช้ผลลัพธ์หรือทำบรรทัดถัดไป
      where: status ? { status } : {},
      include: {
      // ขอข้อมูล Relation ที่เกี่ยวข้องกลับมาพร้อม Place
        place: true,
        reporter: { select: { id: true, name: true, email: true } },
      },
      orderBy: { createdAt: "desc" },
      // กำหนดลำดับผลลัพธ์จาก Database ตามเวลาสร้าง
    });

    return res.json({ reports });
    // ส่งข้อมูล JSON กลับ Postman หรือ Frontend
  } catch (error) {
  // รับ Error ที่เกิดจาก Prisma หรือ Logic ภายใน try
    console.error(error);
    // พิมพ์ Error จริงใน Terminal เพื่อช่วยวิเคราะห์โดยไม่ส่งรายละเอียดลับให้ Client
    return res.status(500).json({ message: "เกิดข้อผิดพลาดจาก Server" });
  }
}
```

### 16.7 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/controllers/reportController.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/controllers/reportController.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 16.7 ทำหน้าที่ `GET /api/reports/:id สำหรับหน้า Admin Report Detail` ซึ่งเป็นส่วน Business Logic ที่รับข้อมูลจาก Route/Middleware แล้วอ่านหรือแก้ไขข้อมูลผ่าน Prisma

**โค้ดก้อนนี้ทำงานอย่างไร:** Controller จะอ่านข้อมูลที่ผ่าน Validation และตัวตนจาก Request เรียก `prisma` เพื่อ query/create/update/delete ตามเงื่อนไข แล้วส่ง JSON และ HTTP status กลับ Frontend; กรณีผิดพลาดจะส่งต่อให้ Error handler

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อประกอบ Controller ครบ Endpoint ที่ Route เชื่อมไว้จะทำงานกับ Database จริงตามสิทธิ์และเงื่อนไขของระบบ

```js
// GET /api/reports/:id สำหรับหน้า Admin Report Detail
export async function getReportById(req, res) {
// GET /api/reports/:id สำหรับหน้า Admin Report Detail
  try {
    const id = Number(req.params.id);

    if (!Number.isInteger(id)) {
      return res.status(400).json({ message: "Report id ไม่ถูกต้อง" });
      // ส่ง HTTP status และ JSON กลับ Client พร้อมหยุด Controller
    }

    const report = await prisma.report.findUnique({
    // รอ Promise นี้ทำงานเสร็จก่อนใช้ผลลัพธ์หรือทำบรรทัดถัดไป
      where: { id },
      // กำหนดเงื่อนไขที่ Prisma ใช้ค้นหาแถวใน Database
      include: {
      // ขอข้อมูล Relation ที่เกี่ยวข้องกลับมาพร้อม Place
        place: true,
        reporter: { select: { id: true, name: true, email: true } },
      },
    });

    if (!report) {
    // ตรวจเงื่อนไขก่อนอนุญาตให้โค้ดภายในทำงาน
      return res.status(404).json({ message: "ไม่พบ Report" });
      // ส่ง HTTP status และ JSON กลับ Client พร้อมหยุด Controller
    }

    return res.json({ report });
    // ส่งข้อมูล JSON กลับ Postman หรือ Frontend
  } catch (error) {
  // รับ Error ที่เกิดจาก Prisma หรือ Logic ภายใน try
    console.error(error);
    // พิมพ์ Error จริงใน Terminal เพื่อช่วยวิเคราะห์โดยไม่ส่งรายละเอียดลับให้ Client
    return res.status(500).json({ message: "เกิดข้อผิดพลาดจาก Server" });
  }
}
```

### 16.8 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/controllers/reportController.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/controllers/reportController.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 16.8 ทำหน้าที่ `PATCH /api/reports/:id/status สำหรับ Admin จบหรือปฏิเสธ Report` ซึ่งเป็นส่วน Business Logic ที่รับข้อมูลจาก Route/Middleware แล้วอ่านหรือแก้ไขข้อมูลผ่าน Prisma

**โค้ดก้อนนี้ทำงานอย่างไร:** Controller จะอ่านข้อมูลที่ผ่าน Validation และตัวตนจาก Request เรียก `prisma` เพื่อ query/create/update/delete ตามเงื่อนไข แล้วส่ง JSON และ HTTP status กลับ Frontend; กรณีผิดพลาดจะส่งต่อให้ Error handler

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อประกอบ Controller ครบ Endpoint ที่ Route เชื่อมไว้จะทำงานกับ Database จริงตามสิทธิ์และเงื่อนไขของระบบ

```js
// PATCH /api/reports/:id/status สำหรับ Admin จบหรือปฏิเสธ Report
export async function updateReportStatus(req, res) {
// PATCH /api/reports/:id/status สำหรับ Admin จบหรือปฏิเสธ Report
  try {
    const id = Number(req.params.id);
    const { status } = req.body;

    if (!Number.isInteger(id) || !["resolved", "rejected"].includes(status)) {
    // ตรวจเงื่อนไขก่อนอนุญาตให้โค้ดภายในทำงาน
      return res.status(400).json({ message: "status ต้องเป็น resolved หรือ rejected" });
      // ส่ง HTTP status และ JSON กลับ Client พร้อมหยุด Controller
    }

    const existingReport = await prisma.report.findUnique({ where: { id } });
    // รอ Promise นี้ทำงานเสร็จก่อนใช้ผลลัพธ์หรือทำบรรทัดถัดไป

    if (!existingReport) {
    // ตรวจเงื่อนไขก่อนอนุญาตให้โค้ดภายในทำงาน
      return res.status(404).json({ message: "ไม่พบ Report" });
      // ส่ง HTTP status และ JSON กลับ Client พร้อมหยุด Controller
    }

    const report = await prisma.report.update({
    // รอ Promise นี้ทำงานเสร็จก่อนใช้ผลลัพธ์หรือทำบรรทัดถัดไป
      where: { id },
      // กำหนดเงื่อนไขที่ Prisma ใช้ค้นหาแถวใน Database
      data: { status },
      // กำหนดข้อมูลที่ Prisma จะสร้างหรืออัปเดตใน Database
    });

    // รูปใช้ยืนยันก่อนจัดการเท่านั้น จึงลบไฟล์เมื่อ Report จบแล้ว
    if (existingReport.evidenceImageUrl) {
    // รูปใช้ยืนยันก่อนจัดการเท่านั้น จึงลบไฟล์เมื่อ Report จบแล้ว
      const evidencePath = path.resolve(existingReport.evidenceImageUrl.replace(/^\//, ""));
      // ประกาศค่าที่ใช้ภายในขอบเขตนี้และไม่อนุญาตให้เปลี่ยนตัวแปรไปอ้างค่าใหม่
      if (fs.existsSync(evidencePath)) fs.unlinkSync(evidencePath);
      await prisma.report.update({ where: { id }, data: { evidenceImageUrl: null } });
      // รอ Promise นี้ทำงานเสร็จก่อนใช้ผลลัพธ์หรือทำบรรทัดถัดไป
    }

    return res.json({ message: "เปลี่ยนสถานะ Report สำเร็จ", report });
    // ส่งข้อมูล JSON กลับ Postman หรือ Frontend
  } catch (error) {
  // รับ Error ที่เกิดจาก Prisma หรือ Logic ภายใน try
    console.error(error);
    // พิมพ์ Error จริงใน Terminal เพื่อช่วยวิเคราะห์โดยไม่ส่งรายละเอียดลับให้ Client
    return res.status(500).json({ message: "เกิดข้อผิดพลาดจาก Server" });
  }
}
```

เมื่อคัดครบไฟล์ ให้รันจาก `BackEnd`:

```powershell
node --check src/controllers/reportController.js
```

## 17. `BackEnd/src/routes/reportRoutes.js`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนต่อไปนี้เรียงกัน

### 17.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `BackEnd/src/routes/reportRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/reportRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 17.1 ทำหน้าที่ `import express from "express";` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
import express from "express";
// ก้อนนี้นำ Module ที่จำเป็นเข้ามา เพื่อให้โค้ดส่วนถัดไปเรียกใช้ความสามารถเหล่านั้น
```

### 17.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/routes/reportRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/reportRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 17.2 ทำหน้าที่ `import {` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
import {
// ก้อนนี้นำ Module ที่จำเป็นเข้ามา เพื่อให้โค้ดส่วนถัดไปเรียกใช้ความสามารถเหล่านั้น
  createReport,
  getReportById,
  getReports,
  updateReportStatus,
} from "../controllers/reportController.js";
```

### 17.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/routes/reportRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/reportRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 17.3 ทำหน้าที่ `import { authMiddleware } from "../middlewares/authMiddleware.js";` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
import { authMiddleware } from "../middlewares/authMiddleware.js";
// นำ Dependency หรือ Module ที่บรรทัดถัดไปต้องใช้เข้ามาในไฟล์
```

### 17.4 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/routes/reportRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/reportRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 17.4 ทำหน้าที่ `import { adminMiddleware } from "../middlewares/adminMiddleware.js";` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
import { adminMiddleware } from "../middlewares/adminMiddleware.js";
// ก้อนนี้นำ Module ที่จำเป็นเข้ามา เพื่อให้โค้ดส่วนถัดไปเรียกใช้ความสามารถเหล่านั้น
```

### 17.5 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/routes/reportRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/reportRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 17.5 ทำหน้าที่ `import { uploadReportEvidence } from "../middlewares/uploadMiddleware.js";` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
import { uploadReportEvidence } from "../middlewares/uploadMiddleware.js";
// นำ Dependency หรือ Module ที่บรรทัดถัดไปต้องใช้เข้ามาในไฟล์
```

### 17.6 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/routes/reportRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/reportRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 17.6 ทำหน้าที่ `import { validate } from "../middlewares/validateMiddleware.js";` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
import { validate } from "../middlewares/validateMiddleware.js";
// ก้อนนี้นำ Module ที่จำเป็นเข้ามา เพื่อให้โค้ดส่วนถัดไปเรียกใช้ความสามารถเหล่านั้น
```

### 17.7 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/routes/reportRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/reportRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 17.7 ทำหน้าที่ `import {` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
import {
  createReportSchema,
  reportIdSchema,
  reportsQuerySchema,
  reportStatusSchema,
} from "../validations/schemas.js";
// นำ Schema ของ Place มาใช้ตรวจ body, params และ query ก่อนเข้า Controller
```

### 17.8 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/routes/reportRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/reportRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 17.8 ทำหน้าที่ `const router = express.Router();` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
const router = express.Router();
// ก้อนนี้เตรียมข้อมูลและควบคุมลำดับการทำงานก่อนส่งผลไปยังส่วนถัดไปของระบบ
```

### 17.9 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/routes/reportRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/reportRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 17.9 ทำหน้าที่ `Member และ Admin ส่ง Report ได้ แต่ Report ใหม่จะเป็น pending เสมอ` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
router.post(
  // Member และ Admin ส่ง Report ได้ แต่ Report ใหม่จะเป็น pending เสมอ
  "/",
  // กำหนด path "/" ซึ่งจะนำไปต่อกับ URL หลักที่ประกาศไว้ใน server.js
  authMiddleware,
  // ตรวจ Bearer Token; ถ้าไม่มีหรือไม่ถูกต้องจะตอบ 401 และหยุดก่อนขั้นถัดไป
  uploadReportEvidence,
  // รับไฟล์จาก multipart/form-data แล้วบันทึกข้อมูลไฟล์ไว้ให้ Controller ใช้งาน
  validate(createReportSchema),
  // ตรวจข้อมูลด้วย Zod Schema ที่ระบุ; ถ้าไม่ผ่านจะตอบ 400 ก่อนเข้า Controller
  createReport
  // เรียก createReport เป็นขั้นตอนสุดท้าย เพื่อประมวลผลและส่ง Response กลับ
);
```

### 17.10 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/routes/reportRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/reportRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 17.10 ทำหน้าที่ `การอ่านและจัดการ Report เป็นสิทธิ์ของ Admin` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
router.get(
  // การอ่านและจัดการ Report เป็นสิทธิ์ของ Admin
  "/",
  // กำหนด path "/" ซึ่งจะนำไปต่อกับ URL หลักที่ประกาศไว้ใน server.js
  authMiddleware,
  // ตรวจ Bearer Token; ถ้าไม่มีหรือไม่ถูกต้องจะตอบ 401 และหยุดก่อนขั้นถัดไป
  adminMiddleware,
  // ตรวจข้อมูลด้วย Zod Schema ที่ระบุ; ไม่ผ่านจะตอบ 400 ก่อน Controller
  validate(reportsQuerySchema, "query"),
  // ตรวจข้อมูลด้วย Zod Schema ที่ระบุ; ถ้าไม่ผ่านจะตอบ 400 ก่อนเข้า Controller
  getReports
  // เรียก getReports เป็นขั้นตอนสุดท้าย เพื่อประมวลผลและส่ง Response กลับ
);
```

### 17.11 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/routes/reportRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/reportRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 17.11 ทำหน้าที่ `router.get(` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
router.get(
// ประกาศ Route นี้ เพื่อกำหนดลำดับตั้งแต่รับ URL ผ่าน Middleware จนถึง Controller
  "/:id",
  // ตรวจ Bearer Token; ไม่ผ่านจะตอบ 401 และไม่ทำขั้นถัดไป
  authMiddleware,
  // ตรวจ Bearer Token; ถ้าไม่มีหรือไม่ถูกต้องจะตอบ 401 และหยุดก่อนขั้นถัดไป
  adminMiddleware,
  // ตรวจข้อมูลด้วย Zod Schema ที่ระบุ; ไม่ผ่านจะตอบ 400 ก่อน Controller
  validate(reportIdSchema, "params"),
  // ตรวจข้อมูลด้วย Zod Schema ที่ระบุ; ถ้าไม่ผ่านจะตอบ 400 ก่อนเข้า Controller
  getReportById
  // เรียก getReportById เป็นขั้นตอนสุดท้าย เพื่อประมวลผลและส่ง Response กลับ
);
```

### 17.12 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/routes/reportRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/reportRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 17.12 ทำหน้าที่ `router.patch(` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
router.patch(
// ประกาศ Route นี้ เพื่อกำหนดลำดับตั้งแต่รับ URL ผ่าน Middleware จนถึง Controller
  "/:id/status",
  // ตรวจ Bearer Token; ไม่ผ่านจะตอบ 401 และไม่ทำขั้นถัดไป
  authMiddleware,
  // ตรวจ Bearer Token; ถ้าไม่มีหรือไม่ถูกต้องจะตอบ 401 และหยุดก่อนขั้นถัดไป
  adminMiddleware,
  // ตรวจข้อมูลด้วย Zod Schema ที่ระบุ; ไม่ผ่านจะตอบ 400 ก่อน Controller
  validate(reportIdSchema, "params"),
  // ตรวจข้อมูลด้วย Zod Schema ที่ระบุ; ถ้าไม่ผ่านจะตอบ 400 ก่อนเข้า Controller
  validate(reportStatusSchema),
  // ตรวจข้อมูลด้วย Zod Schema ที่ระบุ; ถ้าไม่ผ่านจะตอบ 400 ก่อนเข้า Controller
  updateReportStatus
  // เรียก updateReportStatus เป็นขั้นตอนสุดท้าย เพื่อประมวลผลและส่ง Response กลับ
);
```

### 17.13 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/routes/reportRoutes.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/routes/reportRoutes.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 17.13 ทำหน้าที่ `export default router;` เพื่อประกาศ Endpoint และจัดลำดับ Middleware ก่อนเรียก Controller

**โค้ดก้อนนี้ทำงานอย่างไร:** Express Router จับคู่ HTTP method และ URL แล้วทำงานจากซ้ายไปขวา เช่น upload → auth → admin → validate → controller ดังนั้นลำดับที่วางมีผลต่อความปลอดภัยและข้อมูลที่ Controller จะได้รับ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Router นี้ถูก mount ใน `server.js` Frontend จะเรียก URL กลุ่มนี้ได้ และ Request จะผ่านตัวตรวจทุกชั้นก่อนถึง Business Logic

```js
export default router;
// ส่ง Router ออกไปให้ server.js import และ mount ใช้งาน
```

เมื่อคัดครบไฟล์ ให้รันจาก `BackEnd`:

```powershell
node --check src/routes/reportRoutes.js
```

### CHECKPOINT 5 — RUN DEV: Report API

เพิ่ม Report Route ใน Server checkpoint:

```js
import reportRoutes from "./routes/reportRoutes.js";
// นำ Dependency หรือ Module ที่บรรทัดถัดไปต้องใช้เข้ามาในไฟล์
```

```js
app.use("/api/reports", reportRoutes);
// ติดตั้ง Middleware หรือ Route ให้ Express ตามลำดับจากบนลงล่าง
```

รัน `npm run dev` แล้วทดสอบ Route โดยยังไม่ส่ง Token:

```powershell
try { Invoke-RestMethod -Uri "http://localhost:5000/api/reports" } catch { $_.ErrorDetails.Message }
```

ต้องได้ JSON แจ้งว่าต้องเข้าสู่ระบบหรือ Token ไม่ถูกต้อง และต้องไม่ใช่ 404/HTML ผลนี้ยืนยันว่า Report Route กับ Auth Middleware ถูก mount แล้ว จากนั้นกด `Ctrl+C` และไปประกอบ Server ฉบับเต็มในหัวข้อ 18

## 18. `BackEnd/src/server.js`

ลบก้อน Server ชั่วคราวจาก B1 แล้วประกอบฉบับเต็มด้านล่างตามหมายเลข เพื่อไม่ให้มี app หรือ app.listen ซ้ำ

### 18.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `BackEnd/src/server.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/server.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 18.1 ทำหน้าที่ `import "dotenv/config";` เพื่อประกอบ Express Application และเชื่อม Config, Middleware, Static files, Routes และ Error handler เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Server จะโหลด Environment ตั้งค่า Middleware ตามลำดับ mount Route ภายใต้ URL ที่กำหนด และเปิดฟัง PORT; Request ทุกครั้งจะไหลผ่านลำดับนี้ก่อนส่ง Response

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อก้อน `listen` และ Route ครบ `npm run dev` จะเปิด Backend ที่พอร์ต 5000 และ Endpoint ที่ประกอบแล้วจะเรียกใช้งานได้

```js
import "dotenv/config";
// ก้อนนี้นำ Module ที่จำเป็นเข้ามา เพื่อให้โค้ดส่วนถัดไปเรียกใช้ความสามารถเหล่านั้น
```

### 18.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/server.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/server.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 18.2 ทำหน้าที่ `import express from "express";` เพื่อประกอบ Express Application และเชื่อม Config, Middleware, Static files, Routes และ Error handler เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Server จะโหลด Environment ตั้งค่า Middleware ตามลำดับ mount Route ภายใต้ URL ที่กำหนด และเปิดฟัง PORT; Request ทุกครั้งจะไหลผ่านลำดับนี้ก่อนส่ง Response

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อก้อน `listen` และ Route ครบ `npm run dev` จะเปิด Backend ที่พอร์ต 5000 และ Endpoint ที่ประกอบแล้วจะเรียกใช้งานได้

```js
import express from "express";
// ก้อนนี้นำ Module ที่จำเป็นเข้ามา เพื่อให้โค้ดส่วนถัดไปเรียกใช้ความสามารถเหล่านั้น
```

### 18.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/server.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/server.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 18.3 ทำหน้าที่ `import path from "path";` เพื่อประกอบ Express Application และเชื่อม Config, Middleware, Static files, Routes และ Error handler เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Server จะโหลด Environment ตั้งค่า Middleware ตามลำดับ mount Route ภายใต้ URL ที่กำหนด และเปิดฟัง PORT; Request ทุกครั้งจะไหลผ่านลำดับนี้ก่อนส่ง Response

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อก้อน `listen` และ Route ครบ `npm run dev` จะเปิด Backend ที่พอร์ต 5000 และ Endpoint ที่ประกอบแล้วจะเรียกใช้งานได้

```js
import path from "path";
// ก้อนนี้นำ Module ที่จำเป็นเข้ามา เพื่อให้โค้ดส่วนถัดไปเรียกใช้ความสามารถเหล่านั้น
```

### 18.4 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/server.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/server.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 18.4 ทำหน้าที่ `import { fileURLToPath } from "url";` เพื่อประกอบ Express Application และเชื่อม Config, Middleware, Static files, Routes และ Error handler เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Server จะโหลด Environment ตั้งค่า Middleware ตามลำดับ mount Route ภายใต้ URL ที่กำหนด และเปิดฟัง PORT; Request ทุกครั้งจะไหลผ่านลำดับนี้ก่อนส่ง Response

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อก้อน `listen` และ Route ครบ `npm run dev` จะเปิด Backend ที่พอร์ต 5000 และ Endpoint ที่ประกอบแล้วจะเรียกใช้งานได้

```js
import { fileURLToPath } from "url";
// ก้อนนี้นำ Module ที่จำเป็นเข้ามา เพื่อให้โค้ดส่วนถัดไปเรียกใช้ความสามารถเหล่านั้น
```

### 18.5 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/server.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/server.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 18.5 ทำหน้าที่ `import authRoutes from "./routes/authRoutes.js";` เพื่อประกอบ Express Application และเชื่อม Config, Middleware, Static files, Routes และ Error handler เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Server จะโหลด Environment ตั้งค่า Middleware ตามลำดับ mount Route ภายใต้ URL ที่กำหนด และเปิดฟัง PORT; Request ทุกครั้งจะไหลผ่านลำดับนี้ก่อนส่ง Response

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อก้อน `listen` และ Route ครบ `npm run dev` จะเปิด Backend ที่พอร์ต 5000 และ Endpoint ที่ประกอบแล้วจะเรียกใช้งานได้

```js
import authRoutes from "./routes/authRoutes.js";
// นำ Dependency หรือ Module ที่บรรทัดถัดไปต้องใช้เข้ามาในไฟล์
```

### 18.6 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/server.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/server.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 18.6 ทำหน้าที่ `import placeRoutes from "./routes/placeRoutes.js";` เพื่อประกอบ Express Application และเชื่อม Config, Middleware, Static files, Routes และ Error handler เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Server จะโหลด Environment ตั้งค่า Middleware ตามลำดับ mount Route ภายใต้ URL ที่กำหนด และเปิดฟัง PORT; Request ทุกครั้งจะไหลผ่านลำดับนี้ก่อนส่ง Response

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อก้อน `listen` และ Route ครบ `npm run dev` จะเปิด Backend ที่พอร์ต 5000 และ Endpoint ที่ประกอบแล้วจะเรียกใช้งานได้

```js
import placeRoutes from "./routes/placeRoutes.js";
// นำ Dependency หรือ Module ที่บรรทัดถัดไปต้องใช้เข้ามาในไฟล์
```

### 18.7 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/server.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/server.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 18.7 ทำหน้าที่ `import reportRoutes from "./routes/reportRoutes.js";` เพื่อประกอบ Express Application และเชื่อม Config, Middleware, Static files, Routes และ Error handler เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Server จะโหลด Environment ตั้งค่า Middleware ตามลำดับ mount Route ภายใต้ URL ที่กำหนด และเปิดฟัง PORT; Request ทุกครั้งจะไหลผ่านลำดับนี้ก่อนส่ง Response

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อก้อน `listen` และ Route ครบ `npm run dev` จะเปิด Backend ที่พอร์ต 5000 และ Endpoint ที่ประกอบแล้วจะเรียกใช้งานได้

```js
import reportRoutes from "./routes/reportRoutes.js";
// นำ Dependency หรือ Module ที่บรรทัดถัดไปต้องใช้เข้ามาในไฟล์
```

### 18.8 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/server.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/server.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 18.8 ทำหน้าที่ `import userRoutes from "./routes/userRoutes.js";` เพื่อประกอบ Express Application และเชื่อม Config, Middleware, Static files, Routes และ Error handler เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Server จะโหลด Environment ตั้งค่า Middleware ตามลำดับ mount Route ภายใต้ URL ที่กำหนด และเปิดฟัง PORT; Request ทุกครั้งจะไหลผ่านลำดับนี้ก่อนส่ง Response

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อก้อน `listen` และ Route ครบ `npm run dev` จะเปิด Backend ที่พอร์ต 5000 และ Endpoint ที่ประกอบแล้วจะเรียกใช้งานได้

```js
import userRoutes from "./routes/userRoutes.js";
// นำ Dependency หรือ Module ที่บรรทัดถัดไปต้องใช้เข้ามาในไฟล์
```

### 18.9 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/server.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/server.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 18.9 ทำหน้าที่ `const app = express();` เพื่อประกอบ Express Application และเชื่อม Config, Middleware, Static files, Routes และ Error handler เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Server จะโหลด Environment ตั้งค่า Middleware ตามลำดับ mount Route ภายใต้ URL ที่กำหนด และเปิดฟัง PORT; Request ทุกครั้งจะไหลผ่านลำดับนี้ก่อนส่ง Response

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อก้อน `listen` และ Route ครบ `npm run dev` จะเปิด Backend ที่พอร์ต 5000 และ Endpoint ที่ประกอบแล้วจะเรียกใช้งานได้

```js
const app = express();
// app คือ Express application ที่รับ request จาก Frontend/Postman
```

### 18.10 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/server.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/server.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 18.10 ทำหน้าที่ `const PORT = Number(process.env.PORT || 5000);` เพื่อประกอบ Express Application และเชื่อม Config, Middleware, Static files, Routes และ Error handler เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Server จะโหลด Environment ตั้งค่า Middleware ตามลำดับ mount Route ภายใต้ URL ที่กำหนด และเปิดฟัง PORT; Request ทุกครั้งจะไหลผ่านลำดับนี้ก่อนส่ง Response

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อก้อน `listen` และ Route ครบ `npm run dev` จะเปิด Backend ที่พอร์ต 5000 และ Endpoint ที่ประกอบแล้วจะเรียกใช้งานได้

```js
const PORT = Number(process.env.PORT || 5000);
// ก้อนนี้เตรียมข้อมูลและควบคุมลำดับการทำงานก่อนส่งผลไปยังส่วนถัดไปของระบบ
```

### 18.11 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/server.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/server.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 18.11 ทำหน้าที่ `const __filename = fileURLToPath(import.meta.url);` เพื่อประกอบ Express Application และเชื่อม Config, Middleware, Static files, Routes และ Error handler เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Server จะโหลด Environment ตั้งค่า Middleware ตามลำดับ mount Route ภายใต้ URL ที่กำหนด และเปิดฟัง PORT; Request ทุกครั้งจะไหลผ่านลำดับนี้ก่อนส่ง Response

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อก้อน `listen` และ Route ครบ `npm run dev` จะเปิด Backend ที่พอร์ต 5000 และ Endpoint ที่ประกอบแล้วจะเรียกใช้งานได้

```js
const __filename = fileURLToPath(import.meta.url);
// ก้อนนี้เตรียมข้อมูลและควบคุมลำดับการทำงานก่อนส่งผลไปยังส่วนถัดไปของระบบ
```

### 18.12 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/server.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/server.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 18.12 ทำหน้าที่ `const __dirname = path.dirname(__filename);` เพื่อประกอบ Express Application และเชื่อม Config, Middleware, Static files, Routes และ Error handler เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Server จะโหลด Environment ตั้งค่า Middleware ตามลำดับ mount Route ภายใต้ URL ที่กำหนด และเปิดฟัง PORT; Request ทุกครั้งจะไหลผ่านลำดับนี้ก่อนส่ง Response

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อก้อน `listen` และ Route ครบ `npm run dev` จะเปิด Backend ที่พอร์ต 5000 และ Endpoint ที่ประกอบแล้วจะเรียกใช้งานได้

```js
const __dirname = path.dirname(__filename);
// ก้อนนี้เตรียมข้อมูลและควบคุมลำดับการทำงานก่อนส่งผลไปยังส่วนถัดไปของระบบ
```

### 18.13 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/server.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/server.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 18.13 ทำหน้าที่ `const backendRoot = path.resolve(__dirname, "..");` เพื่อประกอบ Express Application และเชื่อม Config, Middleware, Static files, Routes และ Error handler เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Server จะโหลด Environment ตั้งค่า Middleware ตามลำดับ mount Route ภายใต้ URL ที่กำหนด และเปิดฟัง PORT; Request ทุกครั้งจะไหลผ่านลำดับนี้ก่อนส่ง Response

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อก้อน `listen` และ Route ครบ `npm run dev` จะเปิด Backend ที่พอร์ต 5000 และ Endpoint ที่ประกอบแล้วจะเรียกใช้งานได้

```js
const backendRoot = path.resolve(__dirname, "..");
// ก้อนนี้เตรียมข้อมูลและควบคุมลำดับการทำงานก่อนส่งผลไปยังส่วนถัดไปของระบบ
```

### 18.14 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/server.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/server.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 18.14 ทำหน้าที่ `const frontendDist = path.resolve(backendRoot, "../FrontEnd/dist");` เพื่อประกอบ Express Application และเชื่อม Config, Middleware, Static files, Routes และ Error handler เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Server จะโหลด Environment ตั้งค่า Middleware ตามลำดับ mount Route ภายใต้ URL ที่กำหนด และเปิดฟัง PORT; Request ทุกครั้งจะไหลผ่านลำดับนี้ก่อนส่ง Response

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อก้อน `listen` และ Route ครบ `npm run dev` จะเปิด Backend ที่พอร์ต 5000 และ Endpoint ที่ประกอบแล้วจะเรียกใช้งานได้

```js
const frontendDist = path.resolve(backendRoot, "../FrontEnd/dist");
// ก้อนนี้เตรียมข้อมูลและควบคุมลำดับการทำงานก่อนส่งผลไปยังส่วนถัดไปของระบบ
```

### 18.15 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/server.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/server.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 18.15 ทำหน้าที่ `app.use((req, res, next) => {` เพื่อประกอบ Express Application และเชื่อม Config, Middleware, Static files, Routes และ Error handler เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Server จะโหลด Environment ตั้งค่า Middleware ตามลำดับ mount Route ภายใต้ URL ที่กำหนด และเปิดฟัง PORT; Request ทุกครั้งจะไหลผ่านลำดับนี้ก่อนส่ง Response

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อก้อน `listen` และ Route ครบ `npm run dev` จะเปิด Backend ที่พอร์ต 5000 และ Endpoint ที่ประกอบแล้วจะเรียกใช้งานได้

```js
app.use((req, res, next) => {
// อนุญาตให้ React Frontend คนละ Port เรียก Backend ได้
  const allowedOrigin = process.env.FRONTEND_URL || "http://localhost:5173";
  res.header("Access-Control-Allow-Origin", allowedOrigin);
  res.header("Access-Control-Allow-Headers", "Content-Type, Authorization");
  res.header("Access-Control-Allow-Methods", "GET, POST, PATCH, DELETE, OPTIONS");
  if (req.method === "OPTIONS") return res.sendStatus(204);
  return next();
  // ส่งผลลัพธ์ออกจากฟังก์ชันและหยุดทำบรรทัดถัดไปในฟังก์ชันนี้
});
```

### 18.16 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/server.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/server.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 18.16 ทำหน้าที่ `app.use(express.json());` เพื่อประกอบ Express Application และเชื่อม Config, Middleware, Static files, Routes และ Error handler เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Server จะโหลด Environment ตั้งค่า Middleware ตามลำดับ mount Route ภายใต้ URL ที่กำหนด และเปิดฟัง PORT; Request ทุกครั้งจะไหลผ่านลำดับนี้ก่อนส่ง Response

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อก้อน `listen` และ Route ครบ `npm run dev` จะเปิด Backend ที่พอร์ต 5000 และ Endpoint ที่ประกอบแล้วจะเรียกใช้งานได้

```js
app.use(express.json());
// อ่าน JSON Body ก่อนส่ง request ต่อไปยัง Routes
```

### 18.17 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/server.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/server.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 18.17 ทำหน้าที่ `app.use("/uploads", express.static(path.join(backendRoot, "uploads")));` เพื่อประกอบ Express Application และเชื่อม Config, Middleware, Static files, Routes และ Error handler เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Server จะโหลด Environment ตั้งค่า Middleware ตามลำดับ mount Route ภายใต้ URL ที่กำหนด และเปิดฟัง PORT; Request ทุกครั้งจะไหลผ่านลำดับนี้ก่อนส่ง Response

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อก้อน `listen` และ Route ครบ `npm run dev` จะเปิด Backend ที่พอร์ต 5000 และ Endpoint ที่ประกอบแล้วจะเรียกใช้งานได้

```js
app.use("/uploads", express.static(path.join(backendRoot, "uploads")));
// ก้อนนี้ต่อโค้ดส่วน app.use("/uploads", express.static(path.join(backendRoot, "uploads"))); ให้ทำงานร่วมกับส่วนก่อนหน้าและส่วนถัดไป
```

### 18.18 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/server.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/server.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 18.18 ทำหน้าที่ `app.get("/", (req, res) => {` เพื่อประกอบ Express Application และเชื่อม Config, Middleware, Static files, Routes และ Error handler เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Server จะโหลด Environment ตั้งค่า Middleware ตามลำดับ mount Route ภายใต้ URL ที่กำหนด และเปิดฟัง PORT; Request ทุกครั้งจะไหลผ่านลำดับนี้ก่อนส่ง Response

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อก้อน `listen` และ Route ครบ `npm run dev` จะเปิด Backend ที่พอร์ต 5000 และ Endpoint ที่ประกอบแล้วจะเรียกใช้งานได้

```js
app.get("/", (req, res) => {
// Route ง่าย ๆ สำหรับตรวจว่า Backend เปิดอยู่หรือไม่
  res.json({ status: "ok", message: "Trash & Toilet Japan API" });
});
```

### 18.19 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/server.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/server.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 18.19 ทำหน้าที่ `app.use("/api/auth", authRoutes);` เพื่อประกอบ Express Application และเชื่อม Config, Middleware, Static files, Routes และ Error handler เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Server จะโหลด Environment ตั้งค่า Middleware ตามลำดับ mount Route ภายใต้ URL ที่กำหนด และเปิดฟัง PORT; Request ทุกครั้งจะไหลผ่านลำดับนี้ก่อนส่ง Response

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อก้อน `listen` และ Route ครบ `npm run dev` จะเปิด Backend ที่พอร์ต 5000 และ Endpoint ที่ประกอบแล้วจะเรียกใช้งานได้

```js
app.use("/api/auth", authRoutes);
// เชื่อม URL แต่ละกลุ่มเข้ากับไฟล์ Route ของตัวเอง
```

### 18.20 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/server.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/server.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 18.20 ทำหน้าที่ `app.use("/api/users", userRoutes);` เพื่อประกอบ Express Application และเชื่อม Config, Middleware, Static files, Routes และ Error handler เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Server จะโหลด Environment ตั้งค่า Middleware ตามลำดับ mount Route ภายใต้ URL ที่กำหนด และเปิดฟัง PORT; Request ทุกครั้งจะไหลผ่านลำดับนี้ก่อนส่ง Response

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อก้อน `listen` และ Route ครบ `npm run dev` จะเปิด Backend ที่พอร์ต 5000 และ Endpoint ที่ประกอบแล้วจะเรียกใช้งานได้

```js
app.use("/api/users", userRoutes);
// ติดตั้ง Middleware หรือ Route ให้ Express ตามลำดับจากบนลงล่าง
```

### 18.21 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/server.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/server.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 18.21 ทำหน้าที่ `app.use("/api/places", placeRoutes);` เพื่อประกอบ Express Application และเชื่อม Config, Middleware, Static files, Routes และ Error handler เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Server จะโหลด Environment ตั้งค่า Middleware ตามลำดับ mount Route ภายใต้ URL ที่กำหนด และเปิดฟัง PORT; Request ทุกครั้งจะไหลผ่านลำดับนี้ก่อนส่ง Response

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อก้อน `listen` และ Route ครบ `npm run dev` จะเปิด Backend ที่พอร์ต 5000 และ Endpoint ที่ประกอบแล้วจะเรียกใช้งานได้

```js
app.use("/api/places", placeRoutes);
// ติดตั้ง Middleware หรือ Route ให้ Express ตามลำดับจากบนลงล่าง
```

### 18.22 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/server.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/server.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 18.22 ทำหน้าที่ `app.use("/api/reports", reportRoutes);` เพื่อประกอบ Express Application และเชื่อม Config, Middleware, Static files, Routes และ Error handler เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Server จะโหลด Environment ตั้งค่า Middleware ตามลำดับ mount Route ภายใต้ URL ที่กำหนด และเปิดฟัง PORT; Request ทุกครั้งจะไหลผ่านลำดับนี้ก่อนส่ง Response

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อก้อน `listen` และ Route ครบ `npm run dev` จะเปิด Backend ที่พอร์ต 5000 และ Endpoint ที่ประกอบแล้วจะเรียกใช้งานได้

```js
app.use("/api/reports", reportRoutes);
// ติดตั้ง Middleware หรือ Route ให้ Express ตามลำดับจากบนลงล่าง
```

### 18.23 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/server.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/server.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 18.23 ทำหน้าที่ `app.use((error, req, res, next) => {` เพื่อประกอบ Express Application และเชื่อม Config, Middleware, Static files, Routes และ Error handler เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Server จะโหลด Environment ตั้งค่า Middleware ตามลำดับ mount Route ภายใต้ URL ที่กำหนด และเปิดฟัง PORT; Request ทุกครั้งจะไหลผ่านลำดับนี้ก่อนส่ง Response

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อก้อน `listen` และ Route ครบ `npm run dev` จะเปิด Backend ที่พอร์ต 5000 และ Endpoint ที่ประกอบแล้วจะเรียกใช้งานได้

```js
app.use((error, req, res, next) => {
// รับ error จาก Multer เช่น ไฟล์ใหญ่เกิน 5 MB หรือไม่ใช่รูปภาพ
  if (!error) return next();
  console.error(error);
  // พิมพ์ Error จริงใน Terminal เพื่อช่วยวิเคราะห์โดยไม่ส่งรายละเอียดลับให้ Client
  if (error.code === "LIMIT_FILE_SIZE") {
    return res.status(400).json({ message: "ไฟล์มีขนาดใหญ่เกินไป" });
  }
  return res.status(400).json({ message: error.message || "ข้อมูลที่ส่งมาไม่ถูกต้อง" });
});
```

### 18.24 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/server.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/server.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 18.24 ทำหน้าที่ `if (process.env.NODE_ENV === "production") {` เพื่อประกอบ Express Application และเชื่อม Config, Middleware, Static files, Routes และ Error handler เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Server จะโหลด Environment ตั้งค่า Middleware ตามลำดับ mount Route ภายใต้ URL ที่กำหนด และเปิดฟัง PORT; Request ทุกครั้งจะไหลผ่านลำดับนี้ก่อนส่ง Response

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อก้อน `listen` และ Route ครบ `npm run dev` จะเปิด Backend ที่พอร์ต 5000 และ Endpoint ที่ประกอบแล้วจะเรียกใช้งานได้

```js
if (process.env.NODE_ENV === "production") {
  app.use(express.static(frontendDist));
  // ติดตั้ง Middleware หรือ Route ให้ Express ตามลำดับจากบนลงล่าง
  app.get("/{*path}", (req, res) => res.sendFile(path.join(frontendDist, "index.html")));
} else {
  app.use((req, res) => res.status(404).json({ message: "ไม่พบเส้นทาง" }));
}
```

### 18.25 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/src/server.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/src/server.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 18.25 ทำหน้าที่ `app.listen(PORT, () => {` เพื่อประกอบ Express Application และเชื่อม Config, Middleware, Static files, Routes และ Error handler เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Server จะโหลด Environment ตั้งค่า Middleware ตามลำดับ mount Route ภายใต้ URL ที่กำหนด และเปิดฟัง PORT; Request ทุกครั้งจะไหลผ่านลำดับนี้ก่อนส่ง Response

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อก้อน `listen` และ Route ครบ `npm run dev` จะเปิด Backend ที่พอร์ต 5000 และ Endpoint ที่ประกอบแล้วจะเรียกใช้งานได้

```js
app.listen(PORT, () => {
// เริ่ม Server เป็นลำดับสุดท้าย หลัง Middleware และ Routes พร้อมแล้ว
  console.log(`Server running at http://localhost:${PORT}`);
});
```

เมื่อคัดครบไฟล์ ให้รันจาก `BackEnd`:

```powershell
node --check src/server.js
```

### CHECKPOINT 6 — RUN DEV: Backend ฉบับเต็มเปิดได้

หลัง `node --check` ผ่าน ให้รัน:

```powershell
npm run dev
```

ต้องไม่มี import/module error และเปิด `http://localhost:5000/` แล้วได้ JSON ที่มี `status: "ok"` จากนั้นกด `Ctrl+C` ก่อนทำหัวข้อ 19 หาก Server เปิดไม่ได้ ให้แก้ที่หัวข้อ 4–18 ก่อน ห้ามไปทำ seed หรือ Frontend


## 20–22. ภาคอ้างอิงสำหรับ Database รุ่นเก่า (ข้ามในการสร้างโปรเจกต์ใหม่)

ถ้ากำลังสร้างโปรเจกต์ตามคู่มือนี้และรัน `npx prisma db push` สำเร็จ ให้ข้ามหัวข้อ 20–22 ทั้งหมดไปที่ **ยืนยัน CHECKPOINT 6 — Backend API ครบก่อนเริ่ม Frontend** ห้ามรันสคริปต์เพิ่มคอลัมน์เหล่านี้ เพราะ field อยู่ใน `schema.prisma` และ `db push` จัดการให้แล้ว เนื้อหาสามหัวข้อต่อไปนี้เก็บไว้ใช้อ้างอิงเฉพาะกรณีมี Database รุ่นเก่าที่อัปเดตด้วย `db push` ไม่สำเร็จเท่านั้น

### 20. `BackEnd/scripts/addAvatarColumn.js` (ไม่ต้องทำสำหรับโปรเจกต์ใหม่)

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนต่อไปนี้เรียงกัน

### 20.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `BackEnd/scripts/addAvatarColumn.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/scripts/addAvatarColumn.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 20.1 ทำหน้าที่ `import "dotenv/config";`

**โค้ดก้อนนี้ทำงานอย่างไร:** เมื่อ JavaScript อ่านไฟล์นี้ โค้ดก้อนนี้จะถูกรวมกับก้อนก่อนหน้าเพื่อสร้างหน้าที่ของไฟล์ให้สมบูรณ์

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** หลังบันทึก `BackEnd/scripts/addAvatarColumn.js` จะมีความสามารถส่วนนี้เพิ่มขึ้น แต่ให้ประกอบทุกก้อนของไฟล์ก่อนแล้วจึงทดสอบที่ CHECKPOINT ถัดไป

```js
import "dotenv/config";
// ก้อนนี้นำ Module ที่จำเป็นเข้ามา เพื่อให้โค้ดส่วนถัดไปเรียกใช้ความสามารถเหล่านั้น
```

### 20.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/scripts/addAvatarColumn.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/scripts/addAvatarColumn.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 20.2 ทำหน้าที่ `import { prisma } from "../src/lib/prisma.js";`

**โค้ดก้อนนี้ทำงานอย่างไร:** เมื่อ JavaScript อ่านไฟล์นี้ โค้ดก้อนนี้จะถูกรวมกับก้อนก่อนหน้าเพื่อสร้างหน้าที่ของไฟล์ให้สมบูรณ์

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** หลังบันทึก `BackEnd/scripts/addAvatarColumn.js` จะมีความสามารถส่วนนี้เพิ่มขึ้น แต่ให้ประกอบทุกก้อนของไฟล์ก่อนแล้วจึงทดสอบที่ CHECKPOINT ถัดไป

```js
import { prisma } from "../src/lib/prisma.js";
// นำ Dependency หรือ Module ที่บรรทัดถัดไปต้องใช้เข้ามาในไฟล์
```

### 20.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/scripts/addAvatarColumn.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/scripts/addAvatarColumn.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 20.3 ทำหน้าที่ `try {`

**โค้ดก้อนนี้ทำงานอย่างไร:** เมื่อ JavaScript อ่านไฟล์นี้ โค้ดก้อนนี้จะถูกรวมกับก้อนก่อนหน้าเพื่อสร้างหน้าที่ของไฟล์ให้สมบูรณ์

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** หลังบันทึก `BackEnd/scripts/addAvatarColumn.js` จะมีความสามารถส่วนนี้เพิ่มขึ้น แต่ให้ประกอบทุกก้อนของไฟล์ก่อนแล้วจึงทดสอบที่ CHECKPOINT ถัดไป

```js
try {
  const columns = await prisma.$queryRawUnsafe("SHOW COLUMNS FROM User LIKE 'avatarUrl'");
  // รอ Promise นี้ทำงานเสร็จก่อนใช้ผลลัพธ์หรือทำบรรทัดถัดไป

  if (columns.length === 0) {
  // ตรวจเงื่อนไขก่อนอนุญาตให้โค้ดภายในทำงาน
    await prisma.$executeRawUnsafe("ALTER TABLE User ADD COLUMN avatarUrl VARCHAR(191) NULL");
    // รอ Promise นี้ทำงานเสร็จก่อนใช้ผลลัพธ์หรือทำบรรทัดถัดไป
    console.log("Added User.avatarUrl");
  } else {
    console.log("User.avatarUrl already exists");
  }
} finally {
  await prisma.$disconnect();
  // รอ Promise นี้ทำงานเสร็จก่อนใช้ผลลัพธ์หรือทำบรรทัดถัดไป
}
```

เมื่อคัดครบไฟล์ ให้รันจาก `BackEnd`:

```powershell
node --check scripts/addAvatarColumn.js
```

### 21. `BackEnd/scripts/addImageColumn.js` (ไม่ต้องทำสำหรับโปรเจกต์ใหม่)

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนต่อไปนี้เรียงกัน

### 21.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `BackEnd/scripts/addImageColumn.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/scripts/addImageColumn.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 21.1 ทำหน้าที่ `import "dotenv/config";`

**โค้ดก้อนนี้ทำงานอย่างไร:** เมื่อ JavaScript อ่านไฟล์นี้ โค้ดก้อนนี้จะถูกรวมกับก้อนก่อนหน้าเพื่อสร้างหน้าที่ของไฟล์ให้สมบูรณ์

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** หลังบันทึก `BackEnd/scripts/addImageColumn.js` จะมีความสามารถส่วนนี้เพิ่มขึ้น แต่ให้ประกอบทุกก้อนของไฟล์ก่อนแล้วจึงทดสอบที่ CHECKPOINT ถัดไป

```js
import "dotenv/config";
// ก้อนนี้นำ Module ที่จำเป็นเข้ามา เพื่อให้โค้ดส่วนถัดไปเรียกใช้ความสามารถเหล่านั้น
```

### 21.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/scripts/addImageColumn.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/scripts/addImageColumn.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 21.2 ทำหน้าที่ `import { prisma } from "../src/lib/prisma.js";`

**โค้ดก้อนนี้ทำงานอย่างไร:** เมื่อ JavaScript อ่านไฟล์นี้ โค้ดก้อนนี้จะถูกรวมกับก้อนก่อนหน้าเพื่อสร้างหน้าที่ของไฟล์ให้สมบูรณ์

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** หลังบันทึก `BackEnd/scripts/addImageColumn.js` จะมีความสามารถส่วนนี้เพิ่มขึ้น แต่ให้ประกอบทุกก้อนของไฟล์ก่อนแล้วจึงทดสอบที่ CHECKPOINT ถัดไป

```js
import { prisma } from "../src/lib/prisma.js";
// นำ Dependency หรือ Module ที่บรรทัดถัดไปต้องใช้เข้ามาในไฟล์
```

### 21.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/scripts/addImageColumn.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/scripts/addImageColumn.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 21.3 ทำหน้าที่ `try {`

**โค้ดก้อนนี้ทำงานอย่างไร:** เมื่อ JavaScript อ่านไฟล์นี้ โค้ดก้อนนี้จะถูกรวมกับก้อนก่อนหน้าเพื่อสร้างหน้าที่ของไฟล์ให้สมบูรณ์

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** หลังบันทึก `BackEnd/scripts/addImageColumn.js` จะมีความสามารถส่วนนี้เพิ่มขึ้น แต่ให้ประกอบทุกก้อนของไฟล์ก่อนแล้วจึงทดสอบที่ CHECKPOINT ถัดไป

```js
try {
  const columns = await prisma.$queryRawUnsafe("SHOW COLUMNS FROM Place LIKE 'imageUrl'");
  // รอ Promise นี้ทำงานเสร็จก่อนใช้ผลลัพธ์หรือทำบรรทัดถัดไป

  if (columns.length === 0) {
  // ตรวจเงื่อนไขก่อนอนุญาตให้โค้ดภายในทำงาน
    await prisma.$executeRawUnsafe("ALTER TABLE Place ADD COLUMN imageUrl VARCHAR(191) NULL");
    // รอ Promise นี้ทำงานเสร็จก่อนใช้ผลลัพธ์หรือทำบรรทัดถัดไป
    console.log("Added Place.imageUrl");
  } else {
    console.log("Place.imageUrl already exists");
  }
} finally {
  await prisma.$disconnect();
  // รอ Promise นี้ทำงานเสร็จก่อนใช้ผลลัพธ์หรือทำบรรทัดถัดไป
}
```

เมื่อคัดครบไฟล์ ให้รันจาก `BackEnd`:

```powershell
node --check scripts/addImageColumn.js
```

### 22. `BackEnd/scripts/addReportEvidenceColumn.js` (ไม่ต้องทำสำหรับโปรเจกต์ใหม่)

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนต่อไปนี้เรียงกัน

### 22.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `BackEnd/scripts/addReportEvidenceColumn.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/scripts/addReportEvidenceColumn.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 22.1 ทำหน้าที่ `import "dotenv/config";`

**โค้ดก้อนนี้ทำงานอย่างไร:** เมื่อ JavaScript อ่านไฟล์นี้ โค้ดก้อนนี้จะถูกรวมกับก้อนก่อนหน้าเพื่อสร้างหน้าที่ของไฟล์ให้สมบูรณ์

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** หลังบันทึก `BackEnd/scripts/addReportEvidenceColumn.js` จะมีความสามารถส่วนนี้เพิ่มขึ้น แต่ให้ประกอบทุกก้อนของไฟล์ก่อนแล้วจึงทดสอบที่ CHECKPOINT ถัดไป

```js
import "dotenv/config";
// ก้อนนี้นำ Module ที่จำเป็นเข้ามา เพื่อให้โค้ดส่วนถัดไปเรียกใช้ความสามารถเหล่านั้น
```

### 22.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/scripts/addReportEvidenceColumn.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/scripts/addReportEvidenceColumn.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 22.2 ทำหน้าที่ `import { prisma } from "../src/lib/prisma.js";`

**โค้ดก้อนนี้ทำงานอย่างไร:** เมื่อ JavaScript อ่านไฟล์นี้ โค้ดก้อนนี้จะถูกรวมกับก้อนก่อนหน้าเพื่อสร้างหน้าที่ของไฟล์ให้สมบูรณ์

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** หลังบันทึก `BackEnd/scripts/addReportEvidenceColumn.js` จะมีความสามารถส่วนนี้เพิ่มขึ้น แต่ให้ประกอบทุกก้อนของไฟล์ก่อนแล้วจึงทดสอบที่ CHECKPOINT ถัดไป

```js
import { prisma } from "../src/lib/prisma.js";
// นำ Dependency หรือ Module ที่บรรทัดถัดไปต้องใช้เข้ามาในไฟล์
```

### 22.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `BackEnd/scripts/addReportEvidenceColumn.js`

**ตำแหน่งวาง:** วางต่อจากก้อนหมายเลขก่อนหน้าในไฟล์ `BackEnd/scripts/addReportEvidenceColumn.js` โดยให้อยู่ระดับปีกกาเดียวกับโค้ดต้นฉบับ หากเป็นก้อนแรกให้วางหลัง comment แบบฝึกหัดด้านบน; หากบรรทัดก่อนหน้ายังเปิด `{`, `(` หรือ `[` ให้ต่อภายในปีกกานั้น ห้ามย้ายออกนอก function

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ ไม่คัดหัวข้อ Markdown แล้วกดบันทึกเมื่อปีกกาของก้อนปัจจุบันปิดครบ

**ทำไมต้องวางก้อนนี้:** ก้อน 22.3 ทำหน้าที่ `try {`

**โค้ดก้อนนี้ทำงานอย่างไร:** เมื่อ JavaScript อ่านไฟล์นี้ โค้ดก้อนนี้จะถูกรวมกับก้อนก่อนหน้าเพื่อสร้างหน้าที่ของไฟล์ให้สมบูรณ์

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** หลังบันทึก `BackEnd/scripts/addReportEvidenceColumn.js` จะมีความสามารถส่วนนี้เพิ่มขึ้น แต่ให้ประกอบทุกก้อนของไฟล์ก่อนแล้วจึงทดสอบที่ CHECKPOINT ถัดไป

```js
try {
  const columns = await prisma.$queryRawUnsafe("SHOW COLUMNS FROM Report LIKE 'evidenceImageUrl'");
  // รอ Promise นี้ทำงานเสร็จก่อนใช้ผลลัพธ์หรือทำบรรทัดถัดไป
  if (columns.length === 0) {
  // ตรวจเงื่อนไขก่อนอนุญาตให้โค้ดภายในทำงาน
    await prisma.$executeRawUnsafe("ALTER TABLE Report ADD COLUMN evidenceImageUrl VARCHAR(191) NULL");
    // รอ Promise นี้ทำงานเสร็จก่อนใช้ผลลัพธ์หรือทำบรรทัดถัดไป
    console.log("Added Report.evidenceImageUrl");
  } else {
    console.log("Report.evidenceImageUrl already exists");
  }
} finally {
  await prisma.$disconnect();
  // รอ Promise นี้ทำงานเสร็จก่อนใช้ผลลัพธ์หรือทำบรรทัดถัดไป
}
```

เมื่อคัดครบไฟล์ ให้รันจาก `BackEnd`:

```powershell
node --check scripts/addReportEvidenceColumn.js
```

## ยืนยัน CHECKPOINT 6 — Backend API ครบก่อนเริ่ม Frontend

1. เปิด Server ใน Terminal ด้วย `npm run dev`
2. สมัครบัญชีสมาชิกจากหน้า Register หรือ Postman แล้วเปิด `npx prisma studio` เพื่อเปลี่ยน `role` เป็น `admin` เมื่อต้องการทดสอบหน้าผู้ดูแล
3. ทดสอบคำขอของ CHECKPOINT 3–5 ซ้ำให้ครบ และห้ามเริ่ม Frontend จน API ของ Auth/User, Place และ Report ผ่าน


---

# จุดเชื่อมก่อนเริ่ม Frontend

ห้ามลงภาค Frontend จน Backend เปิดด้วย `npm run dev` และทดสอบ CHECKPOINT 3–5 ผ่านแล้ว จากนั้นตรวจคู่สัญญา: `/auth` ↔ Login/Register, `/users` ↔ Profile, `/places` ↔ Map/Search/Add/Detail/Admin และ `/reports` ↔ Report/Admin Reports

# ส่วน F — Frontend


คัดตามหมายเลข ห้ามเริ่มจน Backend ผ่าน CHECKPOINT 6 ก้อนที่ปีกกายังไม่ปิดต้องวางก้อนถัดไปต่อก่อนบันทึก

ตั้งแต่หัวข้อ 2 เป็นต้นไป ข้อความ `เมื่อคัดครบไฟล์ ให้รัน npm run lint` คือ CHECKPOINT ของไฟล์นั้น ถ้า lint มี error แม้เพียงไฟล์เดียว ให้หยุดแก้ก่อน ห้ามสะสม error ไปท้ายโปรเจกต์

## 1. `FrontEnd/package.json`

ไฟล์นี้ทำเสร็จแล้วใน Setup ให้ตรวจว่าเนื้อหาตรงกับ code block ด้านล่างและ **ห้ามวางซ้ำต่อท้ายไฟล์** หากไฟล์ยังเป็นแบบฝึกหัดจึงค่อยแทนที่เนื้อหาทั้งไฟล์ด้วย code block นี้

### 1.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `FrontEnd/package.json`

**ตำแหน่งวาง:** วางหลัง comment แบบฝึกหัดเดิม; import ต้องอยู่บนสุดของไฟล์

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 1.1 ทำหน้าที่ `{` เพื่อกำหนดค่าที่เครื่องมือพัฒนาและ Application ต้องอ่านก่อนเริ่มทำงาน

**โค้ดก้อนนี้ทำงานอย่างไร:** Vite หรือ npm จะอ่านค่านี้ตอนเริ่ม dev/build เพื่อเลือก plugin, script, dependency, URL และ Environment ที่ใช้

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** คำสั่งติดตั้ง พัฒนา และ build จะใช้ค่าชุดเดียวกัน ลดปัญหา Port, URL หรือ Dependency ไม่ตรงกัน

```json
{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint": "oxlint",
    "preview": "vite preview"
  },
  "dependencies": {
    "axios": "^1.19.0",
    "leaflet": "^1.9.4",
    "lucide-react": "^1.31.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "react-geolocated": "^4.5.1",
    "react-leaflet": "^5.0.0",
    "react-router-dom": "^7.18.2"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.4",
    "oxlint": "^1.75.0",
    "tailwindcss": "^4.3.3",
    "vite": "^8.2.0"
  }
}
```

## 2. `FrontEnd/vite.config.js`

ไฟล์นี้ทำเสร็จแล้วใน Setup ให้ตรวจว่าเนื้อหาตรงกับ code block ด้านล่างและ **ห้ามวางซ้ำต่อท้ายไฟล์** หากยังไม่ตรงจึงแทนที่เนื้อหาทั้งไฟล์ด้วย code block นี้

### 2.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `FrontEnd/vite.config.js`

**ตำแหน่งวาง:** วางหลัง comment แบบฝึกหัดเดิม; import ต้องอยู่บนสุดของไฟล์

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 2.1 ทำหน้าที่ `https://vite.dev/config/` เพื่อกำหนดค่าที่เครื่องมือพัฒนาและ Application ต้องอ่านก่อนเริ่มทำงาน

**โค้ดก้อนนี้ทำงานอย่างไร:** Vite หรือ npm จะอ่านค่านี้ตอนเริ่ม dev/build เพื่อเลือก plugin, script, dependency, URL และ Environment ที่ใช้

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** คำสั่งติดตั้ง พัฒนา และ build จะใช้ค่าชุดเดียวกัน ลดปัญหา Port, URL หรือ Dependency ไม่ตรงกัน

```js
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react(), tailwindcss()],
})
```

เมื่อคัดครบไฟล์ ให้รัน `npm run lint` และแก้ error ของไฟล์นี้ก่อนก้อนถัดไป

## 3. `FrontEnd/.oxlintrc.json`

ไฟล์นี้ทำเสร็จแล้วใน Setup ให้ตรวจว่าเนื้อหาตรงกับ code block ด้านล่างและ **ห้ามวางซ้ำต่อท้ายไฟล์** หากยังไม่ตรงจึงแทนที่เนื้อหาทั้งไฟล์ด้วย code block นี้

### 3.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `FrontEnd/.oxlintrc.json`

**ตำแหน่งวาง:** วางหลัง comment แบบฝึกหัดเดิม; import ต้องอยู่บนสุดของไฟล์

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 3.1 ทำหน้าที่ `{`

**โค้ดก้อนนี้ทำงานอย่างไร:** เมื่อ JavaScript อ่านไฟล์นี้ โค้ดก้อนนี้จะถูกรวมกับก้อนก่อนหน้าเพื่อสร้างหน้าที่ของไฟล์ให้สมบูรณ์

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** หลังบันทึก `FrontEnd/.oxlintrc.json` จะมีความสามารถส่วนนี้เพิ่มขึ้น แต่ให้ประกอบทุกก้อนของไฟล์ก่อนแล้วจึงทดสอบที่ CHECKPOINT ถัดไป

```json
{
  "$schema": "./node_modules/oxlint/configuration_schema.json",
  "plugins": ["react", "oxc"],
  "rules": {
    "react/rules-of-hooks": "error",
    "react/only-export-components": ["warn", { "allowConstantExport": true }]
  }
}
```

## 4. `FrontEnd/index.html`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนตามหมายเลข

### 4.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `FrontEnd/index.html`

**ตำแหน่งวาง:** วางหลัง comment แบบฝึกหัดเดิม; import ต้องอยู่บนสุดของไฟล์

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 4.1 ทำหน้าที่ `<!doctype html>`

**โค้ดก้อนนี้ทำงานอย่างไร:** เมื่อ JavaScript อ่านไฟล์นี้ โค้ดก้อนนี้จะถูกรวมกับก้อนก่อนหน้าเพื่อสร้างหน้าที่ของไฟล์ให้สมบูรณ์

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** หลังบันทึก `FrontEnd/index.html` จะมีความสามารถส่วนนี้เพิ่มขึ้น แต่ให้ประกอบทุกก้อนของไฟล์ก่อนแล้วจึงทดสอบที่ CHECKPOINT ถัดไป

```html
<!doctype html>
<!-- วางที่: FrontEnd/index.html | ใช้ทำอะไร: สร้าง HTML และ #root สำหรับ React | เมื่อทำงาน: Browser โหลด main.jsx แล้ว render App -->
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>frontend</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.jsx"></script>
  </body>
</html>
```

## 5. `FrontEnd/src/index.css`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนตามหมายเลข

### 5.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `FrontEnd/src/index.css`

**ตำแหน่งวาง:** วางหลัง comment แบบฝึกหัดเดิม; import ต้องอยู่บนสุดของไฟล์

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 5.1 ทำหน้าที่ `@import "tailwindcss";` เพื่อกำหนด Style ส่วนกลางที่มีผลต่อหลาย Component และรองรับ Theme/Responsive layout

**โค้ดก้อนนี้ทำงานอย่างไร:** Browser นำ selector และ utility ที่ประกาศไปคำนวณรูปแบบของ element ที่ตรงกัน โดยกฎที่อยู่ภายหลังหรือเฉพาะเจาะจงกว่าจะมีสิทธิ์ทับกฎเดิม

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** องค์ประกอบที่ใช้ class/selector ตรงกันจะแสดงสี ระยะห่าง ขนาด และ layout ตามกฎนี้เมื่อ Frontend render

```css
/*
 * คู่มือเฉลยไฟล์นี้
 * วางที่: FrontEnd/src/index.css
 * ใช้ทำอะไร: รวม Tailwind theme และ style กลาง
 * เมื่อทำงาน: ทุกหน้าที่ import ไฟล์นี้ได้รับ style ร่วม
 */
/* ลำดับอ่าน Frontend 5: Tailwind theme และ CSS กลางของ Light/Dark/Leaflet */
/* หน้าใช้ Tailwind ส่วนไฟล์นี้ดูแลสีร่วมและกรณีพิเศษของธีมมืด */
@import "tailwindcss";
```

### 5.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/index.css`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 5.2 ทำหน้าที่ `@custom-variant dark (&:where(.dark, .dark *));` เพื่อกำหนด Style ส่วนกลางที่มีผลต่อหลาย Component และรองรับ Theme/Responsive layout

**โค้ดก้อนนี้ทำงานอย่างไร:** Browser นำ selector และ utility ที่ประกาศไปคำนวณรูปแบบของ element ที่ตรงกัน โดยกฎที่อยู่ภายหลังหรือเฉพาะเจาะจงกว่าจะมีสิทธิ์ทับกฎเดิม

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** องค์ประกอบที่ใช้ class/selector ตรงกันจะแสดงสี ระยะห่าง ขนาด และ layout ตามกฎนี้เมื่อ Frontend render

```css
@custom-variant dark (&:where(.dark, .dark *));
```

### 5.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/index.css`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 5.3 ทำหน้าที่ `@theme {` เพื่อกำหนด Style ส่วนกลางที่มีผลต่อหลาย Component และรองรับ Theme/Responsive layout

**โค้ดก้อนนี้ทำงานอย่างไร:** Browser นำ selector และ utility ที่ประกาศไปคำนวณรูปแบบของ element ที่ตรงกัน โดยกฎที่อยู่ภายหลังหรือเฉพาะเจาะจงกว่าจะมีสิทธิ์ทับกฎเดิม

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** องค์ประกอบที่ใช้ class/selector ตรงกันจะแสดงสี ระยะห่าง ขนาด และ layout ตามกฎนี้เมื่อ Frontend render

```css
@theme {
  --color-cream-50: #fffaf1;
  --color-cream-100: #f8eedf;
  --color-cream-200: #ecddc8;
  --color-brand: #ff8518;
  --color-brand-dark: #e96f08;
  --color-leaf: #4faf6a;
  --color-danger: #ef565c;
  --color-ink: #3f382f;
  --font-sans: "Noto Sans Thai", "Leelawadee UI", Arial, sans-serif;
}
```

### 5.4 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/index.css`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 5.4 ทำหน้าที่ `html,` เพื่อกำหนด Style ส่วนกลางที่มีผลต่อหลาย Component และรองรับ Theme/Responsive layout

**โค้ดก้อนนี้ทำงานอย่างไร:** Browser นำ selector และ utility ที่ประกาศไปคำนวณรูปแบบของ element ที่ตรงกัน โดยกฎที่อยู่ภายหลังหรือเฉพาะเจาะจงกว่าจะมีสิทธิ์ทับกฎเดิม

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** องค์ประกอบที่ใช้ class/selector ตรงกันจะแสดงสี ระยะห่าง ขนาด และ layout ตามกฎนี้เมื่อ Frontend render

```css
html,
body,
#root {
  min-height: 100%;
}
```

### 5.5 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/index.css`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 5.5 ทำหน้าที่ `body {` เพื่อกำหนด Style ส่วนกลางที่มีผลต่อหลาย Component และรองรับ Theme/Responsive layout

**โค้ดก้อนนี้ทำงานอย่างไร:** Browser นำ selector และ utility ที่ประกาศไปคำนวณรูปแบบของ element ที่ตรงกัน โดยกฎที่อยู่ภายหลังหรือเฉพาะเจาะจงกว่าจะมีสิทธิ์ทับกฎเดิม

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** องค์ประกอบที่ใช้ class/selector ตรงกันจะแสดงสี ระยะห่าง ขนาด และ layout ตามกฎนี้เมื่อ Frontend render

```css
body {
  margin: 0;
  background: #eee9e1;
  color: #3f382f;
  font-family: var(--font-sans);
}
```

### 5.6 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/index.css`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 5.6 ทำหน้าที่ `html.dark body {` เพื่อกำหนด Style ส่วนกลางที่มีผลต่อหลาย Component และรองรับ Theme/Responsive layout

**โค้ดก้อนนี้ทำงานอย่างไร:** Browser นำ selector และ utility ที่ประกาศไปคำนวณรูปแบบของ element ที่ตรงกัน โดยกฎที่อยู่ภายหลังหรือเฉพาะเจาะจงกว่าจะมีสิทธิ์ทับกฎเดิม

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** องค์ประกอบที่ใช้ class/selector ตรงกันจะแสดงสี ระยะห่าง ขนาด และ layout ตามกฎนี้เมื่อ Frontend render

```css
/* ธีมมืดใช้สีเทาอุ่น ไม่ใช้ดำสนิท เพื่อลดความต่างของแสง */
html.dark body {
  background: #171614;
  color: #e8e1d7;
  color-scheme: dark;
}
```

### 5.7 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/index.css`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 5.7 ทำหน้าที่ `html.dark .bg-cream-50 { background-color: #211f1c !important; }` เพื่อกำหนด Style ส่วนกลางที่มีผลต่อหลาย Component และรองรับ Theme/Responsive layout

**โค้ดก้อนนี้ทำงานอย่างไร:** Browser นำ selector และ utility ที่ประกาศไปคำนวณรูปแบบของ element ที่ตรงกัน โดยกฎที่อยู่ภายหลังหรือเฉพาะเจาะจงกว่าจะมีสิทธิ์ทับกฎเดิม

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** องค์ประกอบที่ใช้ class/selector ตรงกันจะแสดงสี ระยะห่าง ขนาด และ layout ตามกฎนี้เมื่อ Frontend render

```css
html.dark .bg-cream-50 { background-color: #211f1c !important; }
html.dark .bg-cream-100 { background-color: #2c2925 !important; }
html.dark .bg-white,
html.dark .bg-white\/70 { background-color: #302d29 !important; }
html.dark .border-cream-200 { border-color: #4d4841 !important; }
html.dark .text-ink { color: #eee7dc !important; }
html.dark .text-stone-400 { color: #aaa39a !important; }
html.dark .text-stone-500,
html.dark .text-stone-600 { color: #cfc7bc !important; }
html.dark .text-stone-700,
html.dark .text-stone-800,
html.dark .text-stone-900,
html.dark .text-black { color: #f0e9df !important; }
html.dark h1,
html.dark h2,
html.dark h3,
html.dark label,
html.dark p { text-shadow: 0 1px 1px rgb(0 0 0 / 18%); }
html.dark input,
html.dark textarea,
html.dark select {
  color: #f3ece2;
  background-color: #302d29;
}
html.dark input::placeholder,
html.dark textarea::placeholder { color: #b2aaa0; opacity: 1; }
html.dark nav { color: #d7cfc4; }
html.dark .shadow-sm,
html.dark .shadow-2xl { box-shadow: 0 10px 30px rgb(0 0 0 / 25%); }
html.dark .leaflet-tile-pane { filter: brightness(.72) contrast(.9) saturate(.75); }
html.dark .leaflet-control,
html.dark .leaflet-popup-content-wrapper,
html.dark .leaflet-popup-tip { background: #302d29; color: #eee7dc; }
```

### 5.8 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/index.css`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 5.8 ทำหน้าที่ `html.dark .auth-panel {` เพื่อกำหนด Style ส่วนกลางที่มีผลต่อหลาย Component และรองรับ Theme/Responsive layout

**โค้ดก้อนนี้ทำงานอย่างไร:** Browser นำ selector และ utility ที่ประกาศไปคำนวณรูปแบบของ element ที่ตรงกัน โดยกฎที่อยู่ภายหลังหรือเฉพาะเจาะจงกว่าจะมีสิทธิ์ทับกฎเดิม

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** องค์ประกอบที่ใช้ class/selector ตรงกันจะแสดงสี ระยะห่าง ขนาด และ layout ตามกฎนี้เมื่อ Frontend render

```css
/* Login/Register มีภาพพื้นหลังสว่าง จึงกำหนดกล่องฟอร์มโหมดมืดแยกให้ชัด */
html.dark .auth-panel {
  background: rgb(37 34 31 / 94%) !important;
  color: #f3ece2;
}
```

### 5.9 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/index.css`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 5.9 ทำหน้าที่ `html.dark .auth-panel input {` เพื่อกำหนด Style ส่วนกลางที่มีผลต่อหลาย Component และรองรับ Theme/Responsive layout

**โค้ดก้อนนี้ทำงานอย่างไร:** Browser นำ selector และ utility ที่ประกาศไปคำนวณรูปแบบของ element ที่ตรงกัน โดยกฎที่อยู่ภายหลังหรือเฉพาะเจาะจงกว่าจะมีสิทธิ์ทับกฎเดิม

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** องค์ประกอบที่ใช้ class/selector ตรงกันจะแสดงสี ระยะห่าง ขนาด และ layout ตามกฎนี้เมื่อ Frontend render

```css
html.dark .auth-panel input {
  border-color: #5e574f;
  background: #302d29 !important;
  color: #f3ece2 !important;
  -webkit-text-fill-color: #f3ece2;
}
```

### 5.10 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/index.css`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 5.10 ทำหน้าที่ `html.dark .auth-panel input:-webkit-autofill,` เพื่อกำหนด Style ส่วนกลางที่มีผลต่อหลาย Component และรองรับ Theme/Responsive layout

**โค้ดก้อนนี้ทำงานอย่างไร:** Browser นำ selector และ utility ที่ประกาศไปคำนวณรูปแบบของ element ที่ตรงกัน โดยกฎที่อยู่ภายหลังหรือเฉพาะเจาะจงกว่าจะมีสิทธิ์ทับกฎเดิม

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** องค์ประกอบที่ใช้ class/selector ตรงกันจะแสดงสี ระยะห่าง ขนาด และ layout ตามกฎนี้เมื่อ Frontend render

```css
html.dark .auth-panel input:-webkit-autofill,
html.dark .auth-panel input:-webkit-autofill:hover,
html.dark .auth-panel input:-webkit-autofill:focus {
  -webkit-text-fill-color: #f3ece2;
  box-shadow: 0 0 0 1000px #302d29 inset;
  transition: background-color 9999s ease-out;
}
```

### 5.11 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/index.css`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 5.11 ทำหน้าที่ `html.dark .auth-panel label,` เพื่อกำหนด Style ส่วนกลางที่มีผลต่อหลาย Component และรองรับ Theme/Responsive layout

**โค้ดก้อนนี้ทำงานอย่างไร:** Browser นำ selector และ utility ที่ประกาศไปคำนวณรูปแบบของ element ที่ตรงกัน โดยกฎที่อยู่ภายหลังหรือเฉพาะเจาะจงกว่าจะมีสิทธิ์ทับกฎเดิม

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** องค์ประกอบที่ใช้ class/selector ตรงกันจะแสดงสี ระยะห่าง ขนาด และ layout ตามกฎนี้เมื่อ Frontend render

```css
html.dark .auth-panel label,
html.dark .auth-panel h1,
html.dark .auth-panel h2 { color: #f3ece2 !important; }
html.dark .auth-panel p { color: #cfc7bc; }
html.dark .auth-brand-text {
  color: #51483f !important;
  text-shadow: 0 1px 2px rgb(255 255 255 / 75%);
}
```

### 5.12 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/index.css`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 5.12 ทำหน้าที่ `button,` เพื่อกำหนด Style ส่วนกลางที่มีผลต่อหลาย Component และรองรับ Theme/Responsive layout

**โค้ดก้อนนี้ทำงานอย่างไร:** Browser นำ selector และ utility ที่ประกาศไปคำนวณรูปแบบของ element ที่ตรงกัน โดยกฎที่อยู่ภายหลังหรือเฉพาะเจาะจงกว่าจะมีสิทธิ์ทับกฎเดิม

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** องค์ประกอบที่ใช้ class/selector ตรงกันจะแสดงสี ระยะห่าง ขนาด และ layout ตามกฎนี้เมื่อ Frontend render

```css
button,
input,
textarea,
select {
  font: inherit;
}
```

### 5.13 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/index.css`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 5.13 ทำหน้าที่ `.leaflet-container {` เพื่อกำหนด Style ส่วนกลางที่มีผลต่อหลาย Component และรองรับ Theme/Responsive layout

**โค้ดก้อนนี้ทำงานอย่างไร:** Browser นำ selector และ utility ที่ประกาศไปคำนวณรูปแบบของ element ที่ตรงกัน โดยกฎที่อยู่ภายหลังหรือเฉพาะเจาะจงกว่าจะมีสิทธิ์ทับกฎเดิม

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** องค์ประกอบที่ใช้ class/selector ตรงกันจะแสดงสี ระยะห่าง ขนาด และ layout ตามกฎนี้เมื่อ Frontend render

```css
.leaflet-container {
  width: 100%;
  height: 100%;
  font-family: var(--font-sans);
}
```

### 5.14 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/index.css`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 5.14 ทำหน้าที่ `.map-dot {` เพื่อกำหนด Style ส่วนกลางที่มีผลต่อหลาย Component และรองรับ Theme/Responsive layout

**โค้ดก้อนนี้ทำงานอย่างไร:** Browser นำ selector และ utility ที่ประกาศไปคำนวณรูปแบบของ element ที่ตรงกัน โดยกฎที่อยู่ภายหลังหรือเฉพาะเจาะจงกว่าจะมีสิทธิ์ทับกฎเดิม

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** องค์ประกอบที่ใช้ class/selector ตรงกันจะแสดงสี ระยะห่าง ขนาด และ layout ตามกฎนี้เมื่อ Frontend render

```css
.map-dot {
  border: 3px solid white;
  border-radius: 999px;
  box-shadow: 0 3px 12px rgb(63 56 47 / 25%);
}
```

## 6. `FrontEnd/src/services/api.js`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนตามหมายเลข

### 6.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `FrontEnd/src/services/api.js`

**ตำแหน่งวาง:** วางหลัง comment แบบฝึกหัดเดิม; import ต้องอยู่บนสุดของไฟล์

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 6.1 ทำหน้าที่ `import axios from "axios";` เพื่อรวมการเรียก Backend API ไว้จุดเดียว แทนการเขียน fetch ซ้ำทุกหน้า

**โค้ดก้อนนี้ทำงานอย่างไร:** โค้ดจะสร้าง URL จาก `VITE_API_URL`, แนบ token/headers/body ตามชนิด Request, แปลง Response เป็นข้อมูล JavaScript และโยน Error ให้หน้าที่เรียกนำไปแสดง

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page และ Hook จะเรียกฟังก์ชัน API ชุดเดียวกันได้ ทำให้ URL, Authentication และการจัดการ Error สอดคล้องกันทั้งเว็บ

```js
import axios from "axios";
// ลำดับอ่าน Frontend 6: Axios ตัวกลางที่ทุก Page ใช้เรียก Backend | Interceptor นำ JWT ไปใส่ Authorization ให้อัตโนมัติ
```

### 6.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/services/api.js`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 6.2 ทำหน้าที่ `const apiUrl = import.meta.env.VITE_API_URL || "http://localhost:5000/api";` เพื่อรวมการเรียก Backend API ไว้จุดเดียว แทนการเขียน fetch ซ้ำทุกหน้า

**โค้ดก้อนนี้ทำงานอย่างไร:** โค้ดจะสร้าง URL จาก `VITE_API_URL`, แนบ token/headers/body ตามชนิด Request, แปลง Response เป็นข้อมูล JavaScript และโยน Error ให้หน้าที่เรียกนำไปแสดง

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page และ Hook จะเรียกฟังก์ชัน API ชุดเดียวกันได้ ทำให้ URL, Authentication และการจัดการ Error สอดคล้องกันทั้งเว็บ

```js
const apiUrl = import.meta.env.VITE_API_URL || "http://localhost:5000/api";
// ก้อนนี้เตรียมข้อมูลและควบคุมลำดับการทำงานก่อนส่งผลไปยังส่วนถัดไปของระบบ
```

### 6.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/services/api.js`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 6.3 ทำหน้าที่ `const api = axios.create({ baseURL: apiUrl });` เพื่อรวมการเรียก Backend API ไว้จุดเดียว แทนการเขียน fetch ซ้ำทุกหน้า

**โค้ดก้อนนี้ทำงานอย่างไร:** โค้ดจะสร้าง URL จาก `VITE_API_URL`, แนบ token/headers/body ตามชนิด Request, แปลง Response เป็นข้อมูล JavaScript และโยน Error ให้หน้าที่เรียกนำไปแสดง

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page และ Hook จะเรียกฟังก์ชัน API ชุดเดียวกันได้ ทำให้ URL, Authentication และการจัดการ Error สอดคล้องกันทั้งเว็บ

```js
const api = axios.create({ baseURL: apiUrl });
// ทุก Page import instance นี้ จึงไม่ต้องเขียน Base URL ซ้ำ
```

### 6.4 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/services/api.js`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 6.4 ทำหน้าที่ `Production ใช้ /api แบบ relative จึงอ่าน origin จากหน้าเว็บปัจจุบัน` เพื่อรวมการเรียก Backend API ไว้จุดเดียว แทนการเขียน fetch ซ้ำทุกหน้า

**โค้ดก้อนนี้ทำงานอย่างไร:** โค้ดจะสร้าง URL จาก `VITE_API_URL`, แนบ token/headers/body ตามชนิด Request, แปลง Response เป็นข้อมูล JavaScript และโยน Error ให้หน้าที่เรียกนำไปแสดง

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page และ Hook จะเรียกฟังก์ชัน API ชุดเดียวกันได้ ทำให้ URL, Authentication และการจัดการ Error สอดคล้องกันทั้งเว็บ

```js
export function getAssetUrl(imageUrl) {
// ประกาศฟังก์ชันนี้และกำหนดขอบเขตงานตามชื่อของฟังก์ชัน
  if (!imageUrl) return "";
  // Backend เก็บ URL แบบ /uploads/... ฟังก์ชันนี้เติม Domain ให้ Browser เปิดได้
  if (imageUrl.startsWith("http")) return imageUrl;
  // Production ใช้ /api แบบ relative จึงอ่าน origin จากหน้าเว็บปัจจุบัน
  const origin = apiUrl.startsWith("http") ? new URL(apiUrl).origin : window.location.origin;
  // Production ใช้ /api แบบ relative จึงอ่าน origin จากหน้าเว็บปัจจุบัน
  return `${origin}${imageUrl}`;
  // ส่งผลลัพธ์ออกจากฟังก์ชันและหยุดทำบรรทัดถัดไปในฟังก์ชันนี้
}
```

### 6.5 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/services/api.js`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 6.5 ทำหน้าที่ `api.interceptors.request.use((config) => {` เพื่อรวมการเรียก Backend API ไว้จุดเดียว แทนการเขียน fetch ซ้ำทุกหน้า

**โค้ดก้อนนี้ทำงานอย่างไร:** โค้ดจะสร้าง URL จาก `VITE_API_URL`, แนบ token/headers/body ตามชนิด Request, แปลง Response เป็นข้อมูล JavaScript และโยน Error ให้หน้าที่เรียกนำไปแสดง

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page และ Hook จะเรียกฟังก์ชัน API ชุดเดียวกันได้ ทำให้ URL, Authentication และการจัดการ Error สอดคล้องกันทั้งเว็บ

```js
api.interceptors.request.use((config) => {
  const token = localStorage.getItem("token");

  if (token) {
  // ตรวจเงื่อนไขก่อนอนุญาตให้โค้ดภายในทำงาน
    config.headers.Authorization = `Bearer ${token}`;
  }

  return config;
  // ส่งผลลัพธ์ออกจากฟังก์ชันและหยุดทำบรรทัดถัดไปในฟังก์ชันนี้
});
```

### 6.6 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/services/api.js`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 6.6 ทำหน้าที่ `export function getErrorMessage(error) {` เพื่อรวมการเรียก Backend API ไว้จุดเดียว แทนการเขียน fetch ซ้ำทุกหน้า

**โค้ดก้อนนี้ทำงานอย่างไร:** โค้ดจะสร้าง URL จาก `VITE_API_URL`, แนบ token/headers/body ตามชนิด Request, แปลง Response เป็นข้อมูล JavaScript และโยน Error ให้หน้าที่เรียกนำไปแสดง

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page และ Hook จะเรียกฟังก์ชัน API ชุดเดียวกันได้ ทำให้ URL, Authentication และการจัดการ Error สอดคล้องกันทั้งเว็บ

```js
export function getErrorMessage(error) {
// ประกาศฟังก์ชันนี้และกำหนดขอบเขตงานตามชื่อของฟังก์ชัน
  const validationError = error.response?.data?.errors?.[0]?.message;
  // เลือกข้อความ Zod ก่อน แล้วค่อยข้อความทั่วไปจาก Controller
  return validationError || error.response?.data?.message || "เกิดข้อผิดพลาด กรุณาลองใหม่";
  // ส่งผลลัพธ์ออกจากฟังก์ชันและหยุดทำบรรทัดถัดไปในฟังก์ชันนี้
}
```

### 6.7 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/services/api.js`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 6.7 ทำหน้าที่ `export default api;` เพื่อรวมการเรียก Backend API ไว้จุดเดียว แทนการเขียน fetch ซ้ำทุกหน้า

**โค้ดก้อนนี้ทำงานอย่างไร:** โค้ดจะสร้าง URL จาก `VITE_API_URL`, แนบ token/headers/body ตามชนิด Request, แปลง Response เป็นข้อมูล JavaScript และโยน Error ให้หน้าที่เรียกนำไปแสดง

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page และ Hook จะเรียกฟังก์ชัน API ชุดเดียวกันได้ ทำให้ URL, Authentication และการจัดการ Error สอดคล้องกันทั้งเว็บ

```js
export default api;
// ก้อนนี้ประกาศและส่งออกฟังก์ชันหรือ Component เพื่อให้ไฟล์อื่นเรียกใช้งานต่อได้
```

เมื่อคัดครบไฟล์ ให้รัน `npm run lint` และแก้ error ของไฟล์นี้ก่อนก้อนถัดไป

### CHECKPOINT 7 — RUN DEV: Frontend หน้าเริ่มต้น

สร้างหน้าเริ่มต้นชั่วคราวเพื่อให้เห็นเว็บตั้งแต่ตอนนี้ โดยแทนที่เนื้อหา `FrontEnd/src/App.jsx` และ `FrontEnd/src/main.jsx` ชั่วคราว ไฟล์ทั้งสองจะถูกขยายอีกครั้งใน CHECKPOINT ถัดไปและประกอบฉบับเต็มในหัวข้อ 35–36

`FrontEnd/src/App.jsx`:

```jsx
export default function App() {
  return <main className="grid min-h-screen place-items-center bg-cream-50 text-ink"><h1 className="text-2xl font-bold">Frontend พร้อมแล้ว</h1></main>;
}
```

`FrontEnd/src/main.jsx`:

```jsx
import { StrictMode } from "react";
import { createRoot } from "react-dom/client";
import "./index.css";
import App from "./App.jsx";

createRoot(document.getElementById("root")).render(<StrictMode><App /></StrictMode>);
```

รัน `npm run dev` แล้วเปิด URL ที่ Vite แสดง ต้องเห็นข้อความ **Frontend พร้อมแล้ว** โดยหน้า Browser และ Terminal ไม่มี error จากนั้นกด `Ctrl+C` ก่อนทำหัวข้อ 7

## 7. `FrontEnd/src/hooks/authContext.js`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนตามหมายเลข

### 7.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `FrontEnd/src/hooks/authContext.js`

**ตำแหน่งวาง:** วางหลัง comment แบบฝึกหัดเดิม; import ต้องอยู่บนสุดของไฟล์

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 7.1 ทำหน้าที่ `import { createContext } from "react";` เพื่อเก็บและกระจาย State ที่หลาย Component ต้องใช้ร่วมกันโดยไม่ต้องส่ง Props ผ่านทุกชั้น

**โค้ดก้อนนี้ทำงานอย่างไร:** Context กำหนดช่องทางข้อมูล, Provider เป็นเจ้าของ State และฟังก์ชันเปลี่ยนค่า ส่วน custom Hook ช่วยให้ Component อ่าน Context พร้อมตรวจการใช้งานผิดตำแหน่ง

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Component ที่อยู่ใต้ Provider จะอ่านสถานะ Login, User หรือ Theme เดียวกัน และ UI จะอัปเดตเมื่อ State เปลี่ยน

```js
import { createContext } from "react";
// ก้อนนี้นำ Module ที่จำเป็นเข้ามา เพื่อให้โค้ดส่วนถัดไปเรียกใช้ความสามารถเหล่านั้น
```

### 7.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/hooks/authContext.js`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 7.2 ทำหน้าที่ `const AuthContext = createContext(null);` เพื่อเก็บและกระจาย State ที่หลาย Component ต้องใช้ร่วมกันโดยไม่ต้องส่ง Props ผ่านทุกชั้น

**โค้ดก้อนนี้ทำงานอย่างไร:** Context กำหนดช่องทางข้อมูล, Provider เป็นเจ้าของ State และฟังก์ชันเปลี่ยนค่า ส่วน custom Hook ช่วยให้ Component อ่าน Context พร้อมตรวจการใช้งานผิดตำแหน่ง

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Component ที่อยู่ใต้ Provider จะอ่านสถานะ Login, User หรือ Theme เดียวกัน และ UI จะอัปเดตเมื่อ State เปลี่ยน

```js
const AuthContext = createContext(null);
// ค่าเริ่ม null เพราะ AuthProvider จะส่ง value จริงตอน Render
```

### 7.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/hooks/authContext.js`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 7.3 ทำหน้าที่ `export default AuthContext;` เพื่อเก็บและกระจาย State ที่หลาย Component ต้องใช้ร่วมกันโดยไม่ต้องส่ง Props ผ่านทุกชั้น

**โค้ดก้อนนี้ทำงานอย่างไร:** Context กำหนดช่องทางข้อมูล, Provider เป็นเจ้าของ State และฟังก์ชันเปลี่ยนค่า ส่วน custom Hook ช่วยให้ Component อ่าน Context พร้อมตรวจการใช้งานผิดตำแหน่ง

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Component ที่อยู่ใต้ Provider จะอ่านสถานะ Login, User หรือ Theme เดียวกัน และ UI จะอัปเดตเมื่อ State เปลี่ยน

```js
export default AuthContext;
// Provider และ useAuth ต้องใช้ Context กล่องเดียวกัน
```

เมื่อคัดครบไฟล์ ให้รัน `npm run lint` และแก้ error ของไฟล์นี้ก่อนก้อนถัดไป

## 8. `FrontEnd/src/hooks/AuthProvider.jsx`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนตามหมายเลข

### 8.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `FrontEnd/src/hooks/AuthProvider.jsx`

**ตำแหน่งวาง:** วางหลัง comment แบบฝึกหัดเดิม; import ต้องอยู่บนสุดของไฟล์

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 8.1 ทำหน้าที่ `import { useEffect, useState } from "react";` เพื่อเก็บและกระจาย State ที่หลาย Component ต้องใช้ร่วมกันโดยไม่ต้องส่ง Props ผ่านทุกชั้น

**โค้ดก้อนนี้ทำงานอย่างไร:** Context กำหนดช่องทางข้อมูล, Provider เป็นเจ้าของ State และฟังก์ชันเปลี่ยนค่า ส่วน custom Hook ช่วยให้ Component อ่าน Context พร้อมตรวจการใช้งานผิดตำแหน่ง

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Component ที่อยู่ใต้ Provider จะอ่านสถานะ Login, User หรือ Theme เดียวกัน และ UI จะอัปเดตเมื่อ State เปลี่ยน

```jsx
import { useEffect, useState } from "react";
```

### 8.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/hooks/AuthProvider.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 8.2 ทำหน้าที่ `import api from "../services/api.js";` เพื่อเก็บและกระจาย State ที่หลาย Component ต้องใช้ร่วมกันโดยไม่ต้องส่ง Props ผ่านทุกชั้น

**โค้ดก้อนนี้ทำงานอย่างไร:** Context กำหนดช่องทางข้อมูล, Provider เป็นเจ้าของ State และฟังก์ชันเปลี่ยนค่า ส่วน custom Hook ช่วยให้ Component อ่าน Context พร้อมตรวจการใช้งานผิดตำแหน่ง

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Component ที่อยู่ใต้ Provider จะอ่านสถานะ Login, User หรือ Theme เดียวกัน และ UI จะอัปเดตเมื่อ State เปลี่ยน

```jsx
import api from "../services/api.js";
```

### 8.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/hooks/AuthProvider.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 8.3 ทำหน้าที่ `import AuthContext from "./authContext.js";` เพื่อเก็บและกระจาย State ที่หลาย Component ต้องใช้ร่วมกันโดยไม่ต้องส่ง Props ผ่านทุกชั้น

**โค้ดก้อนนี้ทำงานอย่างไร:** Context กำหนดช่องทางข้อมูล, Provider เป็นเจ้าของ State และฟังก์ชันเปลี่ยนค่า ส่วน custom Hook ช่วยให้ Component อ่าน Context พร้อมตรวจการใช้งานผิดตำแหน่ง

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Component ที่อยู่ใต้ Provider จะอ่านสถานะ Login, User หรือ Theme เดียวกัน และ UI จะอัปเดตเมื่อ State เปลี่ยน

```jsx
import AuthContext from "./authContext.js";
```

### 8.4 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/hooks/AuthProvider.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 8.4 ทำหน้าที่ `export default function AuthProvider({ children }) {` เพื่อเก็บและกระจาย State ที่หลาย Component ต้องใช้ร่วมกันโดยไม่ต้องส่ง Props ผ่านทุกชั้น

**โค้ดก้อนนี้ทำงานอย่างไร:** Context กำหนดช่องทางข้อมูล, Provider เป็นเจ้าของ State และฟังก์ชันเปลี่ยนค่า ส่วน custom Hook ช่วยให้ Component อ่าน Context พร้อมตรวจการใช้งานผิดตำแหน่ง

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Component ที่อยู่ใต้ Provider จะอ่านสถานะ Login, User หรือ Theme เดียวกัน และ UI จะอัปเดตเมื่อ State เปลี่ยน

```jsx
export default function AuthProvider({ children }) {
  const [user, setUser] = useState(null);
  const [loading, setLoading] = useState(true);

  async function loadUser() {
    const token = localStorage.getItem("token");

    if (!token) {
      setLoading(false);
      return null;
    }
```

### 8.5 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/hooks/AuthProvider.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 8.5 ทำหน้าที่ `try {` เพื่อเก็บและกระจาย State ที่หลาย Component ต้องใช้ร่วมกันโดยไม่ต้องส่ง Props ผ่านทุกชั้น

**โค้ดก้อนนี้ทำงานอย่างไร:** Context กำหนดช่องทางข้อมูล, Provider เป็นเจ้าของ State และฟังก์ชันเปลี่ยนค่า ส่วน custom Hook ช่วยให้ Component อ่าน Context พร้อมตรวจการใช้งานผิดตำแหน่ง

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Component ที่อยู่ใต้ Provider จะอ่านสถานะ Login, User หรือ Theme เดียวกัน และ UI จะอัปเดตเมื่อ State เปลี่ยน

```jsx
    try {
      const response = await api.get("/users/me");
      setUser(response.data.user);
      return response.data.user;
    } catch {
      localStorage.removeItem("token");
      setUser(null);
      return null;
    } finally {
      setLoading(false);
    }
  }
```

### 8.6 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/hooks/AuthProvider.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 8.6 ทำหน้าที่ `useEffect(() => {` เพื่อเก็บและกระจาย State ที่หลาย Component ต้องใช้ร่วมกันโดยไม่ต้องส่ง Props ผ่านทุกชั้น

**โค้ดก้อนนี้ทำงานอย่างไร:** Context กำหนดช่องทางข้อมูล, Provider เป็นเจ้าของ State และฟังก์ชันเปลี่ยนค่า ส่วน custom Hook ช่วยให้ Component อ่าน Context พร้อมตรวจการใช้งานผิดตำแหน่ง

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Component ที่อยู่ใต้ Provider จะอ่านสถานะ Login, User หรือ Theme เดียวกัน และ UI จะอัปเดตเมื่อ State เปลี่ยน

```jsx
  useEffect(() => {
    loadUser();
  }, []);

  async function login(email, password) {
    const response = await api.post("/auth/login", { email, password });
    localStorage.setItem("token", response.data.token);
    return loadUser();
  }
```

### 8.7 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/hooks/AuthProvider.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 8.7 ทำหน้าที่ `function logout() {` เพื่อเก็บและกระจาย State ที่หลาย Component ต้องใช้ร่วมกันโดยไม่ต้องส่ง Props ผ่านทุกชั้น

**โค้ดก้อนนี้ทำงานอย่างไร:** Context กำหนดช่องทางข้อมูล, Provider เป็นเจ้าของ State และฟังก์ชันเปลี่ยนค่า ส่วน custom Hook ช่วยให้ Component อ่าน Context พร้อมตรวจการใช้งานผิดตำแหน่ง

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Component ที่อยู่ใต้ Provider จะอ่านสถานะ Login, User หรือ Theme เดียวกัน และ UI จะอัปเดตเมื่อ State เปลี่ยน

```jsx
function logout() {
    localStorage.removeItem("token");
    setUser(null);
  }

  return <AuthContext.Provider value={{ user, loading, login, logout, reloadUser: loadUser }}>{children}</AuthContext.Provider>;
}
```

เมื่อคัดครบไฟล์ ให้รัน `npm run lint` และแก้ error ของไฟล์นี้ก่อนก้อนถัดไป

## 9. `FrontEnd/src/hooks/useAuth.js`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนตามหมายเลข

### 9.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `FrontEnd/src/hooks/useAuth.js`

**ตำแหน่งวาง:** วางหลัง comment แบบฝึกหัดเดิม; import ต้องอยู่บนสุดของไฟล์

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 9.1 ทำหน้าที่ `import { useContext } from "react";` เพื่อเก็บและกระจาย State ที่หลาย Component ต้องใช้ร่วมกันโดยไม่ต้องส่ง Props ผ่านทุกชั้น

**โค้ดก้อนนี้ทำงานอย่างไร:** Context กำหนดช่องทางข้อมูล, Provider เป็นเจ้าของ State และฟังก์ชันเปลี่ยนค่า ส่วน custom Hook ช่วยให้ Component อ่าน Context พร้อมตรวจการใช้งานผิดตำแหน่ง

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Component ที่อยู่ใต้ Provider จะอ่านสถานะ Login, User หรือ Theme เดียวกัน และ UI จะอัปเดตเมื่อ State เปลี่ยน

```js
import { useContext } from "react";
// ก้อนนี้นำ Module ที่จำเป็นเข้ามา เพื่อให้โค้ดส่วนถัดไปเรียกใช้ความสามารถเหล่านั้น
```

### 9.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/hooks/useAuth.js`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 9.2 ทำหน้าที่ `import AuthContext from "./authContext.js";` เพื่อเก็บและกระจาย State ที่หลาย Component ต้องใช้ร่วมกันโดยไม่ต้องส่ง Props ผ่านทุกชั้น

**โค้ดก้อนนี้ทำงานอย่างไร:** Context กำหนดช่องทางข้อมูล, Provider เป็นเจ้าของ State และฟังก์ชันเปลี่ยนค่า ส่วน custom Hook ช่วยให้ Component อ่าน Context พร้อมตรวจการใช้งานผิดตำแหน่ง

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Component ที่อยู่ใต้ Provider จะอ่านสถานะ Login, User หรือ Theme เดียวกัน และ UI จะอัปเดตเมื่อ State เปลี่ยน

```js
import AuthContext from "./authContext.js";
// นำ Dependency หรือ Module ที่บรรทัดถัดไปต้องใช้เข้ามาในไฟล์
```

### 9.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/hooks/useAuth.js`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 9.3 ทำหน้าที่ `export default function useAuth() {` เพื่อเก็บและกระจาย State ที่หลาย Component ต้องใช้ร่วมกันโดยไม่ต้องส่ง Props ผ่านทุกชั้น

**โค้ดก้อนนี้ทำงานอย่างไร:** Context กำหนดช่องทางข้อมูล, Provider เป็นเจ้าของ State และฟังก์ชันเปลี่ยนค่า ส่วน custom Hook ช่วยให้ Component อ่าน Context พร้อมตรวจการใช้งานผิดตำแหน่ง

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Component ที่อยู่ใต้ Provider จะอ่านสถานะ Login, User หรือ Theme เดียวกัน และ UI จะอัปเดตเมื่อ State เปลี่ยน

```js
export default function useAuth() {
// ประกาศฟังก์ชันนี้และกำหนดขอบเขตงานตามชื่อของฟังก์ชัน
  return useContext(AuthContext);
  // ซ่อนรายละเอียด useContext เพื่อให้ Page เรียกสั้นและอ่านง่าย
}
```

เมื่อคัดครบไฟล์ ให้รัน `npm run lint` และแก้ error ของไฟล์นี้ก่อนก้อนถัดไป

## 10. `FrontEnd/src/hooks/themeContext.js`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนตามหมายเลข

### 10.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `FrontEnd/src/hooks/themeContext.js`

**ตำแหน่งวาง:** วางหลัง comment แบบฝึกหัดเดิม; import ต้องอยู่บนสุดของไฟล์

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 10.1 ทำหน้าที่ `import { createContext } from "react";` เพื่อเก็บและกระจาย State ที่หลาย Component ต้องใช้ร่วมกันโดยไม่ต้องส่ง Props ผ่านทุกชั้น

**โค้ดก้อนนี้ทำงานอย่างไร:** Context กำหนดช่องทางข้อมูล, Provider เป็นเจ้าของ State และฟังก์ชันเปลี่ยนค่า ส่วน custom Hook ช่วยให้ Component อ่าน Context พร้อมตรวจการใช้งานผิดตำแหน่ง

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Component ที่อยู่ใต้ Provider จะอ่านสถานะ Login, User หรือ Theme เดียวกัน และ UI จะอัปเดตเมื่อ State เปลี่ยน

```js
import { createContext } from "react";
// ก้อนนี้นำ Module ที่จำเป็นเข้ามา เพื่อให้โค้ดส่วนถัดไปเรียกใช้ความสามารถเหล่านั้น
```

### 10.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/hooks/themeContext.js`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 10.2 ทำหน้าที่ `const ThemeContext = createContext(null);` เพื่อเก็บและกระจาย State ที่หลาย Component ต้องใช้ร่วมกันโดยไม่ต้องส่ง Props ผ่านทุกชั้น

**โค้ดก้อนนี้ทำงานอย่างไร:** Context กำหนดช่องทางข้อมูล, Provider เป็นเจ้าของ State และฟังก์ชันเปลี่ยนค่า ส่วน custom Hook ช่วยให้ Component อ่าน Context พร้อมตรวจการใช้งานผิดตำแหน่ง

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Component ที่อยู่ใต้ Provider จะอ่านสถานะ Login, User หรือ Theme เดียวกัน และ UI จะอัปเดตเมื่อ State เปลี่ยน

```js
const ThemeContext = createContext(null);
// ค่าเริ่ม null และถูกแทนด้วย { theme, setTheme } ใน ThemeProvider
```

### 10.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/hooks/themeContext.js`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 10.3 ทำหน้าที่ `export default ThemeContext;` เพื่อเก็บและกระจาย State ที่หลาย Component ต้องใช้ร่วมกันโดยไม่ต้องส่ง Props ผ่านทุกชั้น

**โค้ดก้อนนี้ทำงานอย่างไร:** Context กำหนดช่องทางข้อมูล, Provider เป็นเจ้าของ State และฟังก์ชันเปลี่ยนค่า ส่วน custom Hook ช่วยให้ Component อ่าน Context พร้อมตรวจการใช้งานผิดตำแหน่ง

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Component ที่อยู่ใต้ Provider จะอ่านสถานะ Login, User หรือ Theme เดียวกัน และ UI จะอัปเดตเมื่อ State เปลี่ยน

```js
export default ThemeContext;
// Provider และ useTheme ต้องใช้ Context กล่องเดียวกัน
```

เมื่อคัดครบไฟล์ ให้รัน `npm run lint` และแก้ error ของไฟล์นี้ก่อนก้อนถัดไป

## 11. `FrontEnd/src/hooks/ThemeProvider.jsx`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนตามหมายเลข

### 11.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `FrontEnd/src/hooks/ThemeProvider.jsx`

**ตำแหน่งวาง:** วางหลัง comment แบบฝึกหัดเดิม; import ต้องอยู่บนสุดของไฟล์

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 11.1 ทำหน้าที่ `import { useEffect, useState } from "react";` เพื่อเก็บและกระจาย State ที่หลาย Component ต้องใช้ร่วมกันโดยไม่ต้องส่ง Props ผ่านทุกชั้น

**โค้ดก้อนนี้ทำงานอย่างไร:** Context กำหนดช่องทางข้อมูล, Provider เป็นเจ้าของ State และฟังก์ชันเปลี่ยนค่า ส่วน custom Hook ช่วยให้ Component อ่าน Context พร้อมตรวจการใช้งานผิดตำแหน่ง

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Component ที่อยู่ใต้ Provider จะอ่านสถานะ Login, User หรือ Theme เดียวกัน และ UI จะอัปเดตเมื่อ State เปลี่ยน

```jsx
import { useEffect, useState } from "react";
```

### 11.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/hooks/ThemeProvider.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 11.2 ทำหน้าที่ `import ThemeContext from "./themeContext.js";` เพื่อเก็บและกระจาย State ที่หลาย Component ต้องใช้ร่วมกันโดยไม่ต้องส่ง Props ผ่านทุกชั้น

**โค้ดก้อนนี้ทำงานอย่างไร:** Context กำหนดช่องทางข้อมูล, Provider เป็นเจ้าของ State และฟังก์ชันเปลี่ยนค่า ส่วน custom Hook ช่วยให้ Component อ่าน Context พร้อมตรวจการใช้งานผิดตำแหน่ง

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Component ที่อยู่ใต้ Provider จะอ่านสถานะ Login, User หรือ Theme เดียวกัน และ UI จะอัปเดตเมื่อ State เปลี่ยน

```jsx
import ThemeContext from "./themeContext.js";
```

### 11.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/hooks/ThemeProvider.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 11.3 ทำหน้าที่ `ใส่ class dark ที่ <html> เพื่อให้ทุกหน้าเปลี่ยนธีมพร้อมกัน` เพื่อเก็บและกระจาย State ที่หลาย Component ต้องใช้ร่วมกันโดยไม่ต้องส่ง Props ผ่านทุกชั้น

**โค้ดก้อนนี้ทำงานอย่างไร:** Context กำหนดช่องทางข้อมูล, Provider เป็นเจ้าของ State และฟังก์ชันเปลี่ยนค่า ส่วน custom Hook ช่วยให้ Component อ่าน Context พร้อมตรวจการใช้งานผิดตำแหน่ง

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Component ที่อยู่ใต้ Provider จะอ่านสถานะ Login, User หรือ Theme เดียวกัน และ UI จะอัปเดตเมื่อ State เปลี่ยน

```jsx
export default function ThemeProvider({ children }) {
  const [theme, setTheme] = useState(() => localStorage.getItem("theme") || "light");

  useEffect(() => {
    // ใส่ class dark ที่ <html> เพื่อให้ทุกหน้าเปลี่ยนธีมพร้อมกัน
    document.documentElement.classList.toggle("dark", theme === "dark");
    localStorage.setItem("theme", theme);
  }, [theme]);

  return (
    <ThemeContext.Provider value={{ theme, setTheme }}>
      {children}
    </ThemeContext.Provider>
  );
}
```

เมื่อคัดครบไฟล์ ให้รัน `npm run lint` และแก้ error ของไฟล์นี้ก่อนก้อนถัดไป

## 12. `FrontEnd/src/hooks/useTheme.js`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนตามหมายเลข

### 12.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `FrontEnd/src/hooks/useTheme.js`

**ตำแหน่งวาง:** วางหลัง comment แบบฝึกหัดเดิม; import ต้องอยู่บนสุดของไฟล์

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 12.1 ทำหน้าที่ `import { useContext } from "react";` เพื่อเก็บและกระจาย State ที่หลาย Component ต้องใช้ร่วมกันโดยไม่ต้องส่ง Props ผ่านทุกชั้น

**โค้ดก้อนนี้ทำงานอย่างไร:** Context กำหนดช่องทางข้อมูล, Provider เป็นเจ้าของ State และฟังก์ชันเปลี่ยนค่า ส่วน custom Hook ช่วยให้ Component อ่าน Context พร้อมตรวจการใช้งานผิดตำแหน่ง

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Component ที่อยู่ใต้ Provider จะอ่านสถานะ Login, User หรือ Theme เดียวกัน และ UI จะอัปเดตเมื่อ State เปลี่ยน

```js
import { useContext } from "react";
// ก้อนนี้นำ Module ที่จำเป็นเข้ามา เพื่อให้โค้ดส่วนถัดไปเรียกใช้ความสามารถเหล่านั้น
```

### 12.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/hooks/useTheme.js`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 12.2 ทำหน้าที่ `import ThemeContext from "./themeContext.js";` เพื่อเก็บและกระจาย State ที่หลาย Component ต้องใช้ร่วมกันโดยไม่ต้องส่ง Props ผ่านทุกชั้น

**โค้ดก้อนนี้ทำงานอย่างไร:** Context กำหนดช่องทางข้อมูล, Provider เป็นเจ้าของ State และฟังก์ชันเปลี่ยนค่า ส่วน custom Hook ช่วยให้ Component อ่าน Context พร้อมตรวจการใช้งานผิดตำแหน่ง

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Component ที่อยู่ใต้ Provider จะอ่านสถานะ Login, User หรือ Theme เดียวกัน และ UI จะอัปเดตเมื่อ State เปลี่ยน

```js
import ThemeContext from "./themeContext.js";
// นำ Dependency หรือ Module ที่บรรทัดถัดไปต้องใช้เข้ามาในไฟล์
```

### 12.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/hooks/useTheme.js`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 12.3 ทำหน้าที่ `export default function useTheme() {` เพื่อเก็บและกระจาย State ที่หลาย Component ต้องใช้ร่วมกันโดยไม่ต้องส่ง Props ผ่านทุกชั้น

**โค้ดก้อนนี้ทำงานอย่างไร:** Context กำหนดช่องทางข้อมูล, Provider เป็นเจ้าของ State และฟังก์ชันเปลี่ยนค่า ส่วน custom Hook ช่วยให้ Component อ่าน Context พร้อมตรวจการใช้งานผิดตำแหน่ง

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Component ที่อยู่ใต้ Provider จะอ่านสถานะ Login, User หรือ Theme เดียวกัน และ UI จะอัปเดตเมื่อ State เปลี่ยน

```js
export default function useTheme() {
// ประกาศฟังก์ชันนี้และกำหนดขอบเขตงานตามชื่อของฟังก์ชัน
  return useContext(ThemeContext);
  // คืน { theme, setTheme } ที่ ThemeProvider ส่งไว้
}
```

เมื่อคัดครบไฟล์ ให้รัน `npm run lint` และแก้ error ของไฟล์นี้ก่อนก้อนถัดไป

## 13. `FrontEnd/src/components/ui.jsx`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนตามหมายเลข

### 13.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `FrontEnd/src/components/ui.jsx`

**ตำแหน่งวาง:** วางหลัง comment แบบฝึกหัดเดิม; import ต้องอยู่บนสุดของไฟล์

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 13.1 ทำหน้าที่ `import { Eye, EyeOff, LoaderCircle } from "lucide-react";` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
import { Eye, EyeOff, LoaderCircle } from "lucide-react";
```

### 13.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/components/ui.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 13.2 ทำหน้าที่ `import { useState } from "react";` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
import { useState } from "react";
```

### 13.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/components/ui.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 13.3 ทำหน้าที่ `export function Button({ children, variant = "primary", className = "", ...props }) {` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
export function Button({ children, variant = "primary", className = "", ...props }) {
  const styles = {
    primary: "bg-brand text-white hover:bg-brand-dark",
    success: "bg-leaf text-white hover:bg-green-700",
    danger: "bg-danger text-white hover:bg-red-600",
    outline: "border border-cream-200 bg-white text-ink hover:bg-cream-50",
    ghost: "text-brand hover:bg-orange-50",
  };

  return (
    <button
      className={`inline-flex min-h-11 items-center justify-center gap-2 rounded-xl px-4 font-semibold transition disabled:cursor-not-allowed disabled:opacity-50 ${styles[variant]} ${className}`}
      {...props}
    >
      {children}
    </button>
  );
}
```

### 13.4 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/components/ui.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 13.4 ทำหน้าที่ `export function Field({ label, error, className = "", type = "text", ...props }) {` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
export function Field({ label, error, className = "", type = "text", ...props }) {
  const [showPassword, setShowPassword] = useState(false);
  const isPassword = type === "password";

  return (
    <label className={`block ${className}`}>
      <span className="mb-1.5 block text-sm text-stone-600">{label}</span>
      <span className="relative block">
        <input
          className={`w-full rounded-xl border border-cream-200 bg-white px-4 py-3 outline-none transition focus:border-brand focus:ring-2 focus:ring-orange-100 ${isPassword ? "pr-12" : ""}`}
          type={isPassword && showPassword ? "text" : type}
          {...props}
        />
        {isPassword && (
          <button
            aria-label={showPassword ? "ซ่อนรหัสผ่าน" : "แสดงรหัสผ่าน"}
            className="absolute inset-y-0 right-0 grid w-12 place-items-center text-stone-400 transition hover:text-brand"
            onClick={() => setShowPassword((current) => !current)}
            type="button"
          >
            {showPassword ? <EyeOff size={19} /> : <Eye size={19} />}
          </button>
        )}
      </span>
      {error && <span className="mt-1 block text-xs text-danger">{error}</span>}
    </label>
  );
}
```

### 13.5 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/components/ui.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 13.5 ทำหน้าที่ `export function Textarea({ label, className = "", ...props }) {` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
export function Textarea({ label, className = "", ...props }) {
  return (
    <label className={`block ${className}`}>
      <span className="mb-1.5 block text-sm text-stone-600">{label}</span>
      <textarea
        className="min-h-28 w-full resize-none rounded-xl border border-cream-200 bg-white px-4 py-3 outline-none transition focus:border-brand focus:ring-2 focus:ring-orange-100"
        {...props}
      />
    </label>
  );
}
```

### 13.6 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/components/ui.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 13.6 ทำหน้าที่ `export function Chip({ active, children, ...props }) {` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
export function Chip({ active, children, ...props }) {
  return (
    <button
      className={`rounded-full border px-4 py-2 text-sm transition ${
        active
          ? "border-orange-200 bg-orange-100 font-semibold text-brand-dark"
          : "border-cream-200 bg-white text-stone-600"
      }`}
      {...props}
    >
      {children}
    </button>
  );
}
```

### 13.7 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/components/ui.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 13.7 ทำหน้าที่ `export function PageLoader() {` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
export function PageLoader() {
  return (
    <div className="grid min-h-64 place-items-center">
      <LoaderCircle className="animate-spin text-brand" size={32} />
    </div>
  );
}
```

### 13.8 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/components/ui.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 13.8 ทำหน้าที่ `export function EmptyState({ title, description }) {` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
export function EmptyState({ title, description }) {
  return (
    <div className="rounded-2xl border border-dashed border-cream-200 bg-white/70 p-8 text-center">
      <p className="font-semibold">{title}</p>
      {description && <p className="mt-1 text-sm text-stone-500">{description}</p>}
    </div>
  );
}
```

เมื่อคัดครบไฟล์ ให้รัน `npm run lint` และแก้ error ของไฟล์นี้ก่อนก้อนถัดไป

## 14. `FrontEnd/src/components/BrandMark.jsx`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนตามหมายเลข

### 14.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `FrontEnd/src/components/BrandMark.jsx`

**ตำแหน่งวาง:** วางหลัง comment แบบฝึกหัดเดิม; import ต้องอยู่บนสุดของไฟล์

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 14.1 ทำหน้าที่ `export default function BrandMark({ compact = false }) {` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
export default function BrandMark({ compact = false }) {
  return (
    <div className="text-center">
      <img
        className={`mx-auto object-contain drop-shadow-lg ${compact ? "h-32 w-32" : "h-52 w-52"}`}
        src="/trash-toilet-logo.png"
        alt="Trash & Toilet Map"
      />
      {!compact && <p className="auth-brand-text -mt-3 text-sm font-medium text-stone-600">ค้นหาจุดสะอาด เที่ยวญี่ปุ่นสบายใจ</p>}
    </div>
  );
}
```

เมื่อคัดครบไฟล์ ให้รัน `npm run lint` และแก้ error ของไฟล์นี้ก่อนก้อนถัดไป

## 15. `FrontEnd/src/components/ProtectedRoute.jsx`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนตามหมายเลข

### 15.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `FrontEnd/src/components/ProtectedRoute.jsx`

**ตำแหน่งวาง:** วางหลัง comment แบบฝึกหัดเดิม; import ต้องอยู่บนสุดของไฟล์

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 15.1 ทำหน้าที่ `import { Navigate, Outlet } from "react-router-dom";` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
import { Navigate, Outlet } from "react-router-dom";
```

### 15.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/components/ProtectedRoute.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 15.2 ทำหน้าที่ `import useAuth from "../hooks/useAuth.js";` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
import useAuth from "../hooks/useAuth.js";
```

### 15.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/components/ProtectedRoute.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 15.3 ทำหน้าที่ `import { PageLoader } from "./ui.jsx";` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
import { PageLoader } from "./ui.jsx";
```

### 15.4 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/components/ProtectedRoute.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 15.4 ทำหน้าที่ `export default function ProtectedRoute({ adminOnly = false }) {` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
export default function ProtectedRoute({ adminOnly = false }) {
  const { user, loading } = useAuth();

  if (loading) return <PageLoader />;
  if (!user) return <Navigate to="/login" replace />;
  if (adminOnly && user.role !== "admin") return <Navigate to="/" replace />;

  return <Outlet />;
}
```

เมื่อคัดครบไฟล์ ให้รัน `npm run lint` และแก้ error ของไฟล์นี้ก่อนก้อนถัดไป

## 16. `FrontEnd/src/components/PageHeader.jsx`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนตามหมายเลข

### 16.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `FrontEnd/src/components/PageHeader.jsx`

**ตำแหน่งวาง:** วางหลัง comment แบบฝึกหัดเดิม; import ต้องอยู่บนสุดของไฟล์

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 16.1 ทำหน้าที่ `import { ArrowLeft } from "lucide-react";` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
import { ArrowLeft } from "lucide-react";
```

### 16.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/components/PageHeader.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 16.2 ทำหน้าที่ `import { useNavigate } from "react-router-dom";` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
import { useNavigate } from "react-router-dom";
```

### 16.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/components/PageHeader.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 16.3 ทำหน้าที่ `export default function PageHeader({ title, subtitle, back = false }) {` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
export default function PageHeader({ title, subtitle, back = false }) {
  const navigate = useNavigate();

  return (
    <header className="sticky top-0 z-[900] border-b border-cream-100 bg-cream-50/95 px-5 py-4 text-ink backdrop-blur dark:border-stone-700 dark:bg-[#25221f]/95 dark:text-[#f0e9df]">
      <div className="flex items-center gap-3">
        {back && (
          <button aria-label="ย้อนกลับ" onClick={() => navigate(-1)}>
            <ArrowLeft size={22} />
          </button>
        )}
        <div>
          <h1 className="text-lg font-bold">{title}</h1>
          {subtitle && <p className="text-xs text-stone-500 dark:text-[#cfc7bc]">{subtitle}</p>}
        </div>
      </div>
    </header>
  );
}
```

เมื่อคัดครบไฟล์ ให้รัน `npm run lint` และแก้ error ของไฟล์นี้ก่อนก้อนถัดไป

## 17. `FrontEnd/src/components/BottomNav.jsx`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนตามหมายเลข

### 17.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `FrontEnd/src/components/BottomNav.jsx`

**ตำแหน่งวาง:** วางหลัง comment แบบฝึกหัดเดิม; import ต้องอยู่บนสุดของไฟล์

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 17.1 ทำหน้าที่ `import { ClipboardList, FileWarning, House, MapPinPlus, Search, UserRound } from "lucide-react";` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
import { ClipboardList, FileWarning, House, MapPinPlus, Search, UserRound } from "lucide-react";
```

### 17.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/components/BottomNav.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 17.2 ทำหน้าที่ `import { NavLink } from "react-router-dom";` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
import { NavLink } from "react-router-dom";
```

### 17.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/components/BottomNav.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 17.3 ทำหน้าที่ `import useAuth from "../hooks/useAuth.js";` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
import useAuth from "../hooks/useAuth.js";
```

### 17.4 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/components/BottomNav.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 17.4 ทำหน้าที่ `export default function BottomNav() {` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
export default function BottomNav() {
  const { user } = useAuth();
  const links = user?.role === "admin"
    ? [
        { to: "/admin/add-place", label: "เพิ่ม", icon: MapPinPlus },
        { to: "/admin/places", label: "สถานที่", icon: Search },
        { to: "/admin/pending", label: "อนุมัติ", icon: ClipboardList },
        { to: "/admin/reports", label: "รายงาน", icon: FileWarning },
        { to: "/profile", label: "โปรไฟล์", icon: UserRound },
      ]
    : [
        { to: "/", label: "หน้าหลัก", icon: House },
        { to: "/search", label: "ค้นหา", icon: Search },
        { to: "/add-place", label: "เพิ่ม", icon: MapPinPlus },
        { to: "/profile", label: "โปรไฟล์", icon: UserRound },
      ];
```

### 17.5 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/components/BottomNav.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 17.5 ทำหน้าที่ `return (` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
return (
    <nav className={`sticky bottom-0 z-[1000] grid border-t border-cream-200 bg-white/95 backdrop-blur ${links.length === 5 ? "grid-cols-5" : "grid-cols-4"}`}>
      {links.map(({ to, label, icon: Icon }) => (
        <NavLink
          key={to}
          to={to}
          end={to === "/"}
          className={({ isActive }) =>
            `flex min-h-16 flex-col items-center justify-center gap-1 text-[11px] ${isActive ? "text-brand" : "text-stone-400"}`
          }
        >
          <Icon size={20} />
          {label}
        </NavLink>
      ))}
    </nav>
  );
}
```

เมื่อคัดครบไฟล์ ให้รัน `npm run lint` และแก้ error ของไฟล์นี้ก่อนก้อนถัดไป

## 18. `FrontEnd/src/layouts/AppLayout.jsx`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนตามหมายเลข

### 18.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `FrontEnd/src/layouts/AppLayout.jsx`

**ตำแหน่งวาง:** วางหลัง comment แบบฝึกหัดเดิม; import ต้องอยู่บนสุดของไฟล์

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 18.1 ทำหน้าที่ `import { Outlet } from "react-router-dom";` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
import { Outlet } from "react-router-dom";
```

### 18.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/layouts/AppLayout.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 18.2 ทำหน้าที่ `import BottomNav from "../components/BottomNav.jsx";` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
import BottomNav from "../components/BottomNav.jsx";
```

### 18.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/layouts/AppLayout.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 18.3 ทำหน้าที่ `export default function AppLayout() {` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
export default function AppLayout() {
  return (
    <div className="mx-auto min-h-screen max-w-md bg-cream-50 shadow-2xl md:my-6 md:min-h-[calc(100vh-3rem)] md:overflow-hidden md:rounded-[2rem]">
      <main className="min-h-[calc(100vh-4rem)] md:min-h-[calc(100vh-7rem)]">
        <Outlet />
      </main>
      <BottomNav />
    </div>
  );
}
```

เมื่อคัดครบไฟล์ ให้รัน `npm run lint` และแก้ error ของไฟล์นี้ก่อนก้อนถัดไป

## 19. `FrontEnd/src/services/locationService.js`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนตามหมายเลข

### 19.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `FrontEnd/src/services/locationService.js`

**ตำแหน่งวาง:** วางหลัง comment แบบฝึกหัดเดิม; import ต้องอยู่บนสุดของไฟล์

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 19.1 ทำหน้าที่ `export async function reverseGeocode(latitude, longitude, signal) {` เพื่อแยกการอ่านตำแหน่งของ Browser ออกจาก Component

**โค้ดก้อนนี้ทำงานอย่างไร:** Service ครอบ Geolocation API และแปลง callback เป็นผลลัพธ์ที่หน้าเว็บเรียกใช้ได้ รวมทั้งส่ง Error เมื่อผู้ใช้ไม่อนุญาตหรืออุปกรณ์อ่านตำแหน่งไม่ได้

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** หน้า Map/Search สามารถขอตำแหน่งปัจจุบันและนำ latitude/longitude ไปค้นหาหรือแสดงบนแผนที่ได้

```js
export async function reverseGeocode(latitude, longitude, signal) {
// แปลง latitude/longitude เป็นที่อยู่ด้วย OpenStreetMap Nominatim | PlaceForm เรียกหลังผู้ใช้กดใช้ตำแหน่งปัจจุบัน แล้วนำผลไปเติม Address
  const params = new URLSearchParams({
  // URLSearchParams ช่วย encode Query String ให้ปลอดภัย
    format: "jsonv2",
    lat: String(latitude),
    lon: String(longitude),
    "accept-language": "th,en",
  });

  const response = await fetch(
  // signal มาจาก AbortController เพื่อยกเลิก Request เก่าที่ไม่ใช้แล้ว
    `https://nominatim.openstreetmap.org/reverse?${params.toString()}`,
    { signal }
  );

  if (!response.ok) {
  // ตรวจเงื่อนไขก่อนอนุญาตให้โค้ดภายในทำงาน
    throw new Error("ไม่สามารถค้นหาที่อยู่ได้");
    // หยุดงานปัจจุบันและส่ง Error ให้ catch หรือผู้เรียกจัดการ
  }

  const data = await response.json();
  // Nominatim คืน JSON และ display_name คือที่อยู่แบบเต็ม
  return data.display_name || "";
  // ส่งผลลัพธ์ออกจากฟังก์ชันและหยุดทำบรรทัดถัดไปในฟังก์ชันนี้
}
```

เมื่อคัดครบไฟล์ ให้รัน `npm run lint` และแก้ error ของไฟล์นี้ก่อนก้อนถัดไป

## 20. `FrontEnd/src/components/PlaceCard.jsx`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนตามหมายเลข

### 20.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `FrontEnd/src/components/PlaceCard.jsx`

**ตำแหน่งวาง:** วางหลัง comment แบบฝึกหัดเดิม; import ต้องอยู่บนสุดของไฟล์

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 20.1 ทำหน้าที่ `import { MapPin, Trash2, Toilet } from "lucide-react";` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
import { MapPin, Trash2, Toilet } from "lucide-react";
```

### 20.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/components/PlaceCard.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 20.2 ทำหน้าที่ `import { Link } from "react-router-dom";` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
import { Link } from "react-router-dom";
```

### 20.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/components/PlaceCard.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 20.3 ทำหน้าที่ `import { getAssetUrl } from "../services/api.js";` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
import { getAssetUrl } from "../services/api.js";
```

### 20.4 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/components/PlaceCard.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 20.4 ทำหน้าที่ `export default function PlaceCard({ place, action }) {` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
export default function PlaceCard({ place, action }) {
  const Icon = place.type === "toilet" ? Toilet : Trash2;
  return (
    <article className="flex gap-3 rounded-2xl bg-white p-3 shadow-sm">
      <div className={`grid h-16 w-16 shrink-0 place-items-center overflow-hidden rounded-xl ${place.type === "toilet" ? "bg-green-100 text-leaf" : "bg-orange-100 text-brand"}`}>{place.imageUrl ? <img className="h-full w-full object-cover" src={getAssetUrl(place.imageUrl)} alt={place.name} /> : <Icon size={28} />}</div>
      <div className="min-w-0 flex-1"><h3 className="truncate font-semibold">{place.name}</h3><p className="mt-1 flex items-center gap-1 truncate text-xs text-stone-500"><MapPin size={13} />{place.address}</p><div className="mt-2 flex items-center justify-between"><span className="text-xs text-stone-400">{place.distance != null ? `${place.distance.toFixed(2)} km` : place.type}</span>{action || <Link className="rounded-full bg-leaf px-3 py-1 text-xs text-white" to={`/places/${place.id}`}>รายละเอียด</Link>}</div></div>
    </article>
  );
}
```

เมื่อคัดครบไฟล์ ให้รัน `npm run lint` และแก้ error ของไฟล์นี้ก่อนก้อนถัดไป

## 21. `FrontEnd/src/components/PlaceMap.jsx`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนตามหมายเลข

### 21.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `FrontEnd/src/components/PlaceMap.jsx`

**ตำแหน่งวาง:** วางหลัง comment แบบฝึกหัดเดิม; import ต้องอยู่บนสุดของไฟล์

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 21.1 ทำหน้าที่ `import L from "leaflet";` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
import L from "leaflet";
```

### 21.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/components/PlaceMap.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 21.2 ทำหน้าที่ `import { Trash2, Toilet } from "lucide-react";` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
import { Trash2, Toilet } from "lucide-react";
```

### 21.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/components/PlaceMap.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 21.3 ทำหน้าที่ `import { MapContainer, Marker, Popup, TileLayer, useMap } from "react-leaflet";` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
import { MapContainer, Marker, Popup, TileLayer, useMap } from "react-leaflet";
```

### 21.4 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/components/PlaceMap.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 21.4 ทำหน้าที่ `import { createRoot } from "react-dom/client";` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
import { createRoot } from "react-dom/client";
```

### 21.5 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/components/PlaceMap.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 21.5 ทำหน้าที่ `import { Link } from "react-router-dom";` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
import { Link } from "react-router-dom";
```

### 21.6 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/components/PlaceMap.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 21.6 ทำหน้าที่ `import { getAssetUrl } from "../services/api.js";` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
import { getAssetUrl } from "../services/api.js";
```

### 21.7 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/components/PlaceMap.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 21.7 ทำหน้าที่ `function RecenterMap({ center }) {` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
function RecenterMap({ center }) {
  const map = useMap();
  map.setView(center, map.getZoom());
  return null;
}
```

### 21.8 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/components/PlaceMap.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 21.8 ทำหน้าที่ `function markerIcon(type) {` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
function markerIcon(type) {
  const color = type === "toilet" ? "#3f7ee8" : "#ff8518";
  const Icon = type === "toilet" ? Toilet : Trash2;
  const iconElement = document.createElement("div");
  iconElement.style.cssText = `display:grid;place-items:center;width:38px;height:38px;border-radius:10px;background:${color};border:3px solid white;box-shadow:0 4px 12px rgba(63,56,47,.28)`;
  createRoot(iconElement).render(<Icon color="white" size={23} strokeWidth={2.5} />);

  return L.divIcon({
    className: "",
    html: iconElement,
    iconSize: [38, 38],
    iconAnchor: [19, 38],
    popupAnchor: [0, -38],
  });
}
```

### 21.9 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/components/PlaceMap.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 21.9 ทำหน้าที่ `const userIcon = L.divIcon({ className: "", html: '<div class="map-dot" style="width:18px;height:18px;background:#4faf6a"></div...` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
const userIcon = L.divIcon({ className: "", html: '<div class="map-dot" style="width:18px;height:18px;background:#4faf6a"></div>', iconSize: [18, 18], iconAnchor: [9, 9] });
```

### 21.10 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/components/PlaceMap.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 21.10 ทำหน้าที่ `export default function PlaceMap({ places, position }) {` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
export default function PlaceMap({ places, position }) {
  return (
    <MapContainer center={position} zoom={14} zoomControl={false}>
      <TileLayer attribution='&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a>' url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png" />
      {position && <><RecenterMap center={position} /><Marker position={position} icon={userIcon}><Popup>ตำแหน่งของคุณ</Popup></Marker></>}
      {places.map((place) => (
        <Marker key={place.id} position={[place.latitude, place.longitude]} icon={markerIcon(place.type)}>
          <Popup><div className="min-w-40">{place.imageUrl && <img className="mb-2 h-20 w-full rounded-lg object-cover" src={getAssetUrl(place.imageUrl)} alt={place.name} />}<strong>{place.name}</strong><p>{place.type === "toilet" ? "ห้องน้ำ" : "ถังขยะ"}</p><p>{place.address}</p><Link className="font-semibold text-orange-600" to={`/places/${place.id}`}>ดูรายละเอียด</Link></div></Popup>
        </Marker>
      ))}
    </MapContainer>
  );
}
```

เมื่อคัดครบไฟล์ ให้รัน `npm run lint` และแก้ error ของไฟล์นี้ก่อนก้อนถัดไป

## 22. `FrontEnd/src/components/PlaceForm.jsx`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนตามหมายเลข

### 22.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `FrontEnd/src/components/PlaceForm.jsx`

**ตำแหน่งวาง:** วางหลัง comment แบบฝึกหัดเดิม; import ต้องอยู่บนสุดของไฟล์

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 22.1 ทำหน้าที่ `import { Camera, LocateFixed } from "lucide-react";` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
import { Camera, LocateFixed } from "lucide-react";
```

### 22.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/components/PlaceForm.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 22.2 ทำหน้าที่ `import { useEffect, useState } from "react";` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
import { useEffect, useState } from "react";
```

### 22.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/components/PlaceForm.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 22.3 ทำหน้าที่ `import { useGeolocated } from "react-geolocated";` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
import { useGeolocated } from "react-geolocated";
```

### 22.4 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/components/PlaceForm.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 22.4 ทำหน้าที่ `import { Button, Chip, Field } from "./ui.jsx";` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
import { Button, Chip, Field } from "./ui.jsx";
```

### 22.5 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/components/PlaceForm.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 22.5 ทำหน้าที่ `import PlaceMap from "./PlaceMap.jsx";` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
import PlaceMap from "./PlaceMap.jsx";
```

### 22.6 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/components/PlaceForm.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 22.6 ทำหน้าที่ `import { reverseGeocode } from "../services/locationService.js";` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
import { reverseGeocode } from "../services/locationService.js";
```

### 22.7 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/components/PlaceForm.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 22.7 ทำหน้าที่ `export default function PlaceForm({ onSubmit, submitLabel = "เพิ่มสถานที่" }) {` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
export default function PlaceForm({ onSubmit, submitLabel = "เพิ่มสถานที่" }) {
  const [form, setForm] = useState({ name: "", address: "", type: "toilet", latitude: "", longitude: "" });
  const [image, setImage] = useState(null);
  const [imagePreview, setImagePreview] = useState("");
  const [imageError, setImageError] = useState("");
  const [addressLoading, setAddressLoading] = useState(false);
  const [locationError, setLocationError] = useState("");
  const { coords, getPosition, isGeolocationEnabled } = useGeolocated({ positionOptions: { enableHighAccuracy: true }, userDecisionTimeout: 10000, suppressLocationOnMount: true });
```

### 22.8 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/components/PlaceForm.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 22.8 ทำหน้าที่ `ใส่พิกัดทันที แล้วค่อยเติมที่อยู่เมื่อ Nominatim ตอบกลับ` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
  useEffect(() => {
    if (!coords) return;

    const controller = new AbortController();
    setAddressLoading(true);
    setLocationError("");

    // ใส่พิกัดทันที แล้วค่อยเติมที่อยู่เมื่อ Nominatim ตอบกลับ
    setForm((current) => ({
      ...current,
      latitude: coords.latitude,
      longitude: coords.longitude,
    }));
```

### 22.9 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/components/PlaceForm.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 22.9 ทำหน้าที่ `reverseGeocode(coords.latitude, coords.longitude, controller.signal)` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
    reverseGeocode(coords.latitude, coords.longitude, controller.signal)
      .then((address) => {
        setForm((current) => ({ ...current, address }));
      })
      .catch((error) => {
        if (error.name !== "AbortError") {
          setLocationError("ได้พิกัดแล้ว แต่ค้นหาที่อยู่ไม่สำเร็จ กรุณากรอกเอง");
        }
      })
      .finally(() => setAddressLoading(false));
```

### 22.10 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/components/PlaceForm.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 22.10 ทำหน้าที่ `react-geolocated อัปเดต coords หลังผู้ใช้อนุญาต จึงมีปุ่มอีกครั้งเพื่อใส่ค่าล่าสุด` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
    return () => controller.abort();
  }, [coords]);

  function requestCurrentPosition() {
    getPosition();
    if (coords) setForm((current) => ({ ...current, latitude: coords.latitude, longitude: coords.longitude }));
  }

  // react-geolocated อัปเดต coords หลังผู้ใช้อนุญาต จึงมีปุ่มอีกครั้งเพื่อใส่ค่าล่าสุด
  function applyCoordinates() {
    // ขอพิกัดใหม่ทุกครั้งที่กด เผื่อผู้ใช้เคลื่อนที่จากตำแหน่งเดิม
    requestCurrentPosition();
```

### 22.11 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/components/PlaceForm.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 22.11 ทำหน้าที่ `if (coords) {` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
    if (coords) {
      setForm((current) => ({ ...current, latitude: coords.latitude, longitude: coords.longitude }));
    }
  }

  function chooseImage(event) {
    const selectedImage = event.target.files?.[0];
    if (!selectedImage) return;
```

### 22.12 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/components/PlaceForm.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 22.12 ทำหน้าที่ `setImage(selectedImage);` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
    setImage(selectedImage);
    setImagePreview(URL.createObjectURL(selectedImage));
    setImageError("");
  }

  function handleSubmit(event) {
    event.preventDefault();

    if (!image) {
      setImageError("กรุณาถ่ายหรือเลือกรูปสถานที่");
      return;
    }
```

### 22.13 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/components/PlaceForm.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 22.13 ทำหน้าที่ `onSubmit({ ...form, image });` เพื่อสร้าง UI ที่นำกลับมาใช้ซ้ำและแยกหน้าที่การแสดงผลออกจาก Page หลัก

**โค้ดก้อนนี้ทำงานอย่างไร:** Component รับข้อมูลผ่าน props หรือ Context สร้าง JSX และผูก event handler เมื่อ State/props เปลี่ยน React จะ render ส่วนที่เกี่ยวข้องใหม่

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** Page สามารถเรียก Component นี้เพื่อแสดงข้อมูลและรับการโต้ตอบโดยไม่ต้องเขียนโครงสร้าง UI เดิมซ้ำ

```jsx
onSubmit({ ...form, image });
  }

  return (
    <form className="space-y-4" onSubmit={handleSubmit}>
      <div className="relative h-44 overflow-hidden rounded-2xl bg-cream-100 text-center">
        {coords && (
          <PlaceMap
            places={[]}
            position={[coords.latitude, coords.longitude]}
          />
        )}
        <button
          type="button"
          className="absolute bottom-3 left-1/2 z-[800] flex -translate-x-1/2 items-center gap-2 whitespace-nowrap rounded-full bg-white px-4 py-2 text-sm font-semibold text-brand shadow-lg"
          onClick={applyCoordinates}
        >
          <LocateFixed size={17} />
          {addressLoading ? "กำลังค้นหาที่อยู่..." : coords ? "ตำแหน่งปัจจุบัน" : "ค้นหาตำแหน่งปัจจุบัน"}
        </button>
      </div>
      {!isGeolocationEnabled && <p className="text-sm text-danger">อุปกรณ์นี้ปิดการใช้งาน GPS</p>}
      {locationError && <p className="text-sm text-danger">{locationError}</p>}
      <div>
        <span className="mb-2 block text-sm text-stone-600">รูปสถานที่</span>
        <label className="relative grid min-h-40 cursor-pointer place-items-center overflow-hidden rounded-2xl border border-dashed border-orange-200 bg-white text-center text-brand">
          {imagePreview ? (
            <img className="h-48 w-full object-cover" src={imagePreview} alt="ตัวอย่างรูปสถานที่" />
          ) : (
            <span><Camera className="mx-auto" size={28} /><span className="mt-2 block text-sm font-semibold">ถ่ายรูปหรือเลือกรูป</span><span className="mt-1 block text-xs text-stone-400">รองรับรูปไม่เกิน 5 MB</span></span>
          )}
          <input className="sr-only" type="file" accept="image/*" capture="environment" onChange={chooseImage} />
        </label>
        {imageError && <p className="mt-1 text-sm text-danger">{imageError}</p>}
      </div>
      <Field label="ชื่อสถานที่" placeholder="กรอกชื่อสถานที่" value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} />
      <div><span className="mb-2 block text-sm text-stone-600">ประเภท</span><div className="flex gap-2"><Chip type="button" active={form.type === "toilet"} onClick={() => setForm({ ...form, type: "toilet" })}>ห้องน้ำ</Chip><Chip type="button" active={form.type === "trash"} onClick={() => setForm({ ...form, type: "trash" })}>ถังขยะ</Chip></div></div>
      <Field label="ที่อยู่" placeholder="กรอกที่อยู่" value={form.address} onChange={(e) => setForm({ ...form, address: e.target.value })} />
      <div className="grid grid-cols-2 gap-3"><Field label="Latitude" type="number" step="any" value={form.latitude} onChange={(e) => setForm({ ...form, latitude: e.target.value })} /><Field label="Longitude" type="number" step="any" value={form.longitude} onChange={(e) => setForm({ ...form, longitude: e.target.value })} /></div>
      <Button className="w-full">{submitLabel}</Button>
    </form>
  );
}
```

เมื่อคัดครบไฟล์ ให้รัน `npm run lint` และแก้ error ของไฟล์นี้ก่อนก้อนถัดไป

## 23. `FrontEnd/src/pages/RegisterPage.jsx`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนตามหมายเลข

### 23.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/RegisterPage.jsx`

**ตำแหน่งวาง:** วางหลัง comment แบบฝึกหัดเดิม; import ต้องอยู่บนสุดของไฟล์

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 23.1 ทำหน้าที่ `import { useState } from "react";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import { useState } from "react";
```

### 23.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/RegisterPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 23.2 ทำหน้าที่ `import { Link, useNavigate } from "react-router-dom";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import { Link, useNavigate } from "react-router-dom";
```

### 23.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/RegisterPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 23.3 ทำหน้าที่ `import BrandMark from "../components/BrandMark.jsx";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import BrandMark from "../components/BrandMark.jsx";
```

### 23.4 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/RegisterPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 23.4 ทำหน้าที่ `import { Button, Field } from "../components/ui.jsx";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import { Button, Field } from "../components/ui.jsx";
```

### 23.5 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/RegisterPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 23.5 ทำหน้าที่ `import api, { getErrorMessage } from "../services/api.js";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import api, { getErrorMessage } from "../services/api.js";
```

### 23.6 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/RegisterPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 23.6 ทำหน้าที่ `export default function RegisterPage() {` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
export default function RegisterPage() {
  const navigate = useNavigate();
  const [form, setForm] = useState({ name: "", email: "", password: "", confirmPassword: "" });
  const [error, setError] = useState("");
  const [submitting, setSubmitting] = useState(false);

  async function handleSubmit(event) {
    event.preventDefault();
    if (form.password !== form.confirmPassword) {
      setError("รหัสผ่านไม่ตรงกัน");
      return;
    }
```

### 23.7 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/RegisterPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 23.7 ทำหน้าที่ `setSubmitting(true);` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
    setSubmitting(true);
    setError("");
    try {
      await api.post("/auth/register", { name: form.name, email: form.email, password: form.password });
      navigate("/login", { state: { message: "สมัครสมาชิกสำเร็จ กรุณาเข้าสู่ระบบ" } });
    } catch (requestError) {
      setError(getErrorMessage(requestError));
    } finally {
      setSubmitting(false);
    }
  }
```

### 23.8 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/RegisterPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 23.8 ทำหน้าที่ `return (` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
return (
    <div className="relative mx-auto min-h-screen max-w-md overflow-hidden bg-cream-50 px-5 py-6 shadow-2xl">
      <div className="absolute inset-0 bg-[url('/auth-background.png')] bg-cover bg-center" />
      <div className="absolute inset-0 bg-white/15" />
      <div className="relative"><BrandMark compact /></div>
      <div className="auth-panel relative -mt-2 rounded-3xl bg-cream-50/88 p-5 shadow-lg backdrop-blur-[2px]">
      <h1 className="text-2xl font-bold">สมัครสมาชิก</h1>
      <p className="text-sm text-stone-500">สร้างบัญชีเพื่อร่วมแบ่งปันสถานที่</p>
      <form className="mt-5 space-y-3" onSubmit={handleSubmit}>
        <Field label="ชื่อผู้ใช้" placeholder="กรอกชื่อผู้ใช้" value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} />
        <Field label="อีเมล" type="email" placeholder="name@example.com" value={form.email} onChange={(e) => setForm({ ...form, email: e.target.value })} />
        <Field label="รหัสผ่าน" type="password" placeholder="อย่างน้อย 6 ตัวอักษร" value={form.password} onChange={(e) => setForm({ ...form, password: e.target.value })} />
        <Field label="ยืนยันรหัสผ่าน" type="password" placeholder="กรอกรหัสผ่านอีกครั้ง" value={form.confirmPassword} onChange={(e) => setForm({ ...form, confirmPassword: e.target.value })} />
        {error && <p className="rounded-xl bg-red-50 p-3 text-sm text-danger">{error}</p>}
        <Button className="w-full" disabled={submitting}>{submitting ? "กำลังสมัคร..." : "สมัครสมาชิก"}</Button>
      </form>
      <p className="mt-4 text-center text-sm text-stone-500">มีบัญชีแล้ว? <Link className="font-semibold text-brand" to="/login">เข้าสู่ระบบ</Link></p>
      </div>
    </div>
  );
}
```

เมื่อคัดครบไฟล์ ให้รัน `npm run lint` และแก้ error ของไฟล์นี้ก่อนก้อนถัดไป

## 24. `FrontEnd/src/pages/LoginPage.jsx`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนตามหมายเลข

### 24.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/LoginPage.jsx`

**ตำแหน่งวาง:** วางหลัง comment แบบฝึกหัดเดิม; import ต้องอยู่บนสุดของไฟล์

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 24.1 ทำหน้าที่ `import { useState } from "react";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import { useState } from "react";
```

### 24.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/LoginPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 24.2 ทำหน้าที่ `import { Link, Navigate, useNavigate } from "react-router-dom";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import { Link, Navigate, useNavigate } from "react-router-dom";
```

### 24.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/LoginPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 24.3 ทำหน้าที่ `import BrandMark from "../components/BrandMark.jsx";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import BrandMark from "../components/BrandMark.jsx";
```

### 24.4 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/LoginPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 24.4 ทำหน้าที่ `import { Button, Field } from "../components/ui.jsx";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import { Button, Field } from "../components/ui.jsx";
```

### 24.5 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/LoginPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 24.5 ทำหน้าที่ `import useAuth from "../hooks/useAuth.js";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import useAuth from "../hooks/useAuth.js";
```

### 24.6 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/LoginPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 24.6 ทำหน้าที่ `import { getErrorMessage } from "../services/api.js";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import { getErrorMessage } from "../services/api.js";
```

### 24.7 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/LoginPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 24.7 ทำหน้าที่ `export default function LoginPage() {` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
export default function LoginPage() {
  const { user, login } = useAuth();
  const navigate = useNavigate();
  const [form, setForm] = useState({ email: "", password: "" });
  const [error, setError] = useState("");
  const [submitting, setSubmitting] = useState(false);

  if (user) return <Navigate to={user.role === "admin" ? "/admin/places" : "/"} replace />;
```

### 24.8 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/LoginPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 24.8 ทำหน้าที่ `async function handleSubmit(event) {` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
  async function handleSubmit(event) {
    event.preventDefault();
    setSubmitting(true);
    setError("");

    try {
      const loggedInUser = await login(form.email, form.password);
      navigate(loggedInUser?.role === "admin" ? "/admin/places" : "/");
    } catch (requestError) {
      setError(getErrorMessage(requestError));
    } finally {
      setSubmitting(false);
    }
  }
```

### 24.9 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/LoginPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 24.9 ทำหน้าที่ `return (` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
return (
    <div className="relative mx-auto flex min-h-screen max-w-md flex-col overflow-hidden bg-cream-50 px-5 py-8 shadow-2xl">
      <div className="absolute inset-0 bg-[url('/auth-background.png')] bg-cover bg-center" />
      <div className="absolute inset-0 bg-white/15" />
      <div className="relative"><BrandMark /></div>
      <div className="auth-panel relative mt-3 rounded-3xl bg-cream-50/88 p-5 shadow-lg backdrop-blur-[2px]">
        <h2 className="text-2xl font-bold">ยินดีต้อนรับ</h2>
        <p className="text-sm text-stone-500">เข้าสู่ระบบก่อนเริ่มค้นหาสถานที่</p>
        <form className="mt-6 space-y-4" onSubmit={handleSubmit}>
          <Field label="อีเมล" type="email" placeholder="name@example.com" value={form.email} onChange={(e) => setForm({ ...form, email: e.target.value })} />
          <Field label="รหัสผ่าน" type="password" placeholder="อย่างน้อย 6 ตัวอักษร" value={form.password} onChange={(e) => setForm({ ...form, password: e.target.value })} />
          {error && <p className="rounded-xl bg-red-50 p-3 text-sm text-danger">{error}</p>}
          <Button className="w-full" disabled={submitting}>{submitting ? "กำลังเข้าสู่ระบบ..." : "เข้าสู่ระบบ"}</Button>
        </form>
        <p className="mt-5 text-center text-sm text-stone-500">ยังไม่มีบัญชี? <Link className="font-semibold text-brand" to="/register">สมัครสมาชิก</Link></p>
      </div>
    </div>
  );
}
```

เมื่อคัดครบไฟล์ ให้รัน `npm run lint` และแก้ error ของไฟล์นี้ก่อนก้อนถัดไป

### CHECKPOINT 8 — RUN DEV: หน้า Register และ Login

เมื่อหัวข้อ 7–24 ผ่าน lint แล้ว ให้แทนที่ไฟล์ชั่วคราวสองไฟล์ดังนี้

`FrontEnd/src/App.jsx`:

```jsx
import { Navigate, Route, Routes } from "react-router-dom";
import LoginPage from "./pages/LoginPage.jsx";
import RegisterPage from "./pages/RegisterPage.jsx";

export default function App() {
  return <Routes><Route path="/login" element={<LoginPage />} /><Route path="/register" element={<RegisterPage />} /><Route path="*" element={<Navigate to="/login" replace />} /></Routes>;
}
```

`FrontEnd/src/main.jsx`:

```jsx
import { StrictMode } from "react";
import { createRoot } from "react-dom/client";
import { BrowserRouter } from "react-router-dom";
import "./index.css";
import App from "./App.jsx";
import AuthProvider from "./hooks/AuthProvider.jsx";
import ThemeProvider from "./hooks/ThemeProvider.jsx";

createRoot(document.getElementById("root")).render(<StrictMode><ThemeProvider><BrowserRouter><AuthProvider><App /></AuthProvider></BrowserRouter></ThemeProvider></StrictMode>);
```

ให้ Backend เปิดอยู่ที่ Port 5000 แล้วรัน Frontend ด้วย `npm run dev` เปิด `/register` สร้างบัญชีใหม่ จากนั้นไป `/login` และ Login ให้สำเร็จ ถ้า Browser Console หรือ Terminal มี error ให้หยุดแก้หัวข้อ 7–24 ก่อนทำ Profile

## 25. `FrontEnd/src/pages/ProfilePage.jsx`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนตามหมายเลข

### 25.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/ProfilePage.jsx`

**ตำแหน่งวาง:** วางหลัง comment แบบฝึกหัดเดิม; import ต้องอยู่บนสุดของไฟล์

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 25.1 ทำหน้าที่ `import { Camera, LogOut, Moon, ShieldCheck, Sun, UserRound } from "lucide-react";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import { Camera, LogOut, Moon, ShieldCheck, Sun, UserRound } from "lucide-react";
```

### 25.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/ProfilePage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 25.2 ทำหน้าที่ `import { useRef, useState } from "react";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import { useRef, useState } from "react";
```

### 25.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/ProfilePage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 25.3 ทำหน้าที่ `import { useNavigate } from "react-router-dom";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import { useNavigate } from "react-router-dom";
```

### 25.4 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/ProfilePage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 25.4 ทำหน้าที่ `import PageHeader from "../components/PageHeader.jsx";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import PageHeader from "../components/PageHeader.jsx";
```

### 25.5 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/ProfilePage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 25.5 ทำหน้าที่ `import { Button } from "../components/ui.jsx";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import { Button } from "../components/ui.jsx";
```

### 25.6 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/ProfilePage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 25.6 ทำหน้าที่ `import useAuth from "../hooks/useAuth.js";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import useAuth from "../hooks/useAuth.js";
```

### 25.7 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/ProfilePage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 25.7 ทำหน้าที่ `import useTheme from "../hooks/useTheme.js";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import useTheme from "../hooks/useTheme.js";
```

### 25.8 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/ProfilePage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 25.8 ทำหน้าที่ `import api, { getAssetUrl, getErrorMessage } from "../services/api.js";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import api, { getAssetUrl, getErrorMessage } from "../services/api.js";
```

### 25.9 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/ProfilePage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 25.9 ทำหน้าที่ `export default function ProfilePage() {` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
export default function ProfilePage() {
  const { user, logout, reloadUser } = useAuth();
  const navigate = useNavigate();
  const { theme, setTheme } = useTheme();
  const fileInputRef = useRef(null);
  const [uploading, setUploading] = useState(false);
  const [message, setMessage] = useState("");

  function handleLogout() {
    logout();
    navigate("/login");
  }
```

### 25.10 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/ProfilePage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 25.10 ทำหน้าที่ `async function handleAvatarChange(event) {` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
  async function handleAvatarChange(event) {
    const file = event.target.files?.[0];
    if (!file) return;

    setUploading(true);
    setMessage("");
    try {
      const formData = new FormData();
      formData.append("avatar", file);
      await api.patch("/users/me/avatar", formData);
      await reloadUser();
      setMessage("เปลี่ยนรูปโปรไฟล์สำเร็จ");
    } catch (requestError) {
      setMessage(getErrorMessage(requestError));
    } finally {
      setUploading(false);
      event.target.value = "";
    }
  }
```

### 25.11 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/ProfilePage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 25.11 ทำหน้าที่ `return (` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
return (
    <div>
      <PageHeader title="โปรไฟล์" />
      <section className="px-5 py-8 text-center">
        <button className="relative mx-auto block" disabled={uploading} onClick={() => fileInputRef.current?.click()}>
          <span className="grid h-24 w-24 place-items-center overflow-hidden rounded-full bg-orange-100 text-brand ring-4 ring-white">
            {user.avatarUrl ? <img className="h-full w-full object-cover" src={getAssetUrl(user.avatarUrl)} alt="รูปโปรไฟล์" /> : <UserRound size={44} />}
          </span>
          <span className="absolute bottom-0 right-0 grid h-8 w-8 place-items-center rounded-full bg-brand text-white shadow"><Camera size={16} /></span>
        </button>
        <input ref={fileInputRef} className="hidden" type="file" accept="image/*" capture="user" onChange={handleAvatarChange} />
        <p className="mt-2 text-xs text-stone-500">{uploading ? "กำลังอัปโหลด..." : "แตะรูปเพื่อเปลี่ยนรูปโปรไฟล์"}</p>
        {message && <p className="mt-2 text-sm text-brand">{message}</p>}
        <h2 className="mt-4 text-xl font-bold">{user.name}</h2>
        <p className="text-sm text-stone-500">{user.email}</p>
        <div className="mt-8 rounded-2xl bg-white p-4 text-left shadow-sm">
          <div className="flex items-center justify-between"><span>สิทธิ์ผู้ใช้</span><span className="flex items-center gap-1 rounded-full bg-green-50 px-3 py-1 text-sm text-leaf"><ShieldCheck size={16} />{user.role}</span></div>
        </div>
        <div className="mt-3 rounded-2xl bg-white p-4 text-left shadow-sm">
          <p className="mb-3 font-semibold">ธีมหน้าจอ</p>
          <div className="grid grid-cols-2 gap-2 rounded-xl bg-cream-100 p-1">
            <button
              className={`flex items-center justify-center gap-2 rounded-lg px-3 py-2.5 text-sm transition ${theme === "light" ? "bg-white font-semibold text-brand shadow-sm" : "text-stone-500"}`}
              onClick={() => setTheme("light")}
            >
              <Sun size={17} />ธีมปกติ
            </button>
            <button
              className={`flex items-center justify-center gap-2 rounded-lg px-3 py-2.5 text-sm transition ${theme === "dark" ? "bg-stone-700 font-semibold text-orange-300 shadow-sm" : "text-stone-500"}`}
              onClick={() => setTheme("dark")}
            >
              <Moon size={17} />ธีมมืด
            </button>
          </div>
          <p className="mt-2 text-xs text-stone-500">ระบบจะจำธีมนี้ไว้ในเครื่อง</p>
        </div>
        {user.role === "admin" && <Button className="mt-4 w-full" variant="success" onClick={() => navigate("/admin/places")}>เปิดหน้าจัดการ Admin</Button>}
        <Button className="mt-3 w-full" variant="outline" onClick={handleLogout}><LogOut size={18} />ออกจากระบบ</Button>
      </section>
    </div>
  );
}
```

เมื่อคัดครบไฟล์ ให้รัน `npm run lint` และแก้ error ของไฟล์นี้ก่อนก้อนถัดไป

## 26. `FrontEnd/src/pages/MapHomePage.jsx`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนตามหมายเลข

### 26.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/MapHomePage.jsx`

**ตำแหน่งวาง:** วางหลัง comment แบบฝึกหัดเดิม; import ต้องอยู่บนสุดของไฟล์

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 26.1 ทำหน้าที่ `import { LoaderCircle, LocateFixed } from "lucide-react";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import { LoaderCircle, LocateFixed } from "lucide-react";
```

### 26.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/MapHomePage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 26.2 ทำหน้าที่ `import { useEffect, useState } from "react";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import { useEffect, useState } from "react";
```

### 26.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/MapHomePage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 26.3 ทำหน้าที่ `import { useGeolocated } from "react-geolocated";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import { useGeolocated } from "react-geolocated";
```

### 26.4 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/MapHomePage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 26.4 ทำหน้าที่ `import PlaceMap from "../components/PlaceMap.jsx";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import PlaceMap from "../components/PlaceMap.jsx";
```

### 26.5 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/MapHomePage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 26.5 ทำหน้าที่ `import { Chip } from "../components/ui.jsx";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import { Chip } from "../components/ui.jsx";
```

### 26.6 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/MapHomePage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 26.6 ทำหน้าที่ `import api from "../services/api.js";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import api from "../services/api.js";
```

### 26.7 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/MapHomePage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 26.7 ทำหน้าที่ `export default function MapHomePage() {` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
export default function MapHomePage() {
  const [places, setPlaces] = useState([]);
  const [type, setType] = useState("");
  const {
    coords,
    getPosition,
    positionError,
    isGeolocationAvailable,
    isGeolocationEnabled,
  } = useGeolocated({
    positionOptions: { enableHighAccuracy: true },
    suppressLocationOnMount: false,
    userDecisionTimeout: 12000,
  });
```

### 26.8 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/MapHomePage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 26.8 ทำหน้าที่ `useEffect(() => {` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
  useEffect(() => {
    api.get("/places", { params: type ? { type } : {} })
      .then((response) => setPlaces(response.data.places))
      .catch(() => {
        setPlaces([]);
      });
  }, [type]);

  const position = coords ? [coords.latitude, coords.longitude] : null;
```

### 26.9 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/MapHomePage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 26.9 ทำหน้าที่ `return (` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
return (
    <div className="relative h-[calc(100vh-4rem)] min-h-[620px] overflow-hidden md:h-[calc(100vh-7rem)]">
      <div className="absolute inset-0 bg-cream-100">
        {position ? (
          <PlaceMap places={places} position={position} />
        ) : (
          <div className="grid h-full place-items-center px-8 text-center text-stone-500">
            <div>
              {!positionError && (
                <LoaderCircle className="mx-auto mb-3 animate-spin text-brand" size={32} />
              )}
              <p className="font-semibold text-ink">
                {positionError ? "ไม่สามารถอ่านตำแหน่งได้" : "กำลังหาตำแหน่งของคุณ..."}
              </p>
              <p className="mt-1 text-sm">
                {positionError
                  ? "กรุณาเปิด GPS และอนุญาต Location แล้วลองอีกครั้ง"
                  : "แผนที่จะเปิดบริเวณใกล้คุณโดยอัตโนมัติ"}
              </p>
            </div>
          </div>
        )}
      </div>
      <div className="absolute inset-x-4 top-4 z-[800] rounded-2xl bg-white/95 p-3 text-ink shadow-lg backdrop-blur dark:bg-[#302d29]/95 dark:text-[#f0e9df]">
        <h1 className="text-center font-bold text-ink dark:text-[#f0e9df]">ค้นหาใกล้ฉัน</h1>
        <div className="mt-3 flex justify-center gap-2"><Chip active={!type} onClick={() => setType("")}>ทั้งหมด</Chip><Chip active={type === "toilet"} onClick={() => setType("toilet")}>ห้องน้ำ</Chip><Chip active={type === "trash"} onClick={() => setType("trash")}>ถังขยะ</Chip></div>
      </div>
      <button className="absolute bottom-5 left-1/2 z-[800] flex -translate-x-1/2 items-center gap-2 rounded-full bg-white px-5 py-3 font-semibold text-brand shadow-lg" onClick={getPosition}><LocateFixed size={19} />{position ? "ตำแหน่งปัจจุบัน" : "เปิด GPS"}</button>
      {(!isGeolocationAvailable || !isGeolocationEnabled) && <p className="absolute bottom-20 left-1/2 z-[800] w-72 -translate-x-1/2 rounded-xl bg-white p-2 text-center text-xs text-danger shadow">กรุณาเปิด GPS และอนุญาตตำแหน่ง</p>}
    </div>
  );
}
```

เมื่อคัดครบไฟล์ ให้รัน `npm run lint` และแก้ error ของไฟล์นี้ก่อนก้อนถัดไป

## 27. `FrontEnd/src/pages/SearchPage.jsx`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนตามหมายเลข

### 27.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/SearchPage.jsx`

**ตำแหน่งวาง:** วางหลัง comment แบบฝึกหัดเดิม; import ต้องอยู่บนสุดของไฟล์

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 27.1 ทำหน้าที่ `import { LoaderCircle, Search } from "lucide-react";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import { LoaderCircle, Search } from "lucide-react";
```

### 27.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/SearchPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 27.2 ทำหน้าที่ `import { useCallback, useEffect, useState } from "react";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import { useCallback, useEffect, useState } from "react";
```

### 27.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/SearchPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 27.3 ทำหน้าที่ `import { useGeolocated } from "react-geolocated";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import { useGeolocated } from "react-geolocated";
```

### 27.4 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/SearchPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 27.4 ทำหน้าที่ `import PageHeader from "../components/PageHeader.jsx";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import PageHeader from "../components/PageHeader.jsx";
```

### 27.5 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/SearchPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 27.5 ทำหน้าที่ `import PlaceCard from "../components/PlaceCard.jsx";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import PlaceCard from "../components/PlaceCard.jsx";
```

### 27.6 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/SearchPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 27.6 ทำหน้าที่ `import { Button, Chip, EmptyState } from "../components/ui.jsx";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import { Button, Chip, EmptyState } from "../components/ui.jsx";
```

### 27.7 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/SearchPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 27.7 ทำหน้าที่ `import api, { getErrorMessage } from "../services/api.js";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import api, { getErrorMessage } from "../services/api.js";
```

### 27.8 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/SearchPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 27.8 ทำหน้าที่ `export default function SearchPage() {` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
export default function SearchPage() {
  const [search, setSearch] = useState("");
  const [type, setType] = useState("");
  const [radius, setRadius] = useState(500);
  const [places, setPlaces] = useState([]);
  const [error, setError] = useState("");
  const [loading, setLoading] = useState(true);
  const [waitingForGps, setWaitingForGps] = useState(true);
  const [pendingFilters, setPendingFilters] = useState({ type: "", radius: 500, search: "" });
  const [hasSearched, setHasSearched] = useState(false);
```

### 27.9 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/SearchPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 27.9 ทำหน้าที่ `const {` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
  const {
    coords,
    getPosition,
    positionError,
    isGeolocationAvailable,
    isGeolocationEnabled,
  } = useGeolocated({
    positionOptions: { enableHighAccuracy: true },
    suppressLocationOnMount: false,
  });
```

### 27.10 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/SearchPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 27.10 ทำหน้าที่ `ใช้ฟังก์ชันเดียวกันทั้งตอนกดค้นหา และตอนที่ GPS ส่งพิกัดกลับมา` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
  // ใช้ฟังก์ชันเดียวกันทั้งตอนกดค้นหา และตอนที่ GPS ส่งพิกัดกลับมา
  const runSearch = useCallback(
    async (currentCoords = coords, filters = {}) => {
      const selectedType = filters.type ?? type;
      const selectedRadius = filters.radius ?? radius;
      const selectedSearch = filters.search ?? search;
      setLoading(true);
      setError("");
```

### 27.11 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/SearchPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 27.11 ทำหน้าที่ `try {` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
      try {
        if (selectedRadius) {
          const response = await api.get("/places/nearby", {
            params: {
              latitude: currentCoords.latitude,
              longitude: currentCoords.longitude,
              radius: selectedRadius / 1000,
            },
          });
```

### 27.12 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/SearchPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 27.12 ทำหน้าที่ `const filteredPlaces = response.data.places.filter(` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
          const filteredPlaces = response.data.places.filter(
            (place) =>
              (!selectedType || place.type === selectedType) &&
              (!selectedSearch || place.name.toLowerCase().includes(selectedSearch.toLowerCase())),
          );
          setPlaces(filteredPlaces);
        } else {
          const response = await api.get("/places", {
            params: {
              ...(selectedType && { type: selectedType }),
              ...(selectedSearch && { search: selectedSearch }),
            },
          });
          setPlaces(response.data.places);
        }
```

### 27.13 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/SearchPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 27.13 ทำหน้าที่ `setHasSearched(true);` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
        setHasSearched(true);
      } catch (requestError) {
        setError(getErrorMessage(requestError));
      } finally {
        setLoading(false);
      }
    },
    [coords, radius, search, type],
  );
```

### 27.14 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/SearchPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 27.14 ทำหน้าที่ `ถ้าตอนกดค้นหา GPS ยังไม่มา ให้ค้นหาต่ออัตโนมัติทันทีที่ได้พิกัด` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
  // ถ้าตอนกดค้นหา GPS ยังไม่มา ให้ค้นหาต่ออัตโนมัติทันทีที่ได้พิกัด
  useEffect(() => {
    if (!waitingForGps || !coords) return;
    setWaitingForGps(false);
    runSearch(coords, pendingFilters || {});
    setPendingFilters(null);
  }, [coords, pendingFilters, runSearch, waitingForGps]);

  // แสดงข้อผิดพลาดเมื่อผู้ใช้ปิดหรือไม่อนุญาต GPS
  useEffect(() => {
    if (!waitingForGps || !positionError) return;
    setWaitingForGps(false);
    setLoading(false);
    setError("ไม่สามารถใช้ตำแหน่งได้ กรุณาอนุญาต Location ในเบราว์เซอร์");
  }, [positionError, waitingForGps]);
```

### 27.15 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/SearchPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 27.15 ทำหน้าที่ `function handleSearch() {` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
  function handleSearch() {
    if (radius && !coords) {
      if (!isGeolocationAvailable || !isGeolocationEnabled) {
        setError("อุปกรณ์นี้ปิด GPS หรือไม่รองรับตำแหน่ง");
        return;
      }

      setLoading(true);
      setWaitingForGps(true);
      setError("");
      setPendingFilters({ type, radius, search });
      getPosition();
      return;
    }
```

### 27.16 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/SearchPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 27.16 ทำหน้าที่ `runSearch(coords);` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
    runSearch(coords);
  }

  function applyFilters(nextType, nextRadius) {
    setType(nextType);
    setRadius(nextRadius);

    if (nextRadius && !coords) {
      setLoading(true);
      setWaitingForGps(true);
      setPendingFilters({ type: nextType, radius: nextRadius, search });
      getPosition();
      return;
    }
```

### 27.17 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/SearchPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 27.17 ทำหน้าที่ `runSearch(coords, { type: nextType, radius: nextRadius, search });` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
    runSearch(coords, { type: nextType, radius: nextRadius, search });
  }

  return (
    <div>
      <PageHeader title="ค้นหาสถานที่" />
      <section className="space-y-5 px-5 py-5">
        <div className="flex rounded-xl border border-cream-200 bg-white px-3">
          <Search className="my-auto text-stone-400" size={19} />
          <input
            className="w-full bg-transparent px-3 py-3 outline-none"
            placeholder="ค้นหาสถานที่, พื้นที่..."
            value={search}
            onChange={(event) => setSearch(event.target.value)}
          />
        </div>
```

### 27.18 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/SearchPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 27.18 ทำหน้าที่ `<div>` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
        <div>
          <p className="mb-2 text-sm text-stone-500">ประเภท</p>
          <div className="flex gap-2">
            <Chip active={!type} onClick={() => applyFilters("", radius)}>ทั้งหมด</Chip>
            <Chip active={type === "toilet"} onClick={() => applyFilters("toilet", radius)}>ห้องน้ำ</Chip>
            <Chip active={type === "trash"} onClick={() => applyFilters("trash", radius)}>ถังขยะ</Chip>
          </div>
        </div>
```

### 27.19 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/SearchPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 27.19 ทำหน้าที่ `<div>` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
        <div>
          <p className="mb-2 text-sm text-stone-500">ระยะทางใกล้ฉัน</p>
          <div className="flex gap-2">
            <Chip active={radius === 500} onClick={() => applyFilters(type, 500)}>500 m</Chip>
            <Chip active={radius === 1000} onClick={() => applyFilters(type, 1000)}>1000 m</Chip>
            <Chip active={radius === 10000} onClick={() => applyFilters(type, 10000)}>ทั้งหมด</Chip>
          </div>
        </div>
```

### 27.20 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/SearchPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 27.20 ทำหน้าที่ `<Button className="w-full" disabled={loading} onClick={handleSearch}>` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
<Button className="w-full" disabled={loading} onClick={handleSearch}>
          {loading && <LoaderCircle className="animate-spin" size={18} />}
          {waitingForGps ? "กำลังหาตำแหน่ง..." : "ค้นหา"}
        </Button>

        {error && <p className="text-sm text-danger">{error}</p>}

        <div className="space-y-3">
          {places.length ? (
            places.map((place) => <PlaceCard key={place.id} place={place} />)
          ) : !loading ? (
            <EmptyState
              title={hasSearched ? "ไม่พบสถานที่ใกล้คุณ" : "ยังไม่มีสถานที่"}
              description="ลองเพิ่มระยะทาง หรือเลือกทั้งหมดแล้วค้นหาอีกครั้ง"
            />
          ) : null}
        </div>
      </section>
    </div>
  );
}
```

เมื่อคัดครบไฟล์ ให้รัน `npm run lint` และแก้ error ของไฟล์นี้ก่อนก้อนถัดไป

## 28. `FrontEnd/src/pages/PlaceDetailPage.jsx`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนตามหมายเลข

### 28.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/PlaceDetailPage.jsx`

**ตำแหน่งวาง:** วางหลัง comment แบบฝึกหัดเดิม; import ต้องอยู่บนสุดของไฟล์

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 28.1 ทำหน้าที่ `import { Flag, Navigation, Share2, Trash2, Toilet } from "lucide-react";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import { Flag, Navigation, Share2, Trash2, Toilet } from "lucide-react";
```

### 28.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/PlaceDetailPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 28.2 ทำหน้าที่ `import { useEffect, useState } from "react";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import { useEffect, useState } from "react";
```

### 28.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/PlaceDetailPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 28.3 ทำหน้าที่ `import { useNavigate, useParams } from "react-router-dom";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import { useNavigate, useParams } from "react-router-dom";
```

### 28.4 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/PlaceDetailPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 28.4 ทำหน้าที่ `import PageHeader from "../components/PageHeader.jsx";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import PageHeader from "../components/PageHeader.jsx";
```

### 28.5 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/PlaceDetailPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 28.5 ทำหน้าที่ `import { Button, PageLoader } from "../components/ui.jsx";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import { Button, PageLoader } from "../components/ui.jsx";
```

### 28.6 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/PlaceDetailPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 28.6 ทำหน้าที่ `import api, { getAssetUrl } from "../services/api.js";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import api, { getAssetUrl } from "../services/api.js";
```

### 28.7 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/PlaceDetailPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 28.7 ทำหน้าที่ `export default function PlaceDetailPage() {` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
export default function PlaceDetailPage() {
  const { id } = useParams();
  const navigate = useNavigate();
  const [place, setPlace] = useState(null);

  useEffect(() => {
    api.get(`/places/${id}`)
      .then((response) => setPlace(response.data.place))
      .catch(() => navigate("/"));
  }, [id, navigate]);
  if (!place) return <PageLoader />;
  const Icon = place.type === "toilet" ? Toilet : Trash2;
```

### 28.8 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/PlaceDetailPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 28.8 ทำหน้าที่ `async function share() {` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
  async function share() {
    const data = { title: place.name, text: place.address, url: window.location.href };
    try {
      if (navigator.share) await navigator.share(data);
      else {
        await navigator.clipboard.writeText(window.location.href);
      }
    } catch (error) {
      if (error.name !== "AbortError") console.error(error);
    }
  }
```

### 28.9 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/PlaceDetailPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 28.9 ทำหน้าที่ `return (` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
return (
    <div><PageHeader title="รายละเอียดสถานที่" back />
      <div className={`grid h-52 place-items-center overflow-hidden ${place.type === "toilet" ? "bg-green-100 text-leaf" : "bg-orange-100 text-brand"}`}>{place.imageUrl ? <img className="h-full w-full object-cover" src={getAssetUrl(place.imageUrl)} alt={place.name} /> : <Icon size={72} />}</div>
      <section className="px-5 py-5"><span className="rounded-full bg-white px-3 py-1 text-xs">{place.type === "toilet" ? "ห้องน้ำ" : "ถังขยะ"}</span><h1 className="mt-3 text-2xl font-bold">{place.name}</h1><p className="mt-2 text-sm text-stone-500">{place.address}</p><p className="mt-5 text-sm leading-7 text-stone-600">สถานที่นี้ได้รับการอนุมัติและแสดงจากฐานข้อมูล Trash & Toilet Map</p>
        <div className="mt-8 grid grid-cols-2 gap-3"><Button onClick={() => window.open(`https://www.google.com/maps/dir/?api=1&destination=${place.latitude},${place.longitude}`, "_blank")}><Navigation size={18} />นำทาง</Button><Button variant="outline" onClick={share}><Share2 size={18} />แชร์</Button></div>
        <Button className="mt-3 w-full" variant="ghost" onClick={() => navigate(`/places/${id}/report`)}><Flag size={18} />รายงานข้อมูลผิด</Button>
      </section>
    </div>
  );
}
```

เมื่อคัดครบไฟล์ ให้รัน `npm run lint` และแก้ error ของไฟล์นี้ก่อนก้อนถัดไป

## 29. `FrontEnd/src/pages/AddPlacePage.jsx`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนตามหมายเลข

### 29.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/AddPlacePage.jsx`

**ตำแหน่งวาง:** วางหลัง comment แบบฝึกหัดเดิม; import ต้องอยู่บนสุดของไฟล์

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 29.1 ทำหน้าที่ `import { useState } from "react";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import { useState } from "react";
```

### 29.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/AddPlacePage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 29.2 ทำหน้าที่ `import { useNavigate } from "react-router-dom";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import { useNavigate } from "react-router-dom";
```

### 29.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/AddPlacePage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 29.3 ทำหน้าที่ `import PageHeader from "../components/PageHeader.jsx";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import PageHeader from "../components/PageHeader.jsx";
```

### 29.4 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/AddPlacePage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 29.4 ทำหน้าที่ `import PlaceForm from "../components/PlaceForm.jsx";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import PlaceForm from "../components/PlaceForm.jsx";
```

### 29.5 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/AddPlacePage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 29.5 ทำหน้าที่ `import api, { getErrorMessage } from "../services/api.js";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import api, { getErrorMessage } from "../services/api.js";
```

### 29.6 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/AddPlacePage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 29.6 ทำหน้าที่ `รูปและข้อความต้องส่งเป็น FormData เพื่อให้ Multer อ่านไฟล์ได้` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
export default function AddPlacePage({ admin = false }) {
  const navigate = useNavigate();
  const [message, setMessage] = useState("");

  async function handleSubmit(form) {
    try {
      setMessage("");
      // รูปและข้อความต้องส่งเป็น FormData เพื่อให้ Multer อ่านไฟล์ได้
      const formData = new FormData();
      formData.append("name", form.name);
      formData.append("type", form.type);
      formData.append("address", form.address);
      formData.append("latitude", form.latitude);
      formData.append("longitude", form.longitude);
      formData.append("image", form.image);

      const response = await api.post("/places", formData);
      setMessage(response.data.message);
      // Admin เพิ่มแล้วเป็น active จึงพาไปหน้า Map เพื่อเห็น Marker ทันที
      setTimeout(() => navigate("/"), 700);
    } catch (error) {
      setMessage(getErrorMessage(error));
    }
  }

  return (
    <div>
      <PageHeader title={admin ? "เพิ่มสถานที่ใหม่" : "เพิ่มสถานที่ใหม่"} subtitle={admin ? "Admin เพิ่มแล้วแสดงบนแผนที่ทันที" : "ข้อมูลจะถูกส่งให้ผู้ดูแลตรวจสอบ"} />
      <section className="px-5 py-5"><PlaceForm onSubmit={handleSubmit} submitLabel={admin ? "เพิ่ม" : "ส่งข้อมูล"} />{message && <p className="mt-4 rounded-xl bg-white p-3 text-center text-sm">{message}</p>}</section>
    </div>
  );
}
```

เมื่อคัดครบไฟล์ ให้รัน `npm run lint` และแก้ error ของไฟล์นี้ก่อนก้อนถัดไป

### CHECKPOINT 9 — RUN DEV: Map, Search และ Place

เมื่อหัวข้อ 25–29 ผ่าน lint ให้ขยาย `FrontEnd/src/App.jsx` ชั่วคราวเป็น Route ที่ทำเสร็จแล้ว:

```jsx
import { Navigate, Route, Routes } from "react-router-dom";
import ProtectedRoute from "./components/ProtectedRoute.jsx";
import AppLayout from "./layouts/AppLayout.jsx";
import AddPlacePage from "./pages/AddPlacePage.jsx";
import LoginPage from "./pages/LoginPage.jsx";
import MapHomePage from "./pages/MapHomePage.jsx";
import PlaceDetailPage from "./pages/PlaceDetailPage.jsx";
import ProfilePage from "./pages/ProfilePage.jsx";
import RegisterPage from "./pages/RegisterPage.jsx";
import SearchPage from "./pages/SearchPage.jsx";

export default function App() {
  return <Routes>
    <Route path="/login" element={<LoginPage />} />
    <Route path="/register" element={<RegisterPage />} />
    <Route element={<AppLayout />}>
      <Route index element={<MapHomePage />} />
      <Route path="search" element={<SearchPage />} />
      <Route path="places/:id" element={<PlaceDetailPage />} />
      <Route element={<ProtectedRoute />}>
        <Route path="places/new" element={<AddPlacePage />} />
        <Route path="profile" element={<ProfilePage />} />
      </Route>
    </Route>
    <Route path="*" element={<Navigate to="/" replace />} />
  </Routes>;
}
```

รัน `npm run dev` แล้วตรวจหน้า `/`, `/search`, `/profile` และ `/places/new` ต้องเปลี่ยนหน้าได้โดยไม่มีจอขาวหรือ Console error ทดลองค้นหาและเพิ่ม Place หนึ่งรายการให้ API ตอบกลับก่อนทำ Report

## 30. `FrontEnd/src/pages/ReportPage.jsx`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนตามหมายเลข

### 30.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/ReportPage.jsx`

**ตำแหน่งวาง:** วางหลัง comment แบบฝึกหัดเดิม; import ต้องอยู่บนสุดของไฟล์

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 30.1 ทำหน้าที่ `import { Camera } from "lucide-react";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import { Camera } from "lucide-react";
```

### 30.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/ReportPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 30.2 ทำหน้าที่ `import { useEffect, useState } from "react";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import { useEffect, useState } from "react";
```

### 30.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/ReportPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 30.3 ทำหน้าที่ `import { useNavigate, useParams } from "react-router-dom";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import { useNavigate, useParams } from "react-router-dom";
```

### 30.4 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/ReportPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 30.4 ทำหน้าที่ `import PageHeader from "../components/PageHeader.jsx";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import PageHeader from "../components/PageHeader.jsx";
```

### 30.5 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/ReportPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 30.5 ทำหน้าที่ `import { Button, Chip, Textarea } from "../components/ui.jsx";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import { Button, Chip, Textarea } from "../components/ui.jsx";
```

### 30.6 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/ReportPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 30.6 ทำหน้าที่ `import api, { getErrorMessage } from "../services/api.js";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import api, { getErrorMessage } from "../services/api.js";
```

### 30.7 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/ReportPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 30.7 ทำหน้าที่ `export default function ReportPage() {` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
export default function ReportPage() {
  const { id } = useParams();
  const navigate = useNavigate();
  const [reason, setReason] = useState("wrong_info");
  const [description, setDescription] = useState("");
  const [error, setError] = useState("");
  const [evidence, setEvidence] = useState(null);
  const [preview, setPreview] = useState("");
```

### 30.8 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/ReportPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 30.8 ทำหน้าที่ `useEffect(() => () => { if (preview) URL.revokeObjectURL(preview); }, [preview]);` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
  useEffect(() => () => { if (preview) URL.revokeObjectURL(preview); }, [preview]);

  async function submit(event) {
    event.preventDefault();
    try {
      if (!evidence) {
        setError("กรุณาแนบรูปหลักฐาน");
        return;
      }
      const formData = new FormData();
      formData.append("placeId", id);
      formData.append("reason", reason);
      formData.append("description", description);
      formData.append("evidence", evidence);
      await api.post("/reports", formData);
      navigate(`/places/${id}`);
    } catch (requestError) {
      setError(getErrorMessage(requestError));
    }
  }
```

### 30.9 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/ReportPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 30.9 ทำหน้าที่ `return (` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
return (
    <div><PageHeader title="รายงานข้อมูลผิด" back />
      <form className="space-y-5 px-5 py-6" onSubmit={submit}>
        <div><span className="mb-2 block text-sm text-stone-600">ประเภทปัญหา</span><div className="flex flex-wrap gap-2"><Chip type="button" active={reason === "wrong_info"} onClick={() => setReason("wrong_info")}>ข้อมูลผิด</Chip><Chip type="button" active={reason === "not_exists"} onClick={() => setReason("not_exists")}>ไม่มีอยู่จริง</Chip><Chip type="button" active={reason === "closed"} onClick={() => setReason("closed")}>ปิดชั่วคราว</Chip><Chip type="button" active={reason === "other"} onClick={() => setReason("other")}>อื่น ๆ</Chip></div></div>
        <Textarea label="รายละเอียด" placeholder="อธิบายข้อมูลที่ถูกต้อง..." value={description} onChange={(e) => setDescription(e.target.value)} />
        <label className="block">
          <span className="mb-2 block text-sm text-stone-600">รูปหลักฐานสำหรับ Admin</span>
          <span className="grid min-h-36 cursor-pointer place-items-center overflow-hidden rounded-2xl border border-dashed border-cream-200 bg-white">
            {preview ? <img className="h-48 w-full object-cover" src={preview} alt="รูปหลักฐาน" /> : <span className="flex items-center gap-2 text-sm text-brand"><Camera size={20} />ถ่ายหรือเลือกรูป</span>}
          </span>
          <input className="hidden" type="file" accept="image/*" capture="environment" onChange={(event) => { const file = event.target.files?.[0]; if (!file) return; setEvidence(file); setPreview(URL.createObjectURL(file)); }} />
          <span className="mt-1 block text-xs text-stone-500">รูปจะถูกลบเมื่อ Admin จัดการรายงานเสร็จ</span>
        </label>
        {error && <p className="text-sm text-danger">{error}</p>}
        <Button className="w-full">ส่งรายงาน</Button>
      </form>
    </div>
  );
}
```

เมื่อคัดครบไฟล์ ให้รัน `npm run lint` และแก้ error ของไฟล์นี้ก่อนก้อนถัดไป

## 31. `FrontEnd/src/pages/admin/AdminPendingPlacesPage.jsx`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนตามหมายเลข

### 31.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/admin/AdminPendingPlacesPage.jsx`

**ตำแหน่งวาง:** วางหลัง comment แบบฝึกหัดเดิม; import ต้องอยู่บนสุดของไฟล์

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 31.1 ทำหน้าที่ `import { useEffect, useState } from "react";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import { useEffect, useState } from "react";
```

### 31.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/admin/AdminPendingPlacesPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 31.2 ทำหน้าที่ `import PageHeader from "../../components/PageHeader.jsx";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import PageHeader from "../../components/PageHeader.jsx";
```

### 31.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/admin/AdminPendingPlacesPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 31.3 ทำหน้าที่ `import { Button, EmptyState } from "../../components/ui.jsx";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import { Button, EmptyState } from "../../components/ui.jsx";
```

### 31.4 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/admin/AdminPendingPlacesPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 31.4 ทำหน้าที่ `import api, { getErrorMessage } from "../../services/api.js";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import api, { getErrorMessage } from "../../services/api.js";
```

### 31.5 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/admin/AdminPendingPlacesPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 31.5 ทำหน้าที่ `export default function AdminPendingPlacesPage() {` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
export default function AdminPendingPlacesPage() {
  const [places, setPlaces] = useState([]);

  async function load() {
    try {
      const response = await api.get("/places/pending");
      setPlaces(response.data.places);
    } catch (error) {
      console.error(getErrorMessage(error));
    }
  }
  useEffect(() => { load(); }, []);

  async function update(id, status) {
    try {
      await api.patch(`/places/${id}/status`, { status });
      load();
    } catch (error) {
      console.error(getErrorMessage(error));
    }
  }

  return (
    <div><PageHeader title="อนุมัติคำขอเพิ่มสถานที่" subtitle="PLACES.status = pending" />
      <section className="space-y-3 px-5 py-5">{places.length ? places.map((place) => (
        <article className="rounded-2xl bg-white p-4 shadow-sm" key={place.id}><div className="flex items-start justify-between"><div><h2 className="font-semibold">{place.name}</h2><p className="text-xs text-stone-500">ผู้ขอ: {place.createdBy?.name}</p><p className="mt-1 text-xs text-stone-400">GPS: {place.latitude}, {place.longitude}</p></div><span className="rounded-full bg-orange-100 px-3 py-1 text-xs text-brand">pending</span></div><div className="mt-4 grid grid-cols-2 gap-2"><Button variant="success" onClick={() => update(place.id, "active")}>อนุมัติ</Button><Button variant="danger" onClick={() => update(place.id, "rejected")}>ปฏิเสธ</Button></div></article>
      )) : <EmptyState title="ไม่มีคำขอรออนุมัติ" />}</section>
    </div>
  );
}
```

เมื่อคัดครบไฟล์ ให้รัน `npm run lint` และแก้ error ของไฟล์นี้ก่อนก้อนถัดไป

## 32. `FrontEnd/src/pages/admin/AdminPlacesPage.jsx`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนตามหมายเลข

### 32.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/admin/AdminPlacesPage.jsx`

**ตำแหน่งวาง:** วางหลัง comment แบบฝึกหัดเดิม; import ต้องอยู่บนสุดของไฟล์

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 32.1 ทำหน้าที่ `import { Trash2 } from "lucide-react";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import { Trash2 } from "lucide-react";
```

### 32.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/admin/AdminPlacesPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 32.2 ทำหน้าที่ `import { useEffect, useState } from "react";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import { useEffect, useState } from "react";
```

### 32.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/admin/AdminPlacesPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 32.3 ทำหน้าที่ `import PageHeader from "../../components/PageHeader.jsx";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import PageHeader from "../../components/PageHeader.jsx";
```

### 32.4 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/admin/AdminPlacesPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 32.4 ทำหน้าที่ `import PlaceCard from "../../components/PlaceCard.jsx";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import PlaceCard from "../../components/PlaceCard.jsx";
```

### 32.5 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/admin/AdminPlacesPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 32.5 ทำหน้าที่ `import { Chip, EmptyState } from "../../components/ui.jsx";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import { Chip, EmptyState } from "../../components/ui.jsx";
```

### 32.6 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/admin/AdminPlacesPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 32.6 ทำหน้าที่ `import api, { getErrorMessage } from "../../services/api.js";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import api, { getErrorMessage } from "../../services/api.js";
```

### 32.7 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/admin/AdminPlacesPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 32.7 ทำหน้าที่ `export default function AdminPlacesPage() {` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
export default function AdminPlacesPage() {
  const [places, setPlaces] = useState([]);
  const [type, setType] = useState("");
  const [search, setSearch] = useState("");

  async function loadPlaces() {
    try {
      const response = await api.get("/places", { params: { status: "all" } });
      setPlaces(response.data.places);
    } catch (error) {
      console.error(getErrorMessage(error));
    }
  }
```

### 32.8 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/admin/AdminPlacesPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 32.8 ทำหน้าที่ `useEffect(() => { loadPlaces(); }, []);` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
  useEffect(() => { loadPlaces(); }, []);

  async function remove(id) {
    if (!window.confirm("ยืนยันการลบสถานที่นี้?")) return;
    try {
      await api.delete(`/places/${id}`);
      loadPlaces();
    } catch (error) {
      console.error(getErrorMessage(error));
    }
  }
```

### 32.9 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/admin/AdminPlacesPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 32.9 ทำหน้าที่ `const filtered = places.filter((place) => (!type || place.type === type) && place.name.toLowerCase().includes(search.toLowerCas...` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
const filtered = places.filter((place) => (!type || place.type === type) && place.name.toLowerCase().includes(search.toLowerCase()));

  return (
    <div><PageHeader title="จัดการสถานที่" subtitle="เพิ่ม แก้สถานะ และลบสถานที่" />
      <section className="space-y-4 px-5 py-5"><input className="w-full rounded-xl border border-cream-200 bg-white px-4 py-3 outline-none focus:border-brand" placeholder="ค้นหาสถานที่..." value={search} onChange={(e) => setSearch(e.target.value)} /><div className="flex gap-2"><Chip active={!type} onClick={() => setType("")}>ทั้งหมด</Chip><Chip active={type === "toilet"} onClick={() => setType("toilet")}>ห้องน้ำ</Chip><Chip active={type === "trash"} onClick={() => setType("trash")}>ถังขยะ</Chip></div>
        <div className="space-y-3">{filtered.length ? filtered.map((place) => <PlaceCard key={place.id} place={place} action={<div className="flex items-center gap-2"><span className="text-xs text-stone-400">{place.status}</span><button className="rounded-lg bg-danger p-2 text-white" onClick={() => remove(place.id)}><Trash2 size={15} /></button></div>} />) : <EmptyState title="ไม่พบสถานที่" />}</div>
      </section>
    </div>
  );
}
```

เมื่อคัดครบไฟล์ ให้รัน `npm run lint` และแก้ error ของไฟล์นี้ก่อนก้อนถัดไป

## 33. `FrontEnd/src/pages/admin/AdminReportsPage.jsx`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนตามหมายเลข

### 33.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/admin/AdminReportsPage.jsx`

**ตำแหน่งวาง:** วางหลัง comment แบบฝึกหัดเดิม; import ต้องอยู่บนสุดของไฟล์

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 33.1 ทำหน้าที่ `import { ChevronRight } from "lucide-react";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import { ChevronRight } from "lucide-react";
```

### 33.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/admin/AdminReportsPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 33.2 ทำหน้าที่ `import { useEffect, useState } from "react";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import { useEffect, useState } from "react";
```

### 33.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/admin/AdminReportsPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 33.3 ทำหน้าที่ `import { Link } from "react-router-dom";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import { Link } from "react-router-dom";
```

### 33.4 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/admin/AdminReportsPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 33.4 ทำหน้าที่ `import PageHeader from "../../components/PageHeader.jsx";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import PageHeader from "../../components/PageHeader.jsx";
```

### 33.5 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/admin/AdminReportsPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 33.5 ทำหน้าที่ `import { EmptyState } from "../../components/ui.jsx";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import { EmptyState } from "../../components/ui.jsx";
```

### 33.6 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/admin/AdminReportsPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 33.6 ทำหน้าที่ `import api from "../../services/api.js";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import api from "../../services/api.js";
```

### 33.7 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/admin/AdminReportsPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 33.7 ทำหน้าที่ `หน้าแจ้งเตือนแสดงเฉพาะงานที่ยังรอดำเนินการ` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
export default function AdminReportsPage() {
  const [reports, setReports] = useState([]);
  // หน้าแจ้งเตือนแสดงเฉพาะงานที่ยังรอดำเนินการ
  // เมื่อ resolved/rejected แล้ว รายการจะหายจากหน้านี้
  useEffect(() => {
    api
      .get("/reports", { params: { status: "pending" } })
      .then((response) => setReports(response.data.reports))
      .catch(() => setReports([]));
  }, []);

  return (
    <div><PageHeader title="ตรวจรายงานข้อมูลผิด" subtitle="จัดการรายงานจากผู้ใช้งาน" />
      <section className="space-y-3 px-5 py-5">{reports.length ? reports.map((report) => (
        <Link className="flex items-center rounded-2xl bg-white p-4 shadow-sm" to={`/admin/reports/${report.id}`} key={report.id}><div className="min-w-0 flex-1"><p className="text-xs font-bold text-danger">REPORT #{String(report.id).padStart(4, "0")}</p><h2 className="mt-1 truncate font-semibold">{report.place?.name}</h2><p className="text-xs text-stone-500">{report.reason}</p></div><span className={`mr-3 rounded-full px-3 py-1 text-xs ${report.status === "pending" ? "bg-orange-100 text-brand" : "bg-green-100 text-leaf"}`}>{report.status}</span><ChevronRight size={18} /></Link>
      )) : <EmptyState title="ยังไม่มีรายงาน" />}</section>
    </div>
  );
}
```

เมื่อคัดครบไฟล์ ให้รัน `npm run lint` และแก้ error ของไฟล์นี้ก่อนก้อนถัดไป

## 34. `FrontEnd/src/pages/admin/AdminReportDetailPage.jsx`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนตามหมายเลข

### 34.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/admin/AdminReportDetailPage.jsx`

**ตำแหน่งวาง:** วางหลัง comment แบบฝึกหัดเดิม; import ต้องอยู่บนสุดของไฟล์

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 34.1 ทำหน้าที่ `import { useEffect, useState } from "react";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import { useEffect, useState } from "react";
```

### 34.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/admin/AdminReportDetailPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 34.2 ทำหน้าที่ `import { useNavigate, useParams } from "react-router-dom";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import { useNavigate, useParams } from "react-router-dom";
```

### 34.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/admin/AdminReportDetailPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 34.3 ทำหน้าที่ `import PageHeader from "../../components/PageHeader.jsx";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import PageHeader from "../../components/PageHeader.jsx";
```

### 34.4 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/admin/AdminReportDetailPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 34.4 ทำหน้าที่ `import { Button, PageLoader } from "../../components/ui.jsx";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import { Button, PageLoader } from "../../components/ui.jsx";
```

### 34.5 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/admin/AdminReportDetailPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 34.5 ทำหน้าที่ `import api, { getAssetUrl, getErrorMessage } from "../../services/api.js";` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
import api, { getAssetUrl, getErrorMessage } from "../../services/api.js";
```

### 34.6 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/admin/AdminReportDetailPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 34.6 ทำหน้าที่ `export default function AdminReportDetailPage() {` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
export default function AdminReportDetailPage() {
  const { id } = useParams();
  const navigate = useNavigate();
  const [report, setReport] = useState(null);
  useEffect(() => {
    api.get(`/reports/${id}`)
      .then((response) => setReport(response.data.report))
      .catch((error) => console.error(getErrorMessage(error)));
  }, [id]);
  if (!report) return <PageLoader />;
```

### 34.7 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/admin/AdminReportDetailPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 34.7 ทำหน้าที่ `async function update(status) {` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
  async function update(status) {
    try {
      await api.patch(`/reports/${id}/status`, { status });
      navigate("/admin/reports");
    } catch (error) {
      console.error(getErrorMessage(error));
    }
  }
```

### 34.8 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/admin/AdminReportDetailPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 34.8 ทำหน้าที่ `Backend ตั้ง onDelete: Cascade จึงลบ Reports ของ Place นี้ตามไปด้วย` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
  async function deleteReportedPlace() {
    const confirmed = window.confirm(
      "ยืนยันลบสถานที่นี้? Marker และรายงานทั้งหมดของสถานที่จะถูกลบด้วย"
    );

    if (!confirmed) return;

    // Backend ตั้ง onDelete: Cascade จึงลบ Reports ของ Place นี้ตามไปด้วย
    try {
      await api.delete(`/places/${report.place.id}`);
      navigate("/admin/reports");
    } catch (error) {
      console.error(getErrorMessage(error));
    }
  }
```

### 34.9 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/pages/admin/AdminReportDetailPage.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 34.9 ทำหน้าที่ `return (` เพื่อประกอบหน้าจอหนึ่ง Route โดยเชื่อม State, Form, Component และ API เข้าด้วยกัน

**โค้ดก้อนนี้ทำงานอย่างไร:** Page รับการกระทำจากผู้ใช้ เก็บค่าใน State ตรวจหรือส่งข้อมูลผ่าน Service แล้ว render loading/error/success ตามผลลัพธ์ การเปลี่ยน State จะทำให้หน้าจออัปเดต

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อเพิ่ม Route ใน `App.jsx` ผู้ใช้จะเปิดหน้าจอนี้และใช้งานขั้นตอนของระบบที่เกี่ยวข้องได้จาก Browser

```jsx
return (
    <div><PageHeader title="รายละเอียดรายงาน" back />
      <div className="h-48 overflow-hidden bg-green-100">{report.place.imageUrl && <img className="h-full w-full object-cover" src={getAssetUrl(report.place.imageUrl)} alt={report.place.name} />}</div>
      <section className="px-5 py-5"><p className="text-xs font-bold text-danger">REPORT #{String(report.id).padStart(4, "0")}</p><h1 className="mt-2 text-2xl font-bold">{report.place.name}</h1><p className="text-sm text-stone-500">{report.place.address}</p><div className="mt-6 rounded-2xl bg-white p-4"><p className="text-sm font-semibold">เหตุผล: {report.reason}</p><p className="mt-2 text-sm leading-6 text-stone-600">{report.description || "ไม่มีรายละเอียดเพิ่มเติม"}</p><p className="mt-4 text-xs text-stone-400">รายงานโดย {report.reporter.name} · {report.reporter.email}</p></div>
        {report.evidenceImageUrl && <div className="mt-4"><p className="mb-2 text-sm font-semibold">รูปหลักฐานจากผู้รายงาน</p><img className="max-h-80 w-full rounded-2xl object-cover" src={getAssetUrl(report.evidenceImageUrl)} alt="รูปหลักฐานรายงาน" /></div>}
        {report.status === "pending" ? <div className="mt-8 grid grid-cols-2 gap-3"><Button variant="success" onClick={deleteReportedPlace}>ดำเนินการลบ</Button><Button variant="danger" onClick={() => update("rejected")}>ปฏิเสธรายงาน</Button></div> : <p className="mt-8 rounded-xl bg-green-50 p-4 text-center font-semibold text-leaf">รายงานนี้: {report.status}</p>}
      </section>
    </div>
  );
}
```

เมื่อคัดครบไฟล์ ให้รัน `npm run lint` และแก้ error ของไฟล์นี้ก่อนก้อนถัดไป

## 35. `FrontEnd/src/App.jsx`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนตามหมายเลข

### 35.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `FrontEnd/src/App.jsx`

**ตำแหน่งวาง:** วางหลัง comment แบบฝึกหัดเดิม; import ต้องอยู่บนสุดของไฟล์

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 35.1 ทำหน้าที่ `import { Navigate, Route, Routes } from "react-router-dom";` เพื่อกำหนดตารางเส้นทางของ Frontend ว่า URL ใดต้องแสดง Page ใดและเส้นทางใดต้องป้องกันสิทธิ์

**โค้ดก้อนนี้ทำงานอย่างไร:** React Router เปรียบเทียบ URL กับ `<Route>` แล้ว render Page ที่ตรงกัน ส่วน ProtectedRoute ตรวจ Login/role ก่อนให้เข้าหน้าเฉพาะ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Route ครบ ผู้ใช้จะนำทางระหว่างหน้าสมาชิกและ Admin ได้ และ URL ที่ไม่รู้จักจะถูกส่งกลับหน้าที่กำหนด

```jsx
import { Navigate, Route, Routes } from "react-router-dom";
```

### 35.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/App.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 35.2 ทำหน้าที่ `import ProtectedRoute from "./components/ProtectedRoute.jsx";` เพื่อกำหนดตารางเส้นทางของ Frontend ว่า URL ใดต้องแสดง Page ใดและเส้นทางใดต้องป้องกันสิทธิ์

**โค้ดก้อนนี้ทำงานอย่างไร:** React Router เปรียบเทียบ URL กับ `<Route>` แล้ว render Page ที่ตรงกัน ส่วน ProtectedRoute ตรวจ Login/role ก่อนให้เข้าหน้าเฉพาะ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Route ครบ ผู้ใช้จะนำทางระหว่างหน้าสมาชิกและ Admin ได้ และ URL ที่ไม่รู้จักจะถูกส่งกลับหน้าที่กำหนด

```jsx
import ProtectedRoute from "./components/ProtectedRoute.jsx";
```

### 35.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/App.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 35.3 ทำหน้าที่ `import AppLayout from "./layouts/AppLayout.jsx";` เพื่อกำหนดตารางเส้นทางของ Frontend ว่า URL ใดต้องแสดง Page ใดและเส้นทางใดต้องป้องกันสิทธิ์

**โค้ดก้อนนี้ทำงานอย่างไร:** React Router เปรียบเทียบ URL กับ `<Route>` แล้ว render Page ที่ตรงกัน ส่วน ProtectedRoute ตรวจ Login/role ก่อนให้เข้าหน้าเฉพาะ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Route ครบ ผู้ใช้จะนำทางระหว่างหน้าสมาชิกและ Admin ได้ และ URL ที่ไม่รู้จักจะถูกส่งกลับหน้าที่กำหนด

```jsx
import AppLayout from "./layouts/AppLayout.jsx";
```

### 35.4 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/App.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 35.4 ทำหน้าที่ `import AddPlacePage from "./pages/AddPlacePage.jsx";` เพื่อกำหนดตารางเส้นทางของ Frontend ว่า URL ใดต้องแสดง Page ใดและเส้นทางใดต้องป้องกันสิทธิ์

**โค้ดก้อนนี้ทำงานอย่างไร:** React Router เปรียบเทียบ URL กับ `<Route>` แล้ว render Page ที่ตรงกัน ส่วน ProtectedRoute ตรวจ Login/role ก่อนให้เข้าหน้าเฉพาะ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Route ครบ ผู้ใช้จะนำทางระหว่างหน้าสมาชิกและ Admin ได้ และ URL ที่ไม่รู้จักจะถูกส่งกลับหน้าที่กำหนด

```jsx
import AddPlacePage from "./pages/AddPlacePage.jsx";
```

### 35.5 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/App.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 35.5 ทำหน้าที่ `import LoginPage from "./pages/LoginPage.jsx";` เพื่อกำหนดตารางเส้นทางของ Frontend ว่า URL ใดต้องแสดง Page ใดและเส้นทางใดต้องป้องกันสิทธิ์

**โค้ดก้อนนี้ทำงานอย่างไร:** React Router เปรียบเทียบ URL กับ `<Route>` แล้ว render Page ที่ตรงกัน ส่วน ProtectedRoute ตรวจ Login/role ก่อนให้เข้าหน้าเฉพาะ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Route ครบ ผู้ใช้จะนำทางระหว่างหน้าสมาชิกและ Admin ได้ และ URL ที่ไม่รู้จักจะถูกส่งกลับหน้าที่กำหนด

```jsx
import LoginPage from "./pages/LoginPage.jsx";
```

### 35.6 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/App.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 35.6 ทำหน้าที่ `import MapHomePage from "./pages/MapHomePage.jsx";` เพื่อกำหนดตารางเส้นทางของ Frontend ว่า URL ใดต้องแสดง Page ใดและเส้นทางใดต้องป้องกันสิทธิ์

**โค้ดก้อนนี้ทำงานอย่างไร:** React Router เปรียบเทียบ URL กับ `<Route>` แล้ว render Page ที่ตรงกัน ส่วน ProtectedRoute ตรวจ Login/role ก่อนให้เข้าหน้าเฉพาะ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Route ครบ ผู้ใช้จะนำทางระหว่างหน้าสมาชิกและ Admin ได้ และ URL ที่ไม่รู้จักจะถูกส่งกลับหน้าที่กำหนด

```jsx
import MapHomePage from "./pages/MapHomePage.jsx";
```

### 35.7 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/App.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 35.7 ทำหน้าที่ `import PlaceDetailPage from "./pages/PlaceDetailPage.jsx";` เพื่อกำหนดตารางเส้นทางของ Frontend ว่า URL ใดต้องแสดง Page ใดและเส้นทางใดต้องป้องกันสิทธิ์

**โค้ดก้อนนี้ทำงานอย่างไร:** React Router เปรียบเทียบ URL กับ `<Route>` แล้ว render Page ที่ตรงกัน ส่วน ProtectedRoute ตรวจ Login/role ก่อนให้เข้าหน้าเฉพาะ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Route ครบ ผู้ใช้จะนำทางระหว่างหน้าสมาชิกและ Admin ได้ และ URL ที่ไม่รู้จักจะถูกส่งกลับหน้าที่กำหนด

```jsx
import PlaceDetailPage from "./pages/PlaceDetailPage.jsx";
```

### 35.8 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/App.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 35.8 ทำหน้าที่ `import ProfilePage from "./pages/ProfilePage.jsx";` เพื่อกำหนดตารางเส้นทางของ Frontend ว่า URL ใดต้องแสดง Page ใดและเส้นทางใดต้องป้องกันสิทธิ์

**โค้ดก้อนนี้ทำงานอย่างไร:** React Router เปรียบเทียบ URL กับ `<Route>` แล้ว render Page ที่ตรงกัน ส่วน ProtectedRoute ตรวจ Login/role ก่อนให้เข้าหน้าเฉพาะ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Route ครบ ผู้ใช้จะนำทางระหว่างหน้าสมาชิกและ Admin ได้ และ URL ที่ไม่รู้จักจะถูกส่งกลับหน้าที่กำหนด

```jsx
import ProfilePage from "./pages/ProfilePage.jsx";
```

### 35.9 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/App.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 35.9 ทำหน้าที่ `import RegisterPage from "./pages/RegisterPage.jsx";` เพื่อกำหนดตารางเส้นทางของ Frontend ว่า URL ใดต้องแสดง Page ใดและเส้นทางใดต้องป้องกันสิทธิ์

**โค้ดก้อนนี้ทำงานอย่างไร:** React Router เปรียบเทียบ URL กับ `<Route>` แล้ว render Page ที่ตรงกัน ส่วน ProtectedRoute ตรวจ Login/role ก่อนให้เข้าหน้าเฉพาะ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Route ครบ ผู้ใช้จะนำทางระหว่างหน้าสมาชิกและ Admin ได้ และ URL ที่ไม่รู้จักจะถูกส่งกลับหน้าที่กำหนด

```jsx
import RegisterPage from "./pages/RegisterPage.jsx";
```

### 35.10 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/App.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 35.10 ทำหน้าที่ `import ReportPage from "./pages/ReportPage.jsx";` เพื่อกำหนดตารางเส้นทางของ Frontend ว่า URL ใดต้องแสดง Page ใดและเส้นทางใดต้องป้องกันสิทธิ์

**โค้ดก้อนนี้ทำงานอย่างไร:** React Router เปรียบเทียบ URL กับ `<Route>` แล้ว render Page ที่ตรงกัน ส่วน ProtectedRoute ตรวจ Login/role ก่อนให้เข้าหน้าเฉพาะ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Route ครบ ผู้ใช้จะนำทางระหว่างหน้าสมาชิกและ Admin ได้ และ URL ที่ไม่รู้จักจะถูกส่งกลับหน้าที่กำหนด

```jsx
import ReportPage from "./pages/ReportPage.jsx";
```

### 35.11 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/App.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 35.11 ทำหน้าที่ `import SearchPage from "./pages/SearchPage.jsx";` เพื่อกำหนดตารางเส้นทางของ Frontend ว่า URL ใดต้องแสดง Page ใดและเส้นทางใดต้องป้องกันสิทธิ์

**โค้ดก้อนนี้ทำงานอย่างไร:** React Router เปรียบเทียบ URL กับ `<Route>` แล้ว render Page ที่ตรงกัน ส่วน ProtectedRoute ตรวจ Login/role ก่อนให้เข้าหน้าเฉพาะ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Route ครบ ผู้ใช้จะนำทางระหว่างหน้าสมาชิกและ Admin ได้ และ URL ที่ไม่รู้จักจะถูกส่งกลับหน้าที่กำหนด

```jsx
import SearchPage from "./pages/SearchPage.jsx";
```

### 35.12 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/App.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 35.12 ทำหน้าที่ `import AdminPendingPlacesPage from "./pages/admin/AdminPendingPlacesPage.jsx";` เพื่อกำหนดตารางเส้นทางของ Frontend ว่า URL ใดต้องแสดง Page ใดและเส้นทางใดต้องป้องกันสิทธิ์

**โค้ดก้อนนี้ทำงานอย่างไร:** React Router เปรียบเทียบ URL กับ `<Route>` แล้ว render Page ที่ตรงกัน ส่วน ProtectedRoute ตรวจ Login/role ก่อนให้เข้าหน้าเฉพาะ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Route ครบ ผู้ใช้จะนำทางระหว่างหน้าสมาชิกและ Admin ได้ และ URL ที่ไม่รู้จักจะถูกส่งกลับหน้าที่กำหนด

```jsx
import AdminPendingPlacesPage from "./pages/admin/AdminPendingPlacesPage.jsx";
```

### 35.13 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/App.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 35.13 ทำหน้าที่ `import AdminPlacesPage from "./pages/admin/AdminPlacesPage.jsx";` เพื่อกำหนดตารางเส้นทางของ Frontend ว่า URL ใดต้องแสดง Page ใดและเส้นทางใดต้องป้องกันสิทธิ์

**โค้ดก้อนนี้ทำงานอย่างไร:** React Router เปรียบเทียบ URL กับ `<Route>` แล้ว render Page ที่ตรงกัน ส่วน ProtectedRoute ตรวจ Login/role ก่อนให้เข้าหน้าเฉพาะ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Route ครบ ผู้ใช้จะนำทางระหว่างหน้าสมาชิกและ Admin ได้ และ URL ที่ไม่รู้จักจะถูกส่งกลับหน้าที่กำหนด

```jsx
import AdminPlacesPage from "./pages/admin/AdminPlacesPage.jsx";
```

### 35.14 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/App.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 35.14 ทำหน้าที่ `import AdminReportDetailPage from "./pages/admin/AdminReportDetailPage.jsx";` เพื่อกำหนดตารางเส้นทางของ Frontend ว่า URL ใดต้องแสดง Page ใดและเส้นทางใดต้องป้องกันสิทธิ์

**โค้ดก้อนนี้ทำงานอย่างไร:** React Router เปรียบเทียบ URL กับ `<Route>` แล้ว render Page ที่ตรงกัน ส่วน ProtectedRoute ตรวจ Login/role ก่อนให้เข้าหน้าเฉพาะ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Route ครบ ผู้ใช้จะนำทางระหว่างหน้าสมาชิกและ Admin ได้ และ URL ที่ไม่รู้จักจะถูกส่งกลับหน้าที่กำหนด

```jsx
import AdminReportDetailPage from "./pages/admin/AdminReportDetailPage.jsx";
```

### 35.15 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/App.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 35.15 ทำหน้าที่ `import AdminReportsPage from "./pages/admin/AdminReportsPage.jsx";` เพื่อกำหนดตารางเส้นทางของ Frontend ว่า URL ใดต้องแสดง Page ใดและเส้นทางใดต้องป้องกันสิทธิ์

**โค้ดก้อนนี้ทำงานอย่างไร:** React Router เปรียบเทียบ URL กับ `<Route>` แล้ว render Page ที่ตรงกัน ส่วน ProtectedRoute ตรวจ Login/role ก่อนให้เข้าหน้าเฉพาะ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Route ครบ ผู้ใช้จะนำทางระหว่างหน้าสมาชิกและ Admin ได้ และ URL ที่ไม่รู้จักจะถูกส่งกลับหน้าที่กำหนด

```jsx
import AdminReportsPage from "./pages/admin/AdminReportsPage.jsx";
```

### 35.16 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/App.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 35.16 ทำหน้าที่ `export default function App() {` เพื่อกำหนดตารางเส้นทางของ Frontend ว่า URL ใดต้องแสดง Page ใดและเส้นทางใดต้องป้องกันสิทธิ์

**โค้ดก้อนนี้ทำงานอย่างไร:** React Router เปรียบเทียบ URL กับ `<Route>` แล้ว render Page ที่ตรงกัน ส่วน ProtectedRoute ตรวจ Login/role ก่อนให้เข้าหน้าเฉพาะ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ Route ครบ ผู้ใช้จะนำทางระหว่างหน้าสมาชิกและ Admin ได้ และ URL ที่ไม่รู้จักจะถูกส่งกลับหน้าที่กำหนด

```jsx
export default function App() {
  return (
    <Routes>
      <Route path="/login" element={<LoginPage />} />
      <Route path="/register" element={<RegisterPage />} />

      <Route element={<ProtectedRoute />}>
        <Route element={<AppLayout />}>
          <Route index element={<MapHomePage />} />
          <Route path="search" element={<SearchPage />} />
          <Route path="places/:id" element={<PlaceDetailPage />} />
          <Route path="places/:id/report" element={<ReportPage />} />
          <Route path="add-place" element={<AddPlacePage />} />
          <Route path="profile" element={<ProfilePage />} />

          <Route element={<ProtectedRoute adminOnly />}>
            <Route path="admin/add-place" element={<AddPlacePage admin />} />
            <Route path="admin/places" element={<AdminPlacesPage />} />
            <Route path="admin/pending" element={<AdminPendingPlacesPage />} />
            <Route path="admin/reports" element={<AdminReportsPage />} />
            <Route path="admin/reports/:id" element={<AdminReportDetailPage />} />
          </Route>
        </Route>
      </Route>

      <Route path="*" element={<Navigate to="/" replace />} />
    </Routes>
  );
}
```

เมื่อคัดครบไฟล์ ให้รัน `npm run lint` และแก้ error ของไฟล์นี้ก่อนก้อนถัดไป

### CHECKPOINT 10 — RUN DEV: Report และ Admin

ตอนนี้ Page และ Route ทุกกลุ่มอยู่ใน `App.jsx` ครบแล้ว ส่วน `main.jsx` ชั่วคราวจาก CHECKPOINT Auth มี Provider ที่ต้องใช้ครบ ให้เปิด Backend และรัน `npm run dev` อีกครั้ง แล้วตรวจ:

```text
Member: เปิด Place → ส่ง Report
Admin: Login บัญชี Demo → เปิดรายการ Place/Report → เปิดหน้ารายละเอียด
```

ต้องไม่มีจอขาว, import error, Route 404 หรือ API error ที่เกิดจากการประกอบ Frontend หากผ่านจึงทำหัวข้อ 36 เพื่อแทนที่ `main.jsx` ด้วยฉบับปิดงานที่จัดรูปแบบอ่านง่าย

## 36. `FrontEnd/src/main.jsx`

เปิดไฟล์นี้ ลบเฉพาะ comment แบบฝึกหัดเดิม แล้ววางก้อนตามหมายเลข

### 36.1 วางเป็นก้อนแรก

**ไฟล์ปลายทาง:** `FrontEnd/src/main.jsx`

**ตำแหน่งวาง:** วางหลัง comment แบบฝึกหัดเดิม; import ต้องอยู่บนสุดของไฟล์

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 36.1 ทำหน้าที่ `import { StrictMode } from "react";` เพื่อเป็นจุดเริ่มต้นของ Frontend และครอบ Application ด้วย Provider ที่ทุกหน้าต้องใช้

**โค้ดก้อนนี้ทำงานอย่างไร:** React mount ลง `#root` แล้ว Provider ชั้นนอกส่ง Router, Theme และ Authentication state ให้ Component ภายในตามลำดับการครอบ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ `npm run dev` Browser จะเริ่ม Application จากไฟล์นี้และทุก Route จะเข้าถึง Context ที่จำเป็นได้

```jsx
import { StrictMode } from "react";
```

### 36.2 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/main.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 36.2 ทำหน้าที่ `import { createRoot } from "react-dom/client";` เพื่อเป็นจุดเริ่มต้นของ Frontend และครอบ Application ด้วย Provider ที่ทุกหน้าต้องใช้

**โค้ดก้อนนี้ทำงานอย่างไร:** React mount ลง `#root` แล้ว Provider ชั้นนอกส่ง Router, Theme และ Authentication state ให้ Component ภายในตามลำดับการครอบ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ `npm run dev` Browser จะเริ่ม Application จากไฟล์นี้และทุก Route จะเข้าถึง Context ที่จำเป็นได้

```jsx
import { createRoot } from "react-dom/client";
```

### 36.3 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/main.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 36.3 ทำหน้าที่ `import { BrowserRouter } from "react-router-dom";` เพื่อเป็นจุดเริ่มต้นของ Frontend และครอบ Application ด้วย Provider ที่ทุกหน้าต้องใช้

**โค้ดก้อนนี้ทำงานอย่างไร:** React mount ลง `#root` แล้ว Provider ชั้นนอกส่ง Router, Theme และ Authentication state ให้ Component ภายในตามลำดับการครอบ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ `npm run dev` Browser จะเริ่ม Application จากไฟล์นี้และทุก Route จะเข้าถึง Context ที่จำเป็นได้

```jsx
import { BrowserRouter } from "react-router-dom";
```

### 36.4 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/main.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 36.4 ทำหน้าที่ `import "leaflet/dist/leaflet.css";` เพื่อเป็นจุดเริ่มต้นของ Frontend และครอบ Application ด้วย Provider ที่ทุกหน้าต้องใช้

**โค้ดก้อนนี้ทำงานอย่างไร:** React mount ลง `#root` แล้ว Provider ชั้นนอกส่ง Router, Theme และ Authentication state ให้ Component ภายในตามลำดับการครอบ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ `npm run dev` Browser จะเริ่ม Application จากไฟล์นี้และทุก Route จะเข้าถึง Context ที่จำเป็นได้

```jsx
import "leaflet/dist/leaflet.css";
```

### 36.5 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/main.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 36.5 ทำหน้าที่ `import "./index.css";` เพื่อเป็นจุดเริ่มต้นของ Frontend และครอบ Application ด้วย Provider ที่ทุกหน้าต้องใช้

**โค้ดก้อนนี้ทำงานอย่างไร:** React mount ลง `#root` แล้ว Provider ชั้นนอกส่ง Router, Theme และ Authentication state ให้ Component ภายในตามลำดับการครอบ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ `npm run dev` Browser จะเริ่ม Application จากไฟล์นี้และทุก Route จะเข้าถึง Context ที่จำเป็นได้

```jsx
import "./index.css";
```

### 36.6 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/main.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 36.6 ทำหน้าที่ `import App from "./App.jsx";` เพื่อเป็นจุดเริ่มต้นของ Frontend และครอบ Application ด้วย Provider ที่ทุกหน้าต้องใช้

**โค้ดก้อนนี้ทำงานอย่างไร:** React mount ลง `#root` แล้ว Provider ชั้นนอกส่ง Router, Theme และ Authentication state ให้ Component ภายในตามลำดับการครอบ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ `npm run dev` Browser จะเริ่ม Application จากไฟล์นี้และทุก Route จะเข้าถึง Context ที่จำเป็นได้

```jsx
import App from "./App.jsx";
```

### 36.7 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/main.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 36.7 ทำหน้าที่ `import AuthProvider from "./hooks/AuthProvider.jsx";` เพื่อเป็นจุดเริ่มต้นของ Frontend และครอบ Application ด้วย Provider ที่ทุกหน้าต้องใช้

**โค้ดก้อนนี้ทำงานอย่างไร:** React mount ลง `#root` แล้ว Provider ชั้นนอกส่ง Router, Theme และ Authentication state ให้ Component ภายในตามลำดับการครอบ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ `npm run dev` Browser จะเริ่ม Application จากไฟล์นี้และทุก Route จะเข้าถึง Context ที่จำเป็นได้

```jsx
import AuthProvider from "./hooks/AuthProvider.jsx";
```

### 36.8 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/main.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 36.8 ทำหน้าที่ `import ThemeProvider from "./hooks/ThemeProvider.jsx";` เพื่อเป็นจุดเริ่มต้นของ Frontend และครอบ Application ด้วย Provider ที่ทุกหน้าต้องใช้

**โค้ดก้อนนี้ทำงานอย่างไร:** React mount ลง `#root` แล้ว Provider ชั้นนอกส่ง Router, Theme และ Authentication state ให้ Component ภายในตามลำดับการครอบ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ `npm run dev` Browser จะเริ่ม Application จากไฟล์นี้และทุก Route จะเข้าถึง Context ที่จำเป็นได้

```jsx
import ThemeProvider from "./hooks/ThemeProvider.jsx";
```

### 36.9 วางต่อจากก้อนก่อนหน้า

**ไฟล์ปลายทาง:** `FrontEnd/src/main.jsx`

**ตำแหน่งวาง:** วางต่อท้ายบรรทัดสุดท้ายของก้อนก่อนหน้าในไฟล์นี้ หากปีกกายังเปิดอยู่ให้วางภายในปีกกาเดิม

**วิธีวาง:** คัดเฉพาะ code block ใต้หัวข้อนี้ และตรวจชื่อ import/export ก่อนบันทึก

**ทำไมต้องวางก้อนนี้:** ก้อน 36.9 ทำหน้าที่ `createRoot(document.getElementById("root")).render(` เพื่อเป็นจุดเริ่มต้นของ Frontend และครอบ Application ด้วย Provider ที่ทุกหน้าต้องใช้

**โค้ดก้อนนี้ทำงานอย่างไร:** React mount ลง `#root` แล้ว Provider ชั้นนอกส่ง Router, Theme และ Authentication state ให้ Component ภายในตามลำดับการครอบ

**หลังวางก้อนนี้จะเกิดอะไรขึ้น:** เมื่อ `npm run dev` Browser จะเริ่ม Application จากไฟล์นี้และทุก Route จะเข้าถึง Context ที่จำเป็นได้

```jsx
createRoot(document.getElementById("root")).render(
  <StrictMode>
    <ThemeProvider>
      <BrowserRouter>
        <AuthProvider>
          <App />
        </AuthProvider>
      </BrowserRouter>
    </ThemeProvider>
  </StrictMode>
);
```

เมื่อคัดครบไฟล์ ให้รัน `npm run lint` และแก้ error ของไฟล์นี้ก่อนก้อนถัดไป

## QC ปิดงานหลังผ่าน CHECKPOINT 10

```powershell
npm run lint
npm run build
npm run dev
```

เปิด Browser แล้วทดสอบให้ครบตามรายการในหัวข้อ QC ปิดงานด้านล่าง


# QC ปิดงาน

1. Backend: `npm run dev` แล้วทดสอบ Health, Register/Login, Profile, Places และ Reports ตาม CHECKPOINT 1–6
2. Frontend: `npm run lint`, `npm run build`, `npm run dev`
3. Browser: ทดสอบ Register, Login, Map/Search, Add Place, Place Detail, Report, Profile และ Admin ให้ครบ
